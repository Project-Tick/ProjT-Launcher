name: Zlib OSS-Fuzz
on: [pull_request]
concurrency:
  group: fuzz-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true
jobs:
  scope:
    name: Scope Gate
    runs-on: ubuntu-latest
    outputs:
      run: ${{ steps.scope.outputs.run }}
    steps:
      - name: Check PR template scope
        id: scope
        uses: actions/github-script@v8
        with:
          script: |
            const body = context.payload.pull_request?.body ?? "";
            const selected = /- \[[xX]\]\s+Zlib(\s|$)/.test(body);
            core.setOutput("run", selected ? "true" : "false");

  Fuzzing:
    defaults:
      run:
        working-directory: zlib
    runs-on: ubuntu-latest
    needs: scope
    if: needs.scope.outputs.run == 'true'
    steps:
    - name: Build Fuzzers
      uses: google/oss-fuzz/infra/cifuzz/actions/build_fuzzers@master
      with:
        oss-fuzz-project-name: 'zlib'
        dry-run: false

    - name: Run Fuzzers
      uses: google/oss-fuzz/infra/cifuzz/actions/run_fuzzers@master
      with:
        oss-fuzz-project-name: 'zlib'
        fuzz-seconds: 300
        dry-run: false

    - name: Upload Crash
      uses: actions/upload-artifact@v4
      if: failure()
      with:
        name: artifacts
        path: ./out/artifacts
