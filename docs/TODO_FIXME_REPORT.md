# TODO/FIXME Report

Generated by `tools/generate_todo_report.py`.

Summary:

- **complex**: 39 items
- **implementable**: 508 items
- **trivial**: 72 items
- **unknown**: 201 items

---

## Complex (39)

- **todo** in `tools/generate_todo_report.py`:49 — implementable_keywords = ['should', 'maybe', 'add', 'implement', 'todo:', 'need', 'missing', 'want', 'consider', 'allow', 'support', 'fixme', 'refactor', 'update', 'remove', 'bug']
```
        return 'complex'
    # Context-sensitive keyword matching for implementable items
    implementable_keywords = ['should', 'maybe', 'add', 'implement', 'todo:', 'need', 'missing', 'want', 'consider', 'allow', 'support', 'fixme', 'refactor', 'update', 'remove', 'bug']
    if any(k in t for k in implementable_keywords):
        return 'implementable'
```

- **FIXME** in `libqrencode/configure.ac`:57 — # FIXME: isn't it able to integrate the followings to AC_CHECK_LIB?
```
fi
AM_CONDITIONAL([HAVE_LIBPTHREAD], [test "x$ac_cv_lib_pthread_pthread_mutex_init" = "xyes" ])
# FIXME: isn't it able to integrate the followings to AC_CHECK_LIB?
if test x$ac_cv_lib_pthread_pthread_mutex_init = xyes ; then
	AC_DEFINE([HAVE_LIBPTHREAD], [1], [Define to 1 if using pthread is enabled.])
```

- **Note** in `libqrencode/Doxyfile`:321 — # performance by keeping more symbols in memory. Note that the value works on
```
# causing a significant performance penalty.
# If the system has enough physical memory increasing the cache will improve the
# performance by keeping more symbols in memory. Note that the value works on
# a logarithmic scale so increasing the size by one will roughly double the
# memory usage. The cache size is given by this formula:
```

- **Note** in `gamemode/include/gamemode_client.h`:73 — * Note: All the above requests can be blocking - dbus requests can and will block while the daemon
```
 *   returns a string describing any of the above errors
 *
 * Note: All the above requests can be blocking - dbus requests can and will block while the daemon
 * handles the request. It is not recommended to make these calls in performance critical code
 */
```

- **Note** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:415 — # Note that this only applies to C code; C++ already behaves like this.
```
# has performance benefits.
# See https://www.ibm.com/developerworks/community/blogs/zTPF/entry/benefits_of_the_fnocommon_compile_option_peter_lemieszewski?lang=en
# Note that this only applies to C code; C++ already behaves like this.
if (CMAKE_C_COMPILER_ID STREQUAL "GNU" OR
        CMAKE_C_COMPILER_ID MATCHES "Clang" OR
```

- **FIXME** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:450 — # FIXME: Is this actually necessary to have here?
```
if (CMAKE_SYSTEM_NAME STREQUAL GNU)
    # Enable multithreading with the pthread library
    # FIXME: Is this actually necessary to have here?
    #        Can CMakeLists.txt files that require it use FindThreads.cmake
    #        instead?
```

- **note** in `extra-cmake-modules/modules/ECMEnableSanitizers.cmake`:123 — # fixes "note: variable tracking size limit exceeded with
```
        endif()
        if(CMAKE_CXX_COMPILER_ID MATCHES "GNU")
            # fixes "note: variable tracking size limit exceeded with
            # -fvar-tracking-assignments, retrying without" (which is
            # another way of saying "I'll go ahead and compile this
```

- **FIXME** in `extra-cmake-modules/modules/ECMQchDoxygen.config.in`:196 — "FIXME=\xrefitem fixme \"Fixme\" \"Fixme\"" \
```
                         "acronym=\b " \
                         "licenses=\xrefitem licenses \"License(s)\" \"Licenses\"" \
                         "FIXME=\xrefitem fixme \"Fixme\" \"Fixme\"" \
                         "bc=\xrefitem bc \"Binary Compatible\" \"Binary Compatible\"" \
                         "threadsafe=\xrefitem threadsafe \"Threadsafe\" \"Threadsafe\"" \
```

- **TODO** in `ptinstaller/Source/writer.cpp`:86 — //TODO: Why does this allocate memory? It could just write the string and a manual zero character?
```
 }
#else
  //TODO: Why does this allocate memory? It could just write the string and a manual zero character?
  char *wb = new char[size];
  memset(wb, 0, size);
```

- **Note** in `ptinstaller/Contrib/Makeptisw/utils.cpp`:198 — p->SoundName = SoundName, p->MBFallback = MBFallback; // Note: The string must be valid until the sound has started because we don't copy it
```
  PLAYAPPSOUNDDATA *p = (PLAYAPPSOUNDDATA*) MemAlloc(sizeof(PLAYAPPSOUNDDATA));
  if (p) {
    p->SoundName = SoundName, p->MBFallback = MBFallback; // Note: The string must be valid until the sound has started because we don't copy it
    HANDLE hThread = CreateThread(NULL, 0, PlayAppSoundProc, p, 0, SupportsW9X() ? &tid : 0);
    if (hThread) CloseHandle(hThread); else PlayAppSoundProc(p);
```

- **TODO** in `ptinstaller/Contrib/Makeptisw/makeptisw.cpp`:819 — if (INVALID_HANDLE_VALUE != hFile) { // BUGBUG:TODO: Error message for bad hFile or failed EM_STREAMOUT?
```
          if (GetSaveFileName(&l)) {
            HANDLE hFile = CreateFile(buf, GENERIC_WRITE, 0, 0, CREATE_ALWAYS, 0, 0);
            if (INVALID_HANDLE_VALUE != hFile) { // BUGBUG:TODO: Error message for bad hFile or failed EM_STREAMOUT?
              WPARAM opts = sizeof(TCHAR) > 1 ? (SF_TEXT|SF_UNICODE) : (SF_TEXT);
              DWORD_PTR cookie[2] = { (DWORD_PTR)hFile, FALSE };
```

- **Note** in `ptinstaller/Contrib/Makeptisw/wndspy.cpp`:302 — if (pDD->DialogAwarenessContext > DPI::ac_pm2) // Is the dialog AC < PMv2? Note: The canonical AC numbers are negative.
```
    // the dialog HWND to be PMv1 because we are not going to manually reposition our controls.
    // On < 10FU1607 we only have the process awareness (not ideal).
    if (pDD->DialogAwarenessContext > DPI::ac_pm2) // Is the dialog AC < PMv2? Note: The canonical AC numbers are negative.
      DPI::SetThreadDpiAwarenessContext(DPI::ac_pm1);
    SendMessage(hDlg, WM_CAPTURECHANGED, 0, 0);
```

- **Note** in `ptinstaller/Contrib/Makeptisw/wndspy.cpp`:374 — struct ScopedThreadDpiAwarenessContext { // Note: Assumes InitializeDpiApi() has been called!
```
}

struct ScopedThreadDpiAwarenessContext { // Note: Assumes InitializeDpiApi() has been called!
  struct List {
    List() : m_l(0) {}
```

- **NOTE** in `ptinstaller/Contrib/Modern UI/Changelog.txt`:77 — (NOTE: New settings have been introduced in version 1.68)
```
* Support for uninstaller abort warning
* Setting for 3 line text on Welcome and Finish page
  (NOTE: New settings have been introduced in version 1.68)
* Language file backwards compatibility: English for missing strings
* Support for different uninstaller header image
```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nlf`:124 — #FIXME?
```
កំពុង​លុប​ចេញពី:​
# ^FileError
#FIXME?
មាន​កំហុស​ក្នុង​ការ​បើក​ឯកសារ​ដើម្បី​សរសេរ​ចូល​:\r\n\r\n$0\r\n\r\n​ សូមចុចលើ​ បញ្ឈប់​ ដើម្បី​បញ្ចប់ការ​ដំឡើង​ \r\n Retry ដើម្បី​ព្យាយាម​ម្តង​ទៀត​ ឫ បដិសេធ​ដើម្បី​បញ្ឈប់​ឯកសារនេះ​។​
# ^FileError_NoIgnore
```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nlf`:127 — #FIXME?
```
មាន​កំហុស​ក្នុង​ការ​បើក​ឯកសារ​ដើម្បី​សរសេរ​ចូល​:\r\n\r\n$0\r\n\r\n​ សូមចុចលើ​ បញ្ឈប់​ ដើម្បី​បញ្ចប់ការ​ដំឡើង​ \r\n Retry ដើម្បី​ព្យាយាម​ម្តង​ទៀត​ ឫ បដិសេធ​ដើម្បី​បញ្ឈប់​ឯកសារនេះ​។​
# ^FileError_NoIgnore
#FIXME?
មាន​កំហុស​ក្នុង​ការ​បើក​ឯកសារ​ដើម្បី​សរសេរ​ចូល​:​\r\n\r\n$0\r\n\r\n សូមចុចលើ​ Retry ដើម្បី​ព្យាយាម​ម្តង​ទៀត ឫ \r\nបដិសេធ ដើម្បី​បញ្ឈប់​ការ​ដំឡើង​។​
# ^CantWrite
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:69 — #FIXME:MUI_TEXT_ABORT_TITLE
```
  ${LangFileString} MUI_TEXT_FINISH_TITLE "Ó ti fi sí i tán"
  ${LangFileString} MUI_TEXT_FINISH_SUBTITLE "Ó ti parí lórí ètò ìṣiṣẹ́."
  #FIXME:MUI_TEXT_ABORT_TITLE 
  ${LangFileString} MUI_TEXT_ABORT_SUBTITLE "Kò lè parí ètò ìṣiṣẹ́."
!endif
```

- **Note** in `ptinstaller/Contrib/PTIS Menu/wx/setup.h`:187 — // Note that like wxUSE_THREADS this option is automatically set to 0 if
```
// slightly smaller and faster.
//
// Note that like wxUSE_THREADS this option is automatically set to 0 if
// wxNO_EXCEPTIONS is defined.
//
```

- **TODO** in `ptinstaller/Contrib/PTIS Menu/wx/setup.h`:877 — // TODO: setting to choose the generic or native one
```
#define wxUSE_DIRDLG 0

// TODO: setting to choose the generic or native one

// Use file open/save dialogs.
```

- **Note** in `ptinstaller/Contrib/VPatch/Source/GenPat/main.cpp`:146 — tout << _T("Note: filenames should never start with ") << OPT_FSCONFLICTCHARSTR << _T(" character!\n\n");
```
    tout << OPT_CHARSTR << _T("A=500    Maximum number of block matches per block (improves performance).\n");
    tout << _T("          Default is 500, larger is slower. Use ") << OPT_CHARSTR << _T("V to see the cut-off aborts.\n\n");
    tout << _T("Note: filenames should never start with ") << OPT_FSCONFLICTCHARSTR << _T(" character!\n\n");
    tout << _T("Possible exit codes:\n");
    tout << _T("  0  Success\n");
```

- **Note** in `ptinstaller/Docs/src/flowcontrol.but`:7 — Cancels the install, stops execution of script, and displays user_message in the status display. Note: you can use this from \R{callbacks}{Callback functions} to do special things. \R{pages}{Page callbacks} also uses \R{abort}{Abort} for special purposes.
```
\c [user_message]

Cancels the install, stops execution of script, and displays user_message in the status display. Note: you can use this from \R{callbacks}{Callback functions} to do special things. \R{pages}{Page callbacks} also uses \R{abort}{Abort} for special purposes.

\c Abort
```

- **note** in `ptinstaller/Docs/src/langs.but`:23 — \b If you change the language in the .onInit function, note that language strings in .onInit will still use the detected language based on the user's default Windows language because the language is initialized after .onInit.
```
\b Unlike defines that use curly braces - \{\}, language strings use parenthesis - ().

\b If you change the language in the .onInit function, note that language strings in .onInit will still use the detected language based on the user's default Windows language because the language is initialized after .onInit.

\b Always set language strings for every language in your script.
```

- **Note** in `ptinstaller/Docs/src/callback.but`:139 — This callback will be called when the uninstaller is nearly finished initializing. If the ' \R{unonInit}{un.onInit}' function calls \R{abort}{Abort}, the uninstaller will quit instantly. Note that this function can verify and/or modify $INSTDIR if necessary.
```
\S3{unonInit} un.onInit

This callback will be called when the uninstaller is nearly finished initializing. If the ' \R{unonInit}{un.onInit}' function calls \R{abort}{Abort}, the uninstaller will quit instantly. Note that this function can verify and/or modify $INSTDIR if necessary.

Here are two examples of how this might be used:
```

- **Note** in `ptinstaller/Docs/src/bin/halibut/bk_xhtml.c`:494 — * Note that you _can_ have 1.1.1.1 followed by 1.2 - you can change
```
 * (so you can't have a.b.c.d without all of a, a.b and a.b.c existing).
 *
 * Note that you _can_ have 1.1.1.1 followed by 1.2 - you can change
 * more than one level at a time. Lots of asserts, and probably part of
 * the algorithm here, rely on this being true. (It currently isn't
```

- **NOTE** in `ptinstaller/Source/exehead/util.c`:63 — 0x00000201, 0x05000000, 0x00000020, 0x00000220, // ACCESS_ALLOWED_ACE:SID (BUILTIN\Administrators) NOTE: GetAdminGrpSid() relies on this being the first SID in the ACL
```
  0x00180300, // ACCESS_ALLOWED_ACE:ACE_HEADER (ACCESS_ALLOWED_ACE_TYPE, CONTAINER_INHERIT_ACE|OBJECT_INHERIT_ACE)
  0x10000000, // ACCESS_ALLOWED_ACE:ACCESS_MASK: GENERIC_ALL
  0x00000201, 0x05000000, 0x00000020, 0x00000220, // ACCESS_ALLOWED_ACE:SID (BUILTIN\Administrators) NOTE: GetAdminGrpSid() relies on this being the first SID in the ACL
  0x00140300, // ACCESS_ALLOWED_ACE:ACE_HEADER (ACCESS_ALLOWED_ACE_TYPE, CONTAINER_INHERIT_ACE|OBJECT_INHERIT_ACE)
  0x001200c1, // ACCESS_ALLOWED_ACE:ACCESS_MASK: SYNCHRONIZE|READ_CONTROL|FILE_LIST_DIRECTORY|FILE_DELETE_CHILD|FILE_READ_ATTRIBUTES
```

- **Note** in `ptinstaller/Source/exehead/exec.c`:147 — // Note: Calling GetPTISString has the side effect that the buffer holding
```
// this function - be sure the parm being accessed is not modified before the call.
// Use a negative number to get the string validated as a file name
// Note: Calling GetPTISString has the side effect that the buffer holding
// the string to expand gets modified.
// When calling this function with numbers like 0x13, it means create the string
```

- **TODO** in `ptinstaller/Source/exehead/exec.c`:323 — // TODO push/pop flags instead -- https://sourceforge.net/p/ptis/patches/222/
```
    case EW_SETFLAG:
      {
        // TODO push/pop flags instead -- https://sourceforge.net/p/ptis/patches/222/
        static int g_statusuphack = 0;
        if (parm2 <= 0)
```

- **TODO** in `ptinstaller/Source/exehead/Main.c`:308 — // TODO: Should this be moved to ValidateTempDir() so it also updates for %windir%\Temp?
```
      // and some apps probably can't handle a read only %temp%
      // Do it before ValidateTempDir() because it appends a backslash.
      // TODO: Should this be moved to ValidateTempDir() so it also updates for %windir%\Temp?
      SetEnvironmentVariable(_T("TEMP"), state_temp_dir);
      SetEnvironmentVariable(_T("TMP"), state_temp_dir);
```

- **TODO** in `ptinstaller/Source/exehead/Ui.c`:1337 — extern HWND g_SectionHack;// TODO: Can we remove this?
```
  HWND hwndCombo1 = GetUIItem(IDC_COMBO1);
  HWND hwndTree1 = GetUIItem(IDC_TREE1);
  extern HWND g_SectionHack;// TODO: Can we remove this?
  section *sections=g_sections;
  int *install_types=g_header->install_types;
```

- **NOTE** in `launcher/tasks/ConcurrentTask.h`:122 — // NOTE: This is not thread-safe.
```

  protected:
	// NOTE: This is not thread-safe.
	unsigned int totalSize() const
	{
```

- **NOTE** in `launcher/minecraft/MinecraftInstance.cpp`:552 — // NOTE: Oracle Java option. if there are alternate jvm implementations, this would be the place to customize this
```
	auto traits_ = traits();
	// HACK: fix issues on macOS with 1.13 snapshots
	// NOTE: Oracle Java option. if there are alternate jvm implementations, this would be the place to customize this
	// for them
#ifdef Q_OS_MAC
```

- **NOTE** in `launcher/minecraft/ComponentUpdateTask.cpp`:49 — * NOTE: The 'one shot async task' nature of this implementation is a known limitation.
```

/*
 * NOTE: The 'one shot async task' nature of this implementation is a known limitation.
 * Ideally, this should be refactored into a reactor/state machine that receives input
 * from the application and dynamically adapts to changing requirements.
```

- **NOTE** in `launcher/minecraft/update/AssetUpdateTask.cpp`:84 — // NOTE: Current validation is done via AssetsUtils::loadAssetsIndexJson.
```

	QString asset_fname = "assets/indexes/" + assets->id + ".json";
	// NOTE: Current validation is done via AssetsUtils::loadAssetsIndexJson.
	// Future improvement: Implement a generic validator based on JSON schema for more robust checks.
	if (!AssetsUtils::loadAssetsIndexJson(assets->id, asset_fname, index))
```

- **NOTE** in `launcher/modplatform/helpers/HashUtils.cpp`:192 — // NOTE: Here we don't do `emitAborted()` because it will be done when `m_build_zip_future` actually
```
		{
			m_future.cancel();
			// NOTE: Here we don't do `emitAborted()` because it will be done when `m_build_zip_future` actually
			// cancels, which may not occur immediately.
			return true;
```

- **Note** in `launcher/ui/dialogs/ProfileSelectDialog.cpp`:60 — // Note: Manually populating QTreeWidget for custom rendering, pending refactor to QListView + Delegate.
```
	view->setColumnCount(1);
	view->setRootIsDecorated(false);
	// Note: Manually populating QTreeWidget for custom rendering, pending refactor to QListView + Delegate.
	if (QTreeWidgetItem* header = view->headerItem())
	{
```

- **Note** in `launcher/ui/widgets/MinecraftSettingsWidget.ui`:743 — <string>Use Zink, a Mesa OpenGL driver that implements OpenGL on top of Vulkan. Performance may vary depending on the situation. Note: If no suitable Vulkan driver is found, software rendering will be used.</string>
```
               <widget class="QCheckBox" name="useZink">
                <property name="toolTip">
                 <string>Use Zink, a Mesa OpenGL driver that implements OpenGL on top of Vulkan. Performance may vary depending on the situation. Note: If no suitable Vulkan driver is found, software rendering will be used.</string>
                </property>
                <property name="text">
```

- **Note** in `launcher/ui/instanceview/InstanceDelegate.cpp`:186 — // Note: Direct access to static Theme/Icon data is currently required.
```
				return;
			}
			// Note: Direct access to static Theme/Icon data is currently required.
			// Future refactoring should inject an IconProvider interface here.
			auto icon = QIcon::fromTheme(it.next());
```

- **Note** in `launcher/ui/pages/modplatform/ImportPage.cpp`:144 — // Note: UI validation here is intentionally lightweight (file extension/existence) to avoid blocking the
```
		if (url.isLocalFile())
		{
			// Note: UI validation here is intentionally lightweight (file extension/existence) to avoid blocking the
			// main thread. Full content validation occurs in the background InstanceImportTask.
			QFileInfo fi(input);
```

- **NOTE** in `launcher/ui/pages/instance/ManagedPackPage.h`:148 — // NOTE: Hidden page, reserved for future use.
```
	~GenericManagedPackPage() override = default;

	// NOTE: Hidden page, reserved for future use.
	bool shouldDisplay() const override
	{
```

## Implementable (508)

- **NOTE** in `CMakeLists.txt`:399 — # NOTE: CurseForge requires you to change this if you make any kind of derivative work.
```
# By using this key in your builds you accept the terms and conditions laid down in
# https://support.curseforge.com/en/support/solutions/articles/9000207405-curse-forge-3rd-party-api-terms-and-conditions
# NOTE: CurseForge requires you to change this if you make any kind of derivative work.
# This key was issued specifically for ProjT Launcher
set(Launcher_CURSEFORGE_API_KEY "$2a$10$S7KcKijbCj8mCHUQcn0tgOmtHg0kA8q9FI0niNJJ7knPq0INomzrG" CACHE STRING "API key for the CurseForge platform")
```

- **Note** in `CMakeLists.txt`:559 — # Note: Qt6 removed from fuzzer builds - fuzz_qjson_parse disabled due to glib dependency issues
```
    list(APPEND Launcher_QT_DBUS Qt6::DBus)
endif()
# Note: Qt6 removed from fuzzer builds - fuzz_qjson_parse disabled due to glib dependency issues

# System / Package Manager Provided Libs - Using bundled versions
```

- **TODO** in `TODO.md`:1 — # Project Tick Roadmap & TODO
```
# Project Tick Roadmap & TODO

This document tracks upcoming features and architectural goals for the Project Tick ecosystem, categorized by their primary implementation requirements.
```

- **Note** in `.gitignore`:373 — # Note: Comment the next line if you want to checkin your web deploy settings,
```
*.[Pp]ublish.xml
*.azurePubxml
# Note: Comment the next line if you want to checkin your web deploy settings,
# but database connection strings (with potential passwords) will be unencrypted
*.pubxml
```

- **Note** in `launcher/BaseVersionList.h`:52 — * Note that this class also inherits from QAbstractListModel. Methods from that
```
 * should check to see if it has been loaded yet and if not, load the list.
 *
 * Note that this class also inherits from QAbstractListModel. Methods from that
 * class determine how this version list shows up in a list view. Said methods
 * all have a default implementation, but they can be overridden by plugins to
```

- **NOTE** in `launcher/FileSystem.h`:178 — // NOTE: A 'shouldCopy' signal could be added here in the future for granular control.
```
		void fileCopied(const QString& relativeName);
		void copyFailed(const QString& relativeName);
		// NOTE: A 'shouldCopy' signal could be added here in the future for granular control.

	  private:
```

- **NOTE** in `launcher/CMakeLists.txt`:1651 — # NOTE: it seems that this absolutely has to be here, and nowhere else.
```
#### The bundle mess! ####
# Bundle utilities are used to complete packages for different platforms - they add all the libraries that would otherwise be missing on the target system.
# NOTE: it seems that this absolutely has to be here, and nowhere else.
if(WIN32 OR (UNIX AND APPLE))
    if(WIN32)
```

- **NOTE** in `launcher/InstanceList.cpp`:105 — // NOTE: canonicalPath requires the path to exist. Do not move this above the creation block!
```
	connect(this, &InstanceList::instancesChanged, this, &InstanceList::providerUpdated);

	// NOTE: canonicalPath requires the path to exist. Do not move this above the creation block!
	m_instDir = QDir(instDir).canonicalPath();
	m_watcher = new QFileSystemWatcher(this);
```

- **TODO** in `tools/generate_todo_report.py`:3 — Scan repository for TODO/FIXME comments and generate a Markdown report.
```
#!/usr/bin/env python3
"""
Scan repository for TODO/FIXME comments and generate a Markdown report.

Usage: python tools/generate_todo_report.py
```

- **TODO** in `tools/generate_todo_report.py`:24 — PATTERN = re.compile(r"\b(TODO|FIXME|NOTE)\b", re.IGNORECASE)
```
    OUT.parent.mkdir(parents=True, exist_ok=True)

PATTERN = re.compile(r"\b(TODO|FIXME|NOTE)\b", re.IGNORECASE)

def is_text_file(path: Path) -> bool:
```

- **TODO** in `tools/generate_todo_report.py`:107 — f.write('# TODO/FIXME Report\n\n')
```

    with open(OUT, 'w', encoding='utf-8') as f:
        f.write('# TODO/FIXME Report\n\n')
        f.write('Generated by `tools/generate_todo_report.py`.\n\n')
        f.write('Summary:\n\n')
```

- **TODO** in `ptinstaller/SConstruct`:197 — opts.Add(('PREFIX_DOC','Path to install ptis README / INSTALL / TODO files to.', dirs['doc']))
```
opts.Add(('PREFIX_BIN', 'Path to install native binaries to', dirs['bin']))
opts.Add(('PREFIX_DATA', 'Path to install ptis data to (plugins, includes, stubs, contrib, win32 binaries)', dirs['data']))
opts.Add(('PREFIX_DOC','Path to install ptis README / INSTALL / TODO files to.', dirs['doc']))
opts.Add(('PREFIX_PLUGINAPI_INC','Path to install plugin API headers to.', None))
opts.Add(('PREFIX_PLUGINAPI_LIB','Path to install plugin static library to.', None))
```

- **Note** in `ptinstaller/ptisconf.nsh`:18 — ;Note that your executable compressor should not compress the first icon.
```

;Paths should be absolute to allow building from any location.
;Note that your executable compressor should not compress the first icon.

;!packhdr temp.dat '"C:\Program Files\upx\upx" -9 -q temp.dat'
```

- **TODO** in `cmake/CompilerWarnings.cmake`:124 — # TODO support Intel compiler
```
  else()
    message(AUTHOR_WARNING "No compiler warnings set for CXX compiler: '${CMAKE_CXX_COMPILER_ID}'")
    # TODO support Intel compiler
  endif()

```

- **note** in `extra-cmake-modules/README.rst`:32 — (note that you may want to append ``${ECM_MODULE_PATH}`` to
```
  set(CMAKE_MODULE_PATH ${ECM_MODULE_PATH})

(note that you may want to append ``${ECM_MODULE_PATH}`` to
``CMAKE_MODULE_PATH`` rather than discarding the existing value).  You can then
just include the modules you require, or use ``find_package()`` as needed.  For
```

- **Note** in `extra-cmake-modules/README.rst`:52 — Note that any combination of the above includes can be used if you only want
```
  include(KDECMakeSettings)

Note that any combination of the above includes can be used if you only want
some of the settings.  Some of the functionality of
:kde-module:`KDECMakeSettings` can also be selectively disabled.
```

- **TODO** in `ci/code-quality.nix`:42 — todoCount=$(grep -r "TODO\\|FIXME" ${sourceFiles} --include="*.cpp" --include="*.h" 2>/dev/null | wc -l)
```
    echo "Checking for common code issues..."

    todoCount=$(grep -r "TODO\\|FIXME" ${sourceFiles} --include="*.cpp" --include="*.h" 2>/dev/null | wc -l)
    echo "Found $todoCount TODO/FIXME comments"

```

- **TODO** in `ci/code-quality.nix`:43 — echo "Found $todoCount TODO/FIXME comments"
```

    todoCount=$(grep -r "TODO\\|FIXME" ${sourceFiles} --include="*.cpp" --include="*.h" 2>/dev/null | wc -l)
    echo "Found $todoCount TODO/FIXME comments"

    if grep -r "qDebug\\|std::cout" ${sourceFiles} --include="*.cpp" 2>/dev/null | grep -v "// DEBUG" > /dev/null; then
```

- **NOTE** in `tests/Task_test.cpp`:91 — // NOTE: Arbitrary value that manages to trigger a problem when there is one.
```
		m_deadline.setInterval(10000);

		// NOTE: Arbitrary value that manages to trigger a problem when there is one.
		//       Considering each tasks, in a problematic state, adds 1024 * 4 bytes to the stack,
		//       this number is enough to fill up 16 MiB of stack, more than enough to cause a problem.
```

- **Note** in `.github/zizmor.yml`:4 — # instead of adding ignore comments. Note that zizmor cannot ignore by line-within-a-string, so
```
#
# For rules that contain a high number of false positives, prefer listing them here
# instead of adding ignore comments. Note that zizmor cannot ignore by line-within-a-string, so
# there are some ignore items that encompass multiple problems within one `run` block. An issue
# tracking this is at https://github.com/woodruffw/zizmor/issues/648.
```

- **FIXME** in `libqrencode/acinclude.m4`:826 — dnl FIXME: Not sure whether we should use
```
                    fi
                    if test "$acl_hardcode_minus_L" != no; then
                      dnl FIXME: Not sure whether we should use
                      dnl "-L$found_dir -l$name" or "-L$found_dir $found_so"
                      dnl here.
```

- **FIXME** in `libqrencode/acinclude.m4`:835 — dnl FIXME: Not sure whether we should use
```
                      dnl compiler. So give up. No hardcoding. This affects only
                      dnl very old systems.
                      dnl FIXME: Not sure whether we should use
                      dnl "-L$found_dir -l$name" or "-L$found_dir $found_so"
                      dnl here.
```

- **Note** in `libqrencode/Doxyfile`:26 — # identify the project. Note that if you do not use Doxywizard you need
```

# The PROJECT_NAME tag is a single word (or sequence of words) that should
# identify the project. Note that if you do not use Doxywizard you need
# to put quotes around the project name if it contains spaces.

```

- **Note** in `libqrencode/Doxyfile`:238 — # (default is Fortran), use: inc=Fortran f=C. Note that for custom extensions
```
# C++, D, PHP, Objective-C, Python, Fortran, VHDL, C, C++. For instance to make
# doxygen treat .inc files as Fortran files (default is PHP), and .f files as C
# (default is Fortran), use: inc=Fortran f=C. Note that for custom extensions
# you also need to set FILE_PATTERNS otherwise the files are not read by doxygen.

```

- **Note** in `libqrencode/Doxyfile`:871 — # standard header. Note that when using a custom header you are responsible
```
# The HTML_HEADER tag can be used to specify a personal HTML header for
# each generated HTML page. If it is left blank doxygen will generate a
# standard header. Note that when using a custom header you are responsible
#  for the proper inclusion of any scripts and style sheets that doxygen
# needs, which is dependent on the configuration options used.
```

- **Note** in `libqrencode/Doxyfile`:898 — # other source files which should be copied to the HTML output directory. Note
```

# The HTML_EXTRA_FILES tag can be used to specify one or more extra images or
# other source files which should be copied to the HTML output directory. Note
# that these files will be copied to the base HTML output directory. Use the
# $relpath$ marker in the HTML_HEADER and/or HTML_FOOTER files to load these
```

- **Note** in `libqrencode/Doxyfile`:1156 — # Note that when changing this option you need to delete any form_*.png files
```
# generated for formulas are transparent PNGs. Transparent PNGs are
# not supported properly for IE 6.0, but are supported on all modern browsers.
# Note that when changing this option you need to delete any form_*.png files
# in the HTML output before the changes have effect.

```

- **Note** in `libqrencode/Doxyfile`:1749 — # that lay further from the root node will be omitted. Note that setting this
```
# graphs generated by dot. A depth value of 3 means that only nodes reachable
# from the root by following a path via at most 3 edges will be shown. Nodes
# that lay further from the root node will be omitted. Note that setting this
# option to 1 or 2 may greatly reduce the computation time needed for large
# code bases. Also note that the size of a graph can be further restricted by
```

- **note** in `libqrencode/Doxyfile`:1751 — # code bases. Also note that the size of a graph can be further restricted by
```
# that lay further from the root node will be omitted. Note that setting this
# option to 1 or 2 may greatly reduce the computation time needed for large
# code bases. Also note that the size of a graph can be further restricted by
# DOT_GRAPH_MAX_NODES. Using a depth of 0 means no depth restriction.

```

- **FIXME** in `libqrencode/use/config.rpath`:153 — # FIXME: the MSVC++ port hasn't been tested in a loooong time
```
case "$host_os" in
  cygwin* | mingw* | pw32* | cegcc*)
    # FIXME: the MSVC++ port hasn't been tested in a loooong time
    # When not using gcc, we currently assume that we are using
    # Microsoft Visual C++.
```

- **TODO** in `.github/workflows/check.yml`:164 — # Check for TODO/FIXME
```
          fi
          
          # Check for TODO/FIXME
          todo_count=0
          while IFS= read -r file; do
```

- **TODO** in `.github/workflows/check.yml`:168 — count=$(grep -c "TODO\|FIXME" "$file" 2>/dev/null || echo 0)
```
          while IFS= read -r file; do
            if [[ -f "$file" ]]; then
              count=$(grep -c "TODO\|FIXME" "$file" 2>/dev/null || echo 0)
              if [ "$count" -gt 0 ]; then
                echo "Info: $file has $count TODO/FIXME comments"
```

- **TODO** in `.github/workflows/check.yml`:170 — echo "Info: $file has $count TODO/FIXME comments"
```
              count=$(grep -c "TODO\|FIXME" "$file" 2>/dev/null || echo 0)
              if [ "$count" -gt 0 ]; then
                echo "Info: $file has $count TODO/FIXME comments"
                ((todo_count+=count)) || true
              fi
```

- **TODO** in `.github/workflows/check.yml`:179 — echo "  Total TODO/FIXME comments: $todo_count"
```
          echo "Summary:"
          echo "  Files with debug statements: $debug_count"
          echo "  Total TODO/FIXME comments: $todo_count"
          echo ""
          echo "=== Code Quality Check Complete ==="
```

- **fixme** in `.github/codeql/codeql-config.yml`:8 — id: cpp/fixme-comment
```
query-filters:
  - exclude:
      id: cpp/fixme-comment

paths-ignore:
```

- **NOTE** in `systeminfo/src/sys_unix.cpp`:18 — // NOTE: we assume linux here. this needs further elaboration
```
	struct utsname buf;
	uname(&buf);
	// NOTE: we assume linux here. this needs further elaboration
	out.kernelType	= KernelType::Linux;
	out.kernelName	= buf.sysname;
```

- **TODO** in `meta/model/__init__.py`:242 — exclude: List[str]  # TODO maybe drop this completely?
```
    """

    exclude: List[str]  # TODO maybe drop this completely?


```

- **TODO** in `meta/model/mojang.py`:274 — id: str  # TODO: optional?
```
        return v

    id: str  # TODO: optional?
    arguments: Optional[MojangArguments]
    asset_index: Optional[MojangAssets] = Field(alias="assetIndex")
```

- **TODO** in `meta/model/mojang.py`:278 — downloads: Optional[Dict[str, MojangArtifactBase]]  # TODO improve this?
```
    asset_index: Optional[MojangAssets] = Field(alias="assetIndex")
    assets: Optional[str]
    downloads: Optional[Dict[str, MojangArtifactBase]]  # TODO improve this?
    libraries: Optional[List[Library]]  # TODO: optional?
    main_class: Optional[str] = Field(alias="mainClass")
```

- **TODO** in `meta/model/mojang.py`:279 — libraries: Optional[List[Library]]  # TODO: optional?
```
    assets: Optional[str]
    downloads: Optional[Dict[str, MojangArtifactBase]]  # TODO improve this?
    libraries: Optional[List[Library]]  # TODO: optional?
    main_class: Optional[str] = Field(alias="mainClass")
    applet_class: Optional[str] = Field(alias="appletClass")
```

- **TODO** in `meta/model/mojang.py`:324 — ):  # some versions don't have this. TODO: maybe maintain manual overrides
```
        if (
            self.javaVersion is not None
        ):  # some versions don't have this. TODO: maybe maintain manual overrides
            major = self.javaVersion.major_version
            javaName = self.javaVersion.component
```

- **NOTE** in `meta/run/update_forge.py`:228 — # NOTE: we add this later after the fact. The forge promotions file lies about these.
```
                build=build,
                branch=branch,
                # NOTE: we add this later after the fact. The forge promotions file lies about these.
                latest=False,
                recommended=is_recommended,
```

- **NOTE** in `meta/run/update_forge.py`:239 — # NOTE: we add this later after the fact. The forge promotions file lies about these.
```
                new_index.by_mc_version.setdefault(mc_version, ForgeMCVersionInfo())
            new_index.by_mc_version[mc_version].versions.append(long_version)
            # NOTE: we add this later after the fact. The forge promotions file lies about these.
            # if entry.latest:
            # new_index.by_mc_version[mc_version].latest = long_version
```

- **TODO** in `meta/run/update_forge.py`:365 — # TODO: fix those versions?
```
        else:
            # ignore the two versions without install manifests and jar mod class files
            # TODO: fix those versions?
            if version.mc_version_sane == "1.6.1":
                continue
```

- **TODO** in `meta/run/update_neoforge.py`:183 — # TODO: what *is* recommended?
```
            continue

        # TODO: what *is* recommended?
        is_recommended = False

```

- **NOTE** in `meta/run/update_neoforge.py`:193 — # NOTE: we add this later after the fact. The forge promotions file lies about these.
```
            build=build,
            branch=branch,
            # NOTE: we add this later after the fact. The forge promotions file lies about these.
            latest=False,
            recommended=is_recommended,
```

- **NOTE** in `meta/run/update_neoforge.py`:204 — # NOTE: we add this later after the fact. The forge promotions file lies about these.
```
            new_index.by_mc_version.setdefault(mc_version, NeoForgeMCVersionInfo())
        new_index.by_mc_version[mc_version].versions.append(long_version)
        # NOTE: we add this later after the fact. The forge promotions file lies about these.
        # if entry.latest:
        # new_index.by_mc_version[mc_version].latest = long_version
```

- **FIXME** in `meta/run/generate_mojang.py`:401 — # FIXME: workaround for insane log4j nonsense from December 2021. Probably needs adjustment.
```
                    bucket.libraries.append(lib)
                    bucket.release_time = v.release_time
            # FIXME: workaround for insane log4j nonsense from December 2021. Probably needs adjustment.
            elif lib.name.is_log4j():
                version_override, maven_override = map_log4j_artifact(lib.name.version)
```

- **Note** in `tests/testdata/Version/test_vectors.txt`:36 — # Note that a-a < a=a regardless since the prerelease splits the component creating a smaller first component; 0 is added to force splitting regardless
```

# Dash is included in prerelease comparison (if stripped it will be a smaller component)
# Note that a-a < a=a regardless since the prerelease splits the component creating a smaller first component; 0 is added to force splitting regardless
a0-a < a0=a

```

- **TODO** in `extra-cmake-modules/find-modules/FindFFmpeg.cmake`:226 — # TODO: We could add some magic to retrieve the version from the .pc files or version headers ourself,
```
    else()
      # We don't use PkgConfig on Windows an hence the <component>_VERSION var is empty
      # TODO: We could add some magic to retrieve the version from the .pc files or version headers ourself,
      # like https://github.com/Kitware/VTK/blob/master/CMake/FindFFMPEG.cmake
      message(WARNING "${_component}: Version check is not supported on Windows, skipping version check")
```

- **Note** in `extra-cmake-modules/find-modules/FindXCB.cmake`:71 — # Note that this list needs to be ordered such that any component
```
ecm_find_package_version_check(XCB)

# Note that this list needs to be ordered such that any component
# appears after its dependencies
set(XCB_known_components
```

- **note** in `extra-cmake-modules/find-modules/FindEGL.cmake`:17 — The version of EGL; note that this is the API version defined in the
```
    True if (the requested version of) EGL is available
``EGL_VERSION``
    The version of EGL; note that this is the API version defined in the
    headers, rather than the version of the implementation (eg: Mesa)
``EGL_LIBRARIES``
```

- **note** in `extra-cmake-modules/find-modules/FindGperf.cmake`:43 — needed to pass extra parameters to ``gperf`` (note you cannot override that
```
argument is a target, to the list of private sources of that target. The
target must not be an alias. The optional ``GENERATION_FLAGS`` argument is
needed to pass extra parameters to ``gperf`` (note you cannot override that
way the output file).

```

- **Note** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:258 — # Note that only compilers officially supported by Qt are
```
# Toolchain minimal requirements
#
# Note that only compilers officially supported by Qt are
# supported by this file; workarounds for older compilers
# will generally not be included.  See
```

- **FIXME** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:284 — # FIXME: Apple heavily modifies GCC, so checking the
```
        _kde_compiler_min_version("4.7")
    elseif (APPLE)
        # FIXME: Apple heavily modifies GCC, so checking the
        # GCC version on OS/X is not very useful.
    else()
```

- **FIXME** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:325 — # FIXME: this is included in _GNU_SOURCE in glibc; do other libc
```
if (UNIX)
    # Enable extra API for using 64-bit file offsets on 32-bit systems.
    # FIXME: this is included in _GNU_SOURCE in glibc; do other libc
    # implementation recognize it?
    _kde_add_platform_definitions(-D_LARGEFILE64_SOURCE)
```

- **TODO** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:473 — # TODO: Deal with QT_NO_EXCEPTIONS for non-gnu compilers?
```
############################################################

# TODO: Deal with QT_NO_EXCEPTIONS for non-gnu compilers?
#       This should be defined if and only if exceptions are disabled.
#       qglobal.h has some magic to set it when exceptions are disabled
```

- **FIXME** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:489 — # FIXME: are exceptions disabled by default for Intel?
```
    # http://msdn.microsoft.com/en-us/library/1deeycx5.aspx

    # FIXME: are exceptions disabled by default for Intel?
endif()

```

- **FIXME** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:626 — # FIXME: someone needs to have a closer look at the Intel compiler options
```

# -w1 turns on warnings and errors
# FIXME: someone needs to have a closer look at the Intel compiler options
if (CMAKE_C_COMPILER_ID STREQUAL "Intel" AND NOT WIN32)
    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -Wall -w1 -Wpointer-arith")
```

- **FIXME** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:681 — # FIXME: do we really want this?
```
if (WIN32)
    # Disable deprecation warnings for some API
    # FIXME: do we really want this?
    add_definitions(-D_CRT_SECURE_NO_DEPRECATE
                    -D_CRT_SECURE_NO_WARNINGS
```

- **FIXME** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:778 — # FIXME: why are these needed?  The commit log is unhelpful
```

if (APPLE)
    # FIXME: why are these needed?  The commit log is unhelpful
    # (it was introduced in svn path=/trunk/KDE/kdelibs/; revision=503025 -
    # kdelibs git commit 4e4cb9cb9a2216b63d3eabf88b8fe94ee3c898cf -
```

- **NOTE** in `extra-cmake-modules/kde-modules/KDEPackageAppTemplates.cmake`:102 — # NOTE: we also pass `--sort=name` here to check if the tar exe supports that
```
    find_program(_tar_executable NAMES gtar tar)
    if(_tar_executable)
        # NOTE: we also pass `--sort=name` here to check if the tar exe supports that
        #       this feature was only added in gnu tar v1.28
        execute_process(
```

- **TODO** in `extra-cmake-modules/kde-modules/KDEFrameworkCompilerSettings.cmake`:50 — # TODO: find a nice way to set an option default
```

# set ENABLE_BSYMBOLICFUNCTIONS default to ON when possible
# TODO: find a nice way to set an option default
# we can only use symbolic functions when everything is built with that
# otherwise we'll break function pointer based connects and method lookups
```

- **Note** in `extra-cmake-modules/kde-modules/KDEInstallDirs5.cmake`:189 — Note that if the ``KDE_INSTALL_<dir>``, ``CMAKE_INSTALL_<dir>`` or deprecated
```
CMake cache, and so the defaults above can be overridden by users.

Note that if the ``KDE_INSTALL_<dir>``, ``CMAKE_INSTALL_<dir>`` or deprecated
form of the variable are set before ``include(KDEInstallDirs)``, they won't be
overwritten by ``KDEInstallDirs``. This is useful if you eg. want to use CMake
```

- **Note** in `extra-cmake-modules/kde-modules/KDEInstallDirs5.cmake`:222 — Note that mixing absolute and relative paths, particularly for ``BINDIR``,
```
``${PROJECT_NAME}`` to provide a sensible default for this CMake option.

Note that mixing absolute and relative paths, particularly for ``BINDIR``,
``LIBDIR`` and ``INCLUDEDIR``, can cause issues with exported targets. Given
that the default values for these are relative paths, relative paths should
```

- **Note** in `extra-cmake-modules/kde-modules/KDEInstallDirs6.cmake`:152 — Note that if the ``KDE_INSTALL_<dir>`` or ``CMAKE_INSTALL_<dir>`` variables are set
```
CMake cache, and so the defaults above can be overridden by users.

Note that if the ``KDE_INSTALL_<dir>`` or ``CMAKE_INSTALL_<dir>`` variables are set
before ``include(KDEInstallDirs)``, they won't be overwritten by ``KDEInstallDirs``.
This is useful if you eg. want to use CMake command line variable definitions to
```

- **Note** in `extra-cmake-modules/kde-modules/KDEInstallDirs6.cmake`:185 — Note that mixing absolute and relative paths, particularly for ``BINDIR``,
```
``${PROJECT_NAME}`` to provide a sensible default for this CMake option.

Note that mixing absolute and relative paths, particularly for ``BINDIR``,
``LIBDIR`` and ``INCLUDEDIR``, can cause issues with exported targets. Given
that the default values for these are relative paths, relative paths should
```

- **TODO** in `extra-cmake-modules/modules/ECMGenerateExportHeader.cmake`:670 — # TODO: the generated code ideally would emit a warning if some consumer used a value
```
    if (ARGS_EXCLUDE_DEPRECATED_BEFORE_AND_AT)
        message(STATUS "Excluding from build all API deprecated before and at: ${ARGS_EXCLUDE_DEPRECATED_BEFORE_AND_AT}")
        # TODO: the generated code ideally would emit a warning if some consumer used a value
        # smaller then what the the build was done with
        _ecm_geh_generate_hex_number(_excluded_before_and_at_version_hexnumber "${ARGS_EXCLUDE_DEPRECATED_BEFORE_AND_AT}")
```

- **TODO** in `extra-cmake-modules/modules/ECMGenerateExportHeader.cmake`:768 — # TODO: MSVC seems to have issues with __declspec(deprecated) being used as enumerator attribute
```
        # reusing the existing version-controlled deprecation macros for enumerator deprecation macros
        # to avoid having to repeat all the explicit version variants
        # TODO: MSVC seems to have issues with __declspec(deprecated) being used as enumerator attribute
        # and deals only with standard [[deprecated(text)]].
        # But for now we have to keep the deprecation macros using the compiler-specific attributes,
```

- **TODO** in `extra-cmake-modules/modules/ECMAddQch.cmake`:294 — # TODO: properly find each tag file
```
        if(Qt${QT_MAJOR_VERSION}Core_FOUND AND _qtcoreTagsPath)
            string(REPLACE "." "" _version ${Qt${QT_MAJOR_VERSION}Core_VERSION})
            # TODO: properly find each tag file
            # TODO: complete list of Qt modules
            if (QT_MAJOR_VERSION EQUAL "6")
```

- **TODO** in `extra-cmake-modules/modules/ECMAddQch.cmake`:295 — # TODO: complete list of Qt modules
```
            string(REPLACE "." "" _version ${Qt${QT_MAJOR_VERSION}Core_VERSION})
            # TODO: properly find each tag file
            # TODO: complete list of Qt modules
            if (QT_MAJOR_VERSION EQUAL "6")
                set(_module_list
```

- **note** in `extra-cmake-modules/modules/ECMGenerateDBusServiceFile.cmake`:29 — .. note::
```
:kde-module:`KDEInstallDirs` it needs to be the ``_FULL_`` variant of the path variable.

.. note::
  On Windows, the macro will only use the file name part of ``<executable>`` since D-Bus
  service executables are to be installed in the same directory as the D-Bus daemon.
```

- **Note** in `extra-cmake-modules/modules/ECMCheckOutboundLicense.cmake`:40 — compatible with the specified license headers. Note that a convenient way is to use
```

This method adds a custom unit test to ensure the specified outbound license to be
compatible with the specified license headers. Note that a convenient way is to use
the CMake ``GLOB`` argument of the ``FILE`` function.

```

- **Note** in `extra-cmake-modules/modules/ECMEnableSanitizers.cmake`:33 — to your ``CMakeLists.txt``. Note that this module is included in
```
   include(ECMEnableSanitizers)

to your ``CMakeLists.txt``. Note that this module is included in
:kde-module:`KDECompilerSettings`, so projects using that module do not need to also
include this one.
```

- **note** in `extra-cmake-modules/modules/ECMEnableSanitizers.cmake`:54 — .. note::
```
"leak" requires the  "address" sanitizer.

.. note::

  To reduce the overhead induced by the instrumentation of the sanitizers, it
```

- **note** in `extra-cmake-modules/modules/ECMEnableSanitizers.cmake`:68 — .. note::
```
  cmake -DECM_ENABLE_SANITIZERS='address;leak;undefined' ..

.. note::

  Most of the sanitizers will require Clang. To enable it, use::
```

- **TODO** in `extra-cmake-modules/modules/ECMGeneratePriFile.cmake`:233 — # TODO: improve API dox, allow control over module name if target name != output name
```
  # and some projects where the target name was not the actual library output name
  # passed the output name for LIB_NAME, so .name & .module prperties are correctly set.
  # TODO: improve API dox, allow control over module name if target name != output name
  if(TARGET ${EGPF_LIB_NAME})
    get_target_property(target_type ${EGPF_LIB_NAME} TYPE)
```

- **note** in `extra-cmake-modules/modules/ECMGeneratePkgConfigFile.cmake`:37 — is ``PUBLIC``, but note that according to the
```
``DEPS`` is the list of libraries required by this library. Libraries that are
not exposed to applications should be marked with ``PRIVATE``. The default
is ``PUBLIC``, but note that according to the
`Guide to pkg-config <https://people.freedesktop.org/~dbn/pkg-config-guide.html>`_
marking dependencies as private is usually preferred. The ``PUBLIC`` and
```

- **Note** in `extra-cmake-modules/modules/ECMSetupQtPluginMacroNames.cmake`:149 — # Note: unused for Qt >= 5.15.
```
    # CMake's automoc needs help to find names of plugin metadata files in case Q_PLUGIN_METADATA
    # is indirectly used via other C++ preprocessor macros
    # Note: unused for Qt >= 5.15.
    foreach(macro_name  ${ESQMN_JSON_ARG1})
        list(APPEND CMAKE_AUTOMOC_DEPEND_FILTERS
```

- **TODO** in `extra-cmake-modules/tests/ECMGenerateExportHeaderTest/CMakeLists.txt`:89 — # TODO: wariant with DEPRECATED_BASE_VERSION
```
# test generating the library with different EXCLUDE_DEPRECATED_BEFORE_AND_AT values
# also install the generated libraries together incl. exported cmake targets, for use in tests below
# TODO: wariant with DEPRECATED_BASE_VERSION
foreach(_group_arg "" "WITH_GROUP")
    foreach(_index RANGE ${_last_index})
```

- **TODO** in `extra-cmake-modules/tests/ECMGenerateExportHeaderTest/CMakeLists.txt`:102 — # TODO: test DEPRECATED_WARNINGS_SINCE
```
# test using the library, built with different EXCLUDE_DEPRECATED_BEFORE_AND_AT values,
# while using different DISABLE_DEPRECATED_BEFORE_AND_AT values
# TODO: test DEPRECATED_WARNINGS_SINCE
foreach(_group_test_mode ${group_test_modes})
    foreach(_exclude_index RANGE ${_last_index})
```

- **NOTE** in `cmake/vcpkg-ports/vcpkg-tool-meson/portfile.cmake`:13 — universal-osx.patch # NOTE(@YongDo-Hyun): THIS IS THE ONLY CHANGE NEEDED FOR PROJT
```
  remove-freebsd-pcfile-specialization.patch
  fix-libcpp-enable-assertions.patch # https://github.com/mesonbuild/meson/pull/14548, Remove in 1.8.3
  universal-osx.patch # NOTE(@YongDo-Hyun): THIS IS THE ONLY CHANGE NEEDED FOR PROJT
)
set(scripts
```

- **Note** in `ptinstaller/Include/WinVer.nsh`:46 — ;   Note: Windows 8.1 and later will be detected as Windows 8 unless ManifestSupportedOS is set correctly!
```
;   11
;
;   Note: Windows 8.1 and later will be detected as Windows 8 unless ManifestSupportedOS is set correctly!
;
; AtLeastServicePack checks if the installer is running on Windows service pack version at least as specified.
```

- **TODO** in `ptinstaller/Include/WinVer.nsh`:439 — ####  TODO
```
    _winver_sp_noex: # OSVERSIONINFO, not OSVERSIONINFOEX

      ####  TODO
      ## For IsServerOS to support < NT4SP6, we need to check the registry
      ## here to see if we are a server and/or DC
```

- **Note** in `ptinstaller/SCons/config.py`:18 — 'defines the maximum install types. Note that this should not exceed 32, ever.',
```
  (
    'PTIS_MAX_INST_TYPES',
    'defines the maximum install types. Note that this should not exceed 32, ever.',
    32
  )
```

- **TODO** in `ptinstaller/SCons/utils.py`:297 — ioh_dc |= 0x8000 # +IMAGE_DLLCHARACTERISTICS_TERMINAL_SERVER_AWARE (TODO: Should we set this on .DLLs?)
```
		if pe.ReadMachine() != 0xaa64: # ARM64 forces exception directory?
			ioh_dc |= 0x0400 # +IMAGE_DLLCHARACTERISTICS_NO_SEH
		ioh_dc |= 0x8000 # +IMAGE_DLLCHARACTERISTICS_TERMINAL_SERVER_AWARE (TODO: Should we set this on .DLLs?)
		if not (ifh_c & 0x0001): # IMAGE_FILE_RELOCS_STRIPPED?
			ioh_dc |= 0x0040 # +IMAGE_DLLCHARACTERISTICS_DYNAMIC_BASE (ASLR)
```

- **TODO** in `ptinstaller/Source/util.cpp`:251 — tmp = nl_langinfo(CODESET); // TODO: Use libcharset or locale_charset if possible
```
    const char *tmp = "";
#ifdef HAVE_NL_LANGINFO
    tmp = nl_langinfo(CODESET); // TODO: Use libcharset or locale_charset if possible
    if (strlen(tmp) >= cchmax) tmp = "";
#endif
```

- **TODO** in `ptinstaller/Source/script.cpp`:549 — if (_T(' ')==*n || _T('\t')==*n) t=0; // TODO: _istspace()?
```
              // We only want to accept "${U+HEXDIGITS}" and not "${U+ -HEXDIGITS }"
              if (*t || _T('-')==*n || _T('+')==*n) t=0;
              if (_T(' ')==*n || _T('\t')==*n) t=0; // TODO: _istspace()?
              if (!utf32) t=0; // Don't allow "${U+0}"
              if (t)
```

- **TODO** in `ptinstaller/Source/mmap.cpp`:43 — int offset = 0; // TODO: this will be removed after converting the mapping code to 64bit.
```

  UINT64 left = size, written = 0;
  int offset = 0; // TODO: this will be removed after converting the mapping code to 64bit.
  while (left > 0 && offset >= 0)
  {
```

- **TODO** in `ptinstaller/Source/mmap.cpp`:126 — // TODO: Convert to UINT64. Right now the mapping is still limited to 31 bits.
```
}

// TODO: Convert to UINT64. Right now the mapping is still limited to 31 bits.
#ifdef _WIN32
int MMapFile::internalsetfile(HANDLE hFile, DWORD dwSize)
```

- **TODO** in `ptinstaller/Source/mmap.cpp`:347 — if (!m_iSize || offset + (int)size > m_iSize) // TODO: once m_iSize and offset is converted to unsigned 64bit, this typecast will disappear
```
  size_t size = *sizep;

  if (!m_iSize || offset + (int)size > m_iSize) // TODO: once m_iSize and offset is converted to unsigned 64bit, this typecast will disappear
  {
    extern void quit(); extern int g_display_errors;
```

- **TODO** in `ptinstaller/Source/mmap.cpp`:467 — if (!size || (offset + (int)*size > m_iSize)) // TODO: once m_iSize and offset is converted to unsigned 64bit, this typecast will disappear
```
void *MMapFake::get(int offset, size_t *size) const
{
  if (!size || (offset + (int)*size > m_iSize)) // TODO: once m_iSize and offset is converted to unsigned 64bit, this typecast will disappear
    return NULL;
  return (void *)(m_pMem + offset);
```

- **Note** in `ptinstaller/Source/ResourceEditor.cpp`:226 — buf[typestart] = buf[namestart] = '\0'; // Note: Type and Name are not decoded.
```
    const TCHAR *rt = buf + typestart + 1, *rn = buf + namestart + 1, *rl = _T("Any");
    my_strncpy(buf, Url + skip, cch);
    buf[typestart] = buf[namestart] = '\0'; // Note: Type and Name are not decoded.
    if (pipe) buf[pipe] = ':'; // "res://C|/.." => "res://C:/.." (The | replacement is technically a file:// legacy feature but we support it for res:// as well)
    for (pD = buf, pS = pD;; ++pS, ++pD) {
```

- **Note** in `ptinstaller/Source/lang.h`:103 — * key string.  Note: the positional index into the storage of
```
    /**
     * From the index into the strings, get the pointer to the
     * key string.  Note: the positional index into the storage of
     * key strings probably should not be exposed to the outside.
     *
```

- **TODO** in `ptinstaller/Source/BinInterop.cpp`:299 — // TODO: if (!result) result = GetTLBVersion16(pFileData, size, resid, high, low); // A resource in a 16-bit executable?
```
    if (!result && !resonly) result = MSTLB_GetVersion(pFileData, size, high, low); // A raw TLB file?
    if (!result) result = GetTLBVersionUsingRE(pFileData, size, resid, high, low);  // A resource in a PE file?
    // TODO: if (!result) result = GetTLBVersion16(pFileData, size, resid, high, low); // A resource in a 16-bit executable?
    free(pFileData);
  }
```

- **TODO** in `ptinstaller/Source/util.h`:356 — #if defined(_MSC_VER) && (_MSC_VER-0 < 1400) // TODO: Figure out which versions are able to optimize the memcpy
```
template<class T> inline T read_ptr_as(const void*p)
{
#if defined(_MSC_VER) && (_MSC_VER-0 < 1400) // TODO: Figure out which versions are able to optimize the memcpy
  return *(T*)(p);
#else
```

- **Note** in `ptinstaller/Source/icon.cpp`:85 — // Note: To handle cursors, use CResourceEditor::ExtractIcoCur
```
  header = (IconGroupHeader*) group;

  // Note: To handle cursors, use CResourceEditor::ExtractIcoCur
  if (MAKEINTRESOURCE((size_t) RT) != RT_GROUP_ICON)
    throw runtime_error("unsupported type");
```

- **Note** in `ptinstaller/Source/strlist.h`:334 — // Note that .name is set with the TCHAR* offset into m_strings.
```
      if (pos==-1) return alwaysreturnpos ? where : -1;

      // Note that .name is set with the TCHAR* offset into m_strings.
      newstruct.name=m_strings.add(name,((unsigned int)_tcslen(name)+1)*sizeof(TCHAR))/sizeof(TCHAR);

```

- **TODO** in `ptinstaller/Source/mmap.h`:34 — typedef DWORD maxfilesizetype; // TODO: Change to UINT64
```
{
  public:
    typedef DWORD maxfilesizetype; // TODO: Change to UINT64
    static inline UINT64 getmaxoffset() { return INT_MAX; } // PTIS offset limit is 31 bits right now

```

- **NOTE** in `ptinstaller/Source/utf.cpp`:283 — NOTE: ungetc is only guaranteed to support 1 pushback,
```
  Tries to detect a BOM at the current position in a stream.
  If a BOM is found it is eaten.
  NOTE: ungetc is only guaranteed to support 1 pushback, 
  lets hope no MBCS file starts with parts of a BOM.
  \*/
```

- **NOTE** in `ptinstaller/Source/lang.cpp`:838 — // NOTE: Only supports plain %s with no options
```

static void CreatePlatformStrfmt(const TCHAR *templ, TCHAR *out) {
  // NOTE: Only supports plain %s with no options
  for ( ;; out++ )
  {
```

- **Note** in `ptinstaller/Source/build.cpp`:347 — m_UserVarNames.add(_T("_OUTDIR"),1);       // 31 Note: nsDialogs also uses this
```
  m_UserVarNames.add(_T("HWNDPARENT"),-1);   // 29
  m_UserVarNames.add(_T("_CLICK"),-1);       // 30
  m_UserVarNames.add(_T("_OUTDIR"),1);       // 31 Note: nsDialogs also uses this

  m_iBaseVarsNum = m_UserVarNames.getnum();
```

- **Note** in `ptinstaller/Source/build.cpp`:368 — m_ShellConstants.add(_T("APPDATA"), CSIDL_APPDATA, CSIDL_COMMON_APPDATA); // Note: There is no all-users roaming appdata folder.
```
  m_ShellConstants.add(_T("FONTS"), CSIDL_FONTS, CSIDL_FONTS);
  m_ShellConstants.add(_T("TEMPLATES"), CSIDL_TEMPLATES, CSIDL_COMMON_TEMPLATES);
  m_ShellConstants.add(_T("APPDATA"), CSIDL_APPDATA, CSIDL_COMMON_APPDATA); // Note: There is no all-users roaming appdata folder.
  m_ShellConstants.add(_T("LOCALAPPDATA"), CSIDL_LOCAL_APPDATA, CSIDL_COMMON_APPDATA);
  m_ShellConstants.add(_T("PRINTHOOD"), CSIDL_PRINTHOOD, CSIDL_PRINTHOOD);
```

- **Note** in `ptinstaller/Source/build.cpp`:450 — // Note: The order matters because some of the strings are preprocessed and cf must be <= 0x40
```
  const int orgunmode = uninstall_mode, t64 = is_target_64bit(), reg = 0x80, r32 = t64 ? 0xC0 : reg, r64 = r32 ^ 0x40;
  set_uninstall_mode(0);
  // Note: The order matters because some of the strings are preprocessed and cf must be <= 0x40
  unsigned int pf       = add_asciistring(_T("ProgramFilesDir"), 0);
  unsigned int cf       = add_asciistring(_T("CommonFilesDir"), 0);
```

- **TODO** in `ptinstaller/Source/build.cpp`:454 — // TODO: 64-bit targets could use CSIDL_PROGRAM_FILES+CSIDL_PROGRAM_FILESX86?
```
  unsigned int cf       = add_asciistring(_T("CommonFilesDir"), 0);
  unsigned int pf_def   = add_asciistring(_T("C:\\Program Files")); // Ultimate fallback
  // TODO: 64-bit targets could use CSIDL_PROGRAM_FILES+CSIDL_PROGRAM_FILESX86?
  m_ShellConstants.set_values(_T("PROGRAMFILES"),   reg | pf, pf_def);
  unsigned int pf_var = add_asciistring(_T("$PROGRAMFILES")); // Fallback for the 32/64 specific constants if the WOW registry view fails
```

- **NOTE** in `ptinstaller/Source/build.cpp`:524 — // NOTE: It is impossible to know how much preprocessing will increase the size, we have to guess
```
  {
    ExpandoString<TCHAR, PTIS_MAX_STRLEN*4> buf;
    // NOTE: It is impossible to know how much preprocessing will increase the size, we have to guess
    buf.Reserve(_tcsclen(string) * 2);
    preprocess_string(buf, string, codepage); // BUGBUG: This could overflow buf
```

- **TODO** in `ptinstaller/Source/build.cpp`:2421 — // TODO: Ideally we should allow this but we must be sure that the manifest is custom and not a manifest from the stub
```
      throw std::runtime_error("Incompatible option");

    // TODO: Ideally we should allow this but we must be sure that the manifest is custom and not a manifest from the stub
    //if (res_editor->ResourceExists(MAKEINTRESOURCE(24), 1, CResourceEditor::ANYLANGID))
    //  return PS_OK; // Allow user to completely override the manifest with PEAddResource
```

- **TODO** in `ptinstaller/Source/build.cpp`:3904 — // TODO: resource-controlling class
```
  update_exehead(new_header, newsize);

  // TODO: resource-controlling class
  delete [] new_header;

```

- **TODO** in `ptinstaller/Source/writer.cpp`:107 — // TODO: Replace all of this with a simple call when GrowBuf is changed to use size_t
```
void growbuf_writer_sink::write_data(const void *data, const size_t size)
{
  // TODO: Replace all of this with a simple call when GrowBuf is changed to use size_t
  if (sizeof(size) == sizeof(sink_type::size_type) && hlp::issigned(size) == hlp::issigned<sink_type::size_type>())
  {
```

- **TODO** in `ptinstaller/Source/DialogTemplate.cpp`:285 — if (!_tcscmp(szFaceName, _T("MS Shell Dlg"))) // TODO: "MS Shell Dlg 2"?
```
void CDialogTemplate::SetFont(TCHAR* szFaceName, WORD wFontSize) {
  m_dwStyle &= ~DS_SHELLFONT;
  if (!_tcscmp(szFaceName, _T("MS Shell Dlg"))) // TODO: "MS Shell Dlg 2"?
    m_dwStyle |= DS_SHELLFONT;
  m_dwStyle |= DS_SETFONT;
```

- **Note** in `ptinstaller/Examples/bigtest.nsi`:327 — MessageBox MB_OK "Note: $INSTDIR could not be removed!" IDOK 0 ; skipped if file doesn't exist
```

  IfFileExists "$INSTDIR" 0 NoErrorMsg
    MessageBox MB_OK "Note: $INSTDIR could not be removed!" IDOK 0 ; skipped if file doesn't exist
  NoErrorMsg:

```

- **ToDo** in `ptinstaller/Scripts/release.py`:501 — ToDo()
```
ManualRelease()
UpdateWiki()
ToDo()
CloseLog()
```

- **TODO** in `ptinstaller/.github/workflows/build.yml`:13 — os: [ubuntu-20.04, ubuntu-22.04, ubuntu-24.04, macos-13, macos-14, macos-15, macos-26, windows-2022, windows-2025]  # TODO fails to find cppunit on macos-14/15
```
      matrix:
        python-version: ['3.x']  # 2.x is supported but EOL on GitHub Actions
        os: [ubuntu-20.04, ubuntu-22.04, ubuntu-24.04, macos-13, macos-14, macos-15, macos-26, windows-2022, windows-2025]  # TODO fails to find cppunit on macos-14/15
        log: [yes, no]
        include:
```

- **Note** in `ptinstaller/Contrib/MultiUser/Readme.html`:51 — Note that all settings need to be set before including the MultiUser.nsh header
```
        aspects related to user accounts and installer privileges.</p>
    <p>
        Note that all settings need to be set before including the MultiUser.nsh header
        file.</p>
    <h2>Initialization and <a name="executionlevel"></a>Execution Level&nbsp;</h2>
```

- **Note** in `ptinstaller/Contrib/nsExec/nsexec.c`:77 — // Note: This is wrong for surrogate pair combining characters but CharNextW does
```
static PWSTR MyCharNext(PCWSTR p) 
{
  // Note: This is wrong for surrogate pair combining characters but CharNextW does 
  // not support surrogate pairs correctly so we have to manually handle the pairs.
  if (!p[0]) return (PWSTR) p;
```

- **TODO** in `ptinstaller/Contrib/nsExec/nsexec.c`:429 — // TODO: This is tricky because we need the complete base character (or surrogate pair) and all the trailing combining characters for a grapheme in the buffer before we can call WideCharToMultiByte.
```
          }
          else { // UTF-16LE --> DBCS
            // TODO: This is tricky because we need the complete base character (or surrogate pair) and all the trailing combining characters for a grapheme in the buffer before we can call WideCharToMultiByte.
            utfOutput = FALSE; // For now we just treat it as DBCS
            continue;
```

- **note** in `ptinstaller/Contrib/ExDLL/exdll.c`:18 — // note if you want parameters from the stack, pop them off in order.
```


  // note if you want parameters from the stack, pop them off in order.
  // i.e. if you are called via exdll::myFunction file.dat read.txt
  // calling popstring() the first time would give you file.dat,
```

- **FIXME** in `ptinstaller/Contrib/ExDLL/SConscript`:1 — # FIXME: install assembly and pascal includes into the correct locations
```
# FIXME: install assembly and pascal includes into the correct locations

lib_target = "pluginapi"
```

- **TODO** in `ptinstaller/Contrib/Makeptisw/utils.cpp`:91 — UINT bpp = GetDeviceCaps(hDc, BITSPIXEL) * GetDeviceCaps(hDc, PLANES); // TODO: COLORRES if RASTERCAPS&RC_PALETTE?
```
UINT GetScreenBPP(HWND hWnd) {
  HDC hDc = GetDC(hWnd);
  UINT bpp = GetDeviceCaps(hDc, BITSPIXEL) * GetDeviceCaps(hDc, PLANES); // TODO: COLORRES if RASTERCAPS&RC_PALETTE?
  ReleaseDC(hWnd, hDc);
  return bpp;
```

- **TODO** in `ptinstaller/Contrib/Makeptisw/utils.cpp`:1240 — return FillRectColor(hDC, rect, RGB((((UINT)r1+r2)/2)>>8, (((UINT)g1+g2)/2)>>8, (((UINT)b1+b2)/2)>>8)); // TODO: Actually try to draw a gradient
```
    if (!((FARPROC&)gf = GetSysProcAddr("MSIMG32", "GradientFill")))
    {
      return FillRectColor(hDC, rect, RGB((((UINT)r1+r2)/2)>>8, (((UINT)g1+g2)/2)>>8, (((UINT)b1+b2)/2)>>8)); // TODO: Actually try to draw a gradient
    }
  }
```

- **note** in `ptinstaller/Contrib/Makeptisw/makeptisw.cpp`:546 — // note: removed the WM_CONTEXTMENU handling to prevent a duplicate menu appearing on
```
        // Allows the detection of the right-click menu when running on OSes below Windows 2000
        // and will then simulate the effective WM_CONTEXTMENU message that would be received
        // note: removed the WM_CONTEXTMENU handling to prevent a duplicate menu appearing on
        // Windows 2000 and higher
        case EN_MSGFILTER:
```

- **TODO** in `ptinstaller/Contrib/Makeptisw/makeptisw.cpp`:919 — if (!rok) break; // TODO: If cb is non-zero we should report a incomplete read error?
```
    if (!cch)
    {
      if (!rok) break; // TODO: If cb is non-zero we should report a incomplete read error?
      cbofs += cbio; // We only have 1 byte, need to read more to get a complete WCHAR
      continue;
```

- **Note** in `ptinstaller/Contrib/Makeptisw/resource.rc`:359 — IDB_TOOLBAR16D24        BITMAP                  "toolbar16d24.bmp" // Note: This image should NEVER be <= 256 colors because then Windows will create its own disabled effect
```
IDB_TOOLBAR             BITMAP                  "toolbar.bmp"
IDB_TOOLBAR16N24        BITMAP                  "toolbar16n24.bmp"
IDB_TOOLBAR16D24        BITMAP                  "toolbar16d24.bmp" // Note: This image should NEVER be <= 256 colors because then Windows will create its own disabled effect
IDB_TOOLBAR16H24        BITMAP                  "toolbar16h24.bmp"
IDB_TOOLBAR24N24        BITMAP                  "toolbar24n24.bmp"
```

- **Note** in `ptinstaller/Contrib/Makeptisw/resource.rc`:362 — IDB_TOOLBAR24D24        BITMAP                  "toolbar24d24.bmp" // Note: This image should NEVER be <= 256 colors because then Windows will create its own disabled effect
```
IDB_TOOLBAR16H24        BITMAP                  "toolbar16h24.bmp"
IDB_TOOLBAR24N24        BITMAP                  "toolbar24n24.bmp"
IDB_TOOLBAR24D24        BITMAP                  "toolbar24d24.bmp" // Note: This image should NEVER be <= 256 colors because then Windows will create its own disabled effect
IDB_TOOLBAR24H24        BITMAP                  "toolbar24h24.bmp"
IDB_TOOLBAR32N24        BITMAP                  "toolbar32n24.bmp"
```

- **Note** in `ptinstaller/Contrib/Makeptisw/resource.rc`:365 — IDB_TOOLBAR32D24        BITMAP                  "toolbar32d24.bmp" // Note: This image should NEVER be <= 256 colors because then Windows will create its own disabled effect
```
IDB_TOOLBAR24H24        BITMAP                  "toolbar24h24.bmp"
IDB_TOOLBAR32N24        BITMAP                  "toolbar32n24.bmp"
IDB_TOOLBAR32D24        BITMAP                  "toolbar32d24.bmp" // Note: This image should NEVER be <= 256 colors because then Windows will create its own disabled effect
IDB_TOOLBAR32H24        BITMAP                  "toolbar32h24.bmp"

```

- **Note** in `ptinstaller/Contrib/Makeptisw/toolbar.cpp`:81 — SendMessage(g_toolbar.hwnd, TB_ADDBUTTONS, BUTTONCOUNT, (LPARAM) &tbbs); // Note: TB_ADDBUTTONSW is IE4+
```
    tbbs[i].dwData = 0, tbbs[i].iString = 0;
  }
  SendMessage(g_toolbar.hwnd, TB_ADDBUTTONS, BUTTONCOUNT, (LPARAM) &tbbs); // Note: TB_ADDBUTTONSW is IE4+
  LoadToolBarImages();
}
```

- **TODO** in `ptinstaller/Contrib/Makeptisw/wndspy.cpp`:131 — if (GetWindowRect(hWnd, &r) && PtInRect(&r, pFCD->pt)) // TODO: Region?
```
  RECT r;
  FINDCHILDDATA*pFCD = (FINDCHILDDATA*) LParam;
  if (GetWindowRect(hWnd, &r) && PtInRect(&r, pFCD->pt)) // TODO: Region?
  {
    ULONG area = (r.right - r.left) * (r.bottom - r.top);
```

- **Note** in `ptinstaller/Contrib/Makeptisw/lookup.cpp`:350 — (FARPROC&) dd.RLMS = GetSysProcAddr("ADVAPI32", "RegLoadMUIStringW"); // WVista+ Note: RegLoadMUIStringA always returns ERROR_CALL_NOT_IMPLEMENTED
```
  (FARPROC&) dd.SHLIS = GetSysProcAddr("SHLWAPI", "SHLoadIndirectString"); // WXP+
  (FARPROC&) dd.SHGLN = GetSysProcAddr("SHELL32", "SHGetLocalizedName"); // WVista+
  (FARPROC&) dd.RLMS = GetSysProcAddr("ADVAPI32", "RegLoadMUIStringW"); // WVista+ Note: RegLoadMUIStringA always returns ERROR_CALL_NOT_IMPLEMENTED
  if (!dd.RLMS && ((FARPROC&) g_RLMSOld = GetSysProcAddr("SHLWAPI", (LPCSTR) 439))) dd.RLMS = RegLoadMUIStringFallbackW; // W98SE+,IE5+
  (FARPROC&) dd.PPIL = 
```

- **Note** in `ptinstaller/Contrib/Math/Math.txt`:181 — it will place a^2 to c. BUT! Note: you should use de-referencer (*) with variable,
```
        test4(a, &b) (*b = a*a; a*a*a)
In this case test4 will return a^3, and if we will call it like that test4(a,c),
it will place a^2 to c. BUT! Note: you should use de-referencer (*) with variable,
at example *b. CAUTION: never use the same variable as function internal reference
variable and external argument variable (for example test4(a,b)). It will surely 
```

- **Note** in `ptinstaller/Contrib/nsDialogs/Readme.html`:284 — <p>Note that not all controls support <a href="#mref-gettext">${NSD_GetText}</a> and some require special handling with specific messages defined in WinMessages.nsh. For example, the ListBox control requires usage of <i>LB_GETCURSEL</i> and <i>LB_GETTEXT</i>. With time, the library of macros in nsDialogs.nsh will fill with more and more macros that'll handle more cases like this.</p>
```
<p>Now that we have some controls that the user can interact with, it's time to see what the user actually does with them. For that, we'll first add a "leave callback function" to our page. In that function, we'll query the state of the text control we've created and display it to the user. To do so, we'll use the <a href="#mref-gettext">${NSD_GetText}</a> macro. Use the <a href="#mref-getstate">${NSD_GetState}</a> macro for RadioButton and CheckBox controls.</p>

<p>Note that not all controls support <a href="#mref-gettext">${NSD_GetText}</a> and some require special handling with specific messages defined in WinMessages.nsh. For example, the ListBox control requires usage of <i>LB_GETCURSEL</i> and <i>LB_GETTEXT</i>. With time, the library of macros in nsDialogs.nsh will fill with more and more macros that'll handle more cases like this.</p>
</span>
<blockquote><pre>!include nsDialogs.nsh
```

- **Note** in `ptinstaller/Contrib/AdvSplash/advsplash.txt`:41 — Note 1: fadein/fadeout supported only on win2k/winxp systems, all other systems
```
(If you already have an .onInit function, put that in it)

Note 1: fadein/fadeout supported only on win2k/winxp systems, all other systems
will show simple splash screen with Delay = Delay + FadeIn + FadeOut. Also, I've
noted my winXP uses no transparent color at 16 bpp, so at bpps lower than 32
```

- **Note** in `ptinstaller/Contrib/InstallOptions/Readme.html`:413 — <strong>Note:</strong> you should not put any extra spaces around the | characters.</td>
```
                                    If not specified, then the filter defaults to All Files|*.*<br />
                                    <br />
                                    <strong>Note:</strong> you should not put any extra spaces around the | characters.</td>
                            </tr>
                            <tr>
```

- **NOTE** in `ptinstaller/Contrib/Modern UI/Changelog.txt`:145 — (NOTE: Some of these macros have been removed in later versions)
```
* Converted Install Options INI files to use dialog units
* More ReserveFile macros
  (NOTE: Some of these macros have been removed in later versions)
* Background color can be changed with a define
* Support for multilingual branding texts
```

- **NOTE** in `ptinstaller/Contrib/Modern UI/Changelog.txt`:150 — (NOTE: The MUI_SYSTEM macro has been removed in version 1.63)
```
* Start Menu / Finish page window titles also work when using custom page commands
* Language files should be inserted after inserting the MUI_SYSTEM macro
  (NOTE: The MUI_SYSTEM macro has been removed in version 1.63)
* Define MUI_MANUALVERBOSE if you don't want the Modern UI to change the verbose settings during compilation
  (NOTE: This setting has been changed in version 1.68)
```

- **NOTE** in `ptinstaller/Contrib/Modern UI/Changelog.txt`:152 — (NOTE: This setting has been changed in version 1.68)
```
  (NOTE: The MUI_SYSTEM macro has been removed in version 1.63)
* Define MUI_MANUALVERBOSE if you don't want the Modern UI to change the verbose settings during compilation
  (NOTE: This setting has been changed in version 1.68)
    
1.61 - December 5, 2002
```

- **NOTE** in `ptinstaller/Contrib/Modern UI/Changelog.txt`:156 — (NOTE: This setting has been changed in version 1.63)
```
1.61 - December 5, 2002
* modern3.exe UI without description area
  (NOTE: This setting has been changed in version 1.63)
* Added define to show uninstall confirm page
* Added language string for finish page title and continue to uninstall
```

- **NOTE** in `ptinstaller/Contrib/Modern UI/Changelog.txt`:195 — (NOTE: These macros have been removed in version 1.4)
```
* SetPage function should be set using defines
* Different NextPage/PrevPage/FinishHeader macros for install/uninstall
  (NOTE: These macros have been removed in version 1.4)
* Interface settings can be defined (for example, MUI_ICON), no parameters for MUI_INTERFACE anymore
* New Install Options macros to read/write IO INI file value
```

- **NOTE** in `ptinstaller/Contrib/Modern UI/Changelog.txt`:203 — (NOTE: The MUI_INTERFACE macro has been removed in version 1.3)
```
* Currentpage & Install Options vars should be set using
  parameters of the MUI_INTERFACE and * MUI_INSTALLOPTIONS macros
  (NOTE: The MUI_INTERFACE macro has been removed in version 1.3)
* MultiLanguage.nsi uses the new language strings

```

- **NOTE** in `ptinstaller/Contrib/Modern UI/Changelog.txt`:207 — (NOTE: All macros mentioned here have been removed in version 1.4)
```

1.2 - September 22, 2002
  (NOTE: All macros mentioned here have been removed in version 1.4)
  
* Lots of macro system updates & fixes
```

- **NOTE** in `ptinstaller/Contrib/Modern UI/Changelog.txt`:249 — (NOTE: The MUI_FINISHHEADER macro has been removed in version 1.64)
```
1.11 - August 29, 2002
* Finish header for uninstaller can also be set using MUI_FINISHHEADER
  (NOTE: The MUI_FINISHHEADER macro has been removed in version 1.64)

1.1 - August 29, 2002
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:7 — #FIXME:MUI_TEXT_WELCOME_INFO_TEXT
```
!ifdef MUI_WELCOMEPAGE
  ${LangFileString} MUI_TEXT_WELCOME_INFO_TITLE "Emi edi $(^NameDA) Nte Ẹtịmde"
  #FIXME:MUI_TEXT_WELCOME_INFO_TEXT
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:11 — #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE
```

!ifdef MUI_UNWELCOMEPAGE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE 
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:12 — #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT
```
!ifdef MUI_UNWELCOMEPAGE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE 
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:19 — #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX
```
  ${LangFileString} MUI_TEXT_LICENSE_SUBTITLE "Mbọk kot ediomi unỵme mbemiso esịnde $(^NameDA)."
  ${LangFileString} MUI_INNERTEXT_LICENSE_BOTTOM "Edieke enyịmede kpukpru se ẹtịn̄de ke ediomi emi, fịk Ami Mmenyịme man aka iso. Ana enyịme ediomi emi mîdịghe idisịnke $(^NameDA)."
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:20 — #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
```
  ${LangFileString} MUI_INNERTEXT_LICENSE_BOTTOM "Edieke enyịmede kpukpru se ẹtịn̄de ke ediomi emi, fịk Ami Mmenyịme man aka iso. Ana enyịme ediomi emi mîdịghe idisịnke $(^NameDA)."
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:24 — #FIXME:MUI_UNTEXT_LICENSE_TITLE
```

!ifdef MUI_UNLICENSEPAGE
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:25 — #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE
```
!ifdef MUI_UNLICENSEPAGE
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:26 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM
```
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:27 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX
```
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:28 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
```
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:36 — #FIXME:MUI_TEXT_COMPONENTS_TITLE
```

!ifdef MUI_COMPONENTSPAGE
  #FIXME:MUI_TEXT_COMPONENTS_TITLE 
  #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:37 — #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE
```
!ifdef MUI_COMPONENTSPAGE
  #FIXME:MUI_TEXT_COMPONENTS_TITLE 
  #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:41 — #FIXME:MUI_UNTEXT_COMPONENTS_TITLE
```

!ifdef MUI_UNCOMPONENTSPAGE
  #FIXME:MUI_UNTEXT_COMPONENTS_TITLE 
  #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:42 — #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE
```
!ifdef MUI_UNCOMPONENTSPAGE
  #FIXME:MUI_UNTEXT_COMPONENTS_TITLE 
  #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:48 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
  ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_TITLE "Editịn̄ nte etiede"
  !ifndef PTIS_CONFIG_COMPONENTPAGE_ALTERNATIVE
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:50 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:55 — #FIXME:MUI_TEXT_DIRECTORY_TITLE
```

!ifdef MUI_DIRECTORYPAGE
  #FIXME:MUI_TEXT_DIRECTORY_TITLE 
  #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:56 — #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE
```
!ifdef MUI_DIRECTORYPAGE
  #FIXME:MUI_TEXT_DIRECTORY_TITLE 
  #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:60 — #FIXME:MUI_UNTEXT_DIRECTORY_TITLE
```

!ifdef MUI_UNDIRECTORYPAGE
  #FIXME:MUI_UNTEXT_DIRECTORY_TITLE 
  #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:61 — #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE
```
!ifdef MUI_UNDIRECTORYPAGE
  #FIXME:MUI_UNTEXT_DIRECTORY_TITLE 
  #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:89 — #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE
```

!ifdef MUI_UNFINISHPAGE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:90 — #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT
```
!ifdef MUI_UNFINISHPAGE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:91 — #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT
```
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:103 — #FIXME:MUI_TEXT_STARTMENU_TITLE
```

!ifdef MUI_STARTMENUPAGE
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:104 — #FIXME:MUI_TEXT_STARTMENU_SUBTITLE
```
!ifdef MUI_STARTMENUPAGE
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:105 — #FIXME:MUI_INNERTEXT_STARTMENU_TOP
```
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:106 — #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX
```
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:110 — #FIXME:MUI_UNTEXT_CONFIRM_TITLE
```

!ifdef MUI_UNCONFIRMPAGE
  #FIXME:MUI_UNTEXT_CONFIRM_TITLE 
  #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:111 — #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE
```
!ifdef MUI_UNCONFIRMPAGE
  #FIXME:MUI_UNTEXT_CONFIRM_TITLE 
  #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:123 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE
```

!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE "
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:124 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE "
```
!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE "
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:125 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE "
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:126 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE "
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Efik.nsh`:127 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER
```
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Catalan.nsh`:51 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "Situeu el ratolí damunt d'un component per a veure'n la descripció."
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:7 — #FIXME:MUI_TEXT_WELCOME_INFO_TEXT
```
!ifdef MUI_WELCOMEPAGE
  ${LangFileString} MUI_TEXT_WELCOME_INFO_TITLE "Anyị na-anabata gị ná Ntinye $(^NameDA)"
  #FIXME:MUI_TEXT_WELCOME_INFO_TEXT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:11 — #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE
```

!ifdef MUI_UNWELCOMEPAGE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE 
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:12 — #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT
```
!ifdef MUI_UNWELCOMEPAGE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE 
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:19 — #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX
```
  ${LangFileString} MUI_TEXT_LICENSE_SUBTITLE "Biko gụgharịa ihe e dere ná nkwekọrịta akwụkwọ ikike tupu i tinyewe $(^NameDA)."
  ${LangFileString} MUI_INNERTEXT_LICENSE_BOTTOM "Ọ bụrụ na ị nabatara ihe e dere ná nkwekọrịta ahụ, pịa M Kwere iji gaa n'ihu. Ị ga-anabatarịrị nkwekọrịta ahụ iji tinye $(^NameDA)."
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:20 — #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
```
  ${LangFileString} MUI_INNERTEXT_LICENSE_BOTTOM "Ọ bụrụ na ị nabatara ihe e dere ná nkwekọrịta ahụ, pịa M Kwere iji gaa n'ihu. Ị ga-anabatarịrị nkwekọrịta ahụ iji tinye $(^NameDA)."
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:24 — #FIXME:MUI_UNTEXT_LICENSE_TITLE
```

!ifdef MUI_UNLICENSEPAGE
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:25 — #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE
```
!ifdef MUI_UNLICENSEPAGE
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:26 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM
```
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:27 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX
```
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:28 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
```
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:36 — #FIXME:MUI_TEXT_COMPONENTS_TITLE
```

!ifdef MUI_COMPONENTSPAGE
  #FIXME:MUI_TEXT_COMPONENTS_TITLE 
  #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:37 — #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE
```
!ifdef MUI_COMPONENTSPAGE
  #FIXME:MUI_TEXT_COMPONENTS_TITLE 
  #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:41 — #FIXME:MUI_UNTEXT_COMPONENTS_TITLE
```

!ifdef MUI_UNCOMPONENTSPAGE
  #FIXME:MUI_UNTEXT_COMPONENTS_TITLE 
  #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:42 — #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE
```
!ifdef MUI_UNCOMPONENTSPAGE
  #FIXME:MUI_UNTEXT_COMPONENTS_TITLE 
  #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:48 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
  ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_TITLE "Nkọwa"
  !ifndef PTIS_CONFIG_COMPONENTPAGE_ALTERNATIVE
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:50 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:55 — #FIXME:MUI_TEXT_DIRECTORY_TITLE
```

!ifdef MUI_DIRECTORYPAGE
  #FIXME:MUI_TEXT_DIRECTORY_TITLE 
  #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:56 — #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE
```
!ifdef MUI_DIRECTORYPAGE
  #FIXME:MUI_TEXT_DIRECTORY_TITLE 
  #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:60 — #FIXME:MUI_UNTEXT_DIRECTORY_TITLE
```

!ifdef MUI_UNDIRECTORYPAGE
  #FIXME:MUI_UNTEXT_DIRECTORY_TITLE 
  #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:61 — #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE
```
!ifdef MUI_UNDIRECTORYPAGE
  #FIXME:MUI_UNTEXT_DIRECTORY_TITLE 
  #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:89 — #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE
```

!ifdef MUI_UNFINISHPAGE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:90 — #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT
```
!ifdef MUI_UNFINISHPAGE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:91 — #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT
```
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:103 — #FIXME:MUI_TEXT_STARTMENU_TITLE
```

!ifdef MUI_STARTMENUPAGE
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:104 — #FIXME:MUI_TEXT_STARTMENU_SUBTITLE
```
!ifdef MUI_STARTMENUPAGE
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:105 — #FIXME:MUI_INNERTEXT_STARTMENU_TOP
```
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:106 — #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX
```
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:110 — #FIXME:MUI_UNTEXT_CONFIRM_TITLE
```

!ifdef MUI_UNCONFIRMPAGE
  #FIXME:MUI_UNTEXT_CONFIRM_TITLE 
  #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:111 — #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE
```
!ifdef MUI_UNCONFIRMPAGE
  #FIXME:MUI_UNTEXT_CONFIRM_TITLE 
  #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:123 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE
```

!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:124 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE
```
!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:125 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:126 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Igbo.nsh`:127 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER
```
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Vietnamese.nsh`:51 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "Rê chuột lên trên một thành phần để thấy mô tả của nó."
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nlf`:45 — #FIXME?
```
&ទៅ​មុខ​​ >
# ^AgreeBtn
#FIXME?
I &យល់​ព្រម​
# ^AcceptBtn
```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nlf`:184 — #FIXME?
```
"ត្រូវបាន​រំលង​: "
# ^CopyDetails
#FIXME?
ចំលង​ពិពណ៌នាទៅ​ Clipboard
# ^LogInstall
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nlf`:86 — #FIXME:Check the components you want to install and uncheck the components you don't want to install. $_CLICK
```
Ohun tó o fẹ́
# ^ComponentsText
#FIXME:Check the components you want to install and uncheck the components you don't want to install. $_CLICK
# ^ComponentsSubText1
Yan bó o ṣe fẹ́ kó fi sí i:
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nlf`:94 — #FIXME:Check the components you want to uninstall and uncheck the components you don't want to uninstall. $_CLICK
```
Tàbí kó o yan àwọn tó wù ọ́ kó fi sí i:
# ^UnComponentsText
#FIXME:Check the components you want to uninstall and uncheck the components you don't want to uninstall. $_CLICK
# ^UnComponentsSubText1
Yan bó o ṣe fẹ́ kó yọ ọ́:
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nlf`:126 — #FIXME:"Can't write: "
```
Kò rí i ṣí fún lílò: \r\n\r\n$0\r\n\r\nTún Dán An Wò kó lè tún un ṣe, tàbí\r\nMá Ṣe É kó lè jáwọ́ fífi sí i.
# ^CantWrite
#FIXME:"Can't write: "
# ^CopyFailed
Kò rí ẹ̀dà ṣe
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nlf`:132 — #FIXME:"Registering: "
```
"Ṣe ẹ̀dà sí"
# ^Registering
#FIXME:"Registering: "
# ^Unregistering
#FIXME:"Unregistering: "
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nlf`:134 — #FIXME:"Unregistering: "
```
#FIXME:"Registering: "
# ^Unregistering
#FIXME:"Unregistering: "
# ^SymbolNotFound
"Kò rí àmì yẹn: "
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nlf`:154 — #FIXME:Error decompressing data! Corrupted installer?
```
"Kò rí i ṣe: "
# ^ErrorDecompressing
#FIXME:Error decompressing data! Corrupted installer?
# ^ErrorRegistering
#FIXME:Error registering DLL
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nlf`:156 — #FIXME:Error registering DLL
```
#FIXME:Error decompressing data! Corrupted installer?
# ^ErrorRegistering
#FIXME:Error registering DLL
# ^ExecShell
"ExecShell: "
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nlf`:166 — #FIXME:Installer corrupted: invalid opcode
```
"Wà á jáde: error writing to file "
# ^InvalidOpcode
#FIXME:Installer corrupted: invalid opcode
# ^NoOLE
"No OLE for: "
```

- **FIXME** in `ptinstaller/Contrib/Language files/Asturian.nsh`:51 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "Allugue'l mur enriba d'un componente pa ver la so descripción."
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Valencian.nsh`:125 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE
```

!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Valencian.nsh`:126 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE
```
!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Valencian.nsh`:127 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Valencian.nsh`:128 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Valencian.nsh`:129 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER
```
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Latvian.nsh`:52 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "Novietojiet peles kursoru uz komponenta, lai tiktu parādīts tā apraksts."
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nsh`:8 — ${LangFileString} MUI_TEXT_WELCOME_INFO_TEXT "កម្មវិធីជំនួយនេះនឹងនាំអ្នកដល់​ការ​ដំឡើង​កម្មវិធី​នៃកម្មវិធី $(^NameDA).$\r$\n$\r$\nIt ដែល​សូម​អោយ​អ្នក​បិទ​នូវ​កម្ម​វិធី​ទាំងឡាយ​ផ្សេង​ទៀត​មុន​ពេល​ចាប់ផ្តើម​ការ​ដំឡើង​។ ដើម្បី​ងាយ​ស្រួល​ក្នុង​ការ​កែប្រែ​ឯកសារ​នៃប្រព័ន្ធប្រតិបត្តការ​ដោយ​មិន​ចាំបាច់​ចាប់​ផ្តើម​ដំណើរ​ការ​ម៉ាស៊ីនរបស់​អ្នក​​សារ​ជា​ថ្មី។​ $\r$\n$\r$\n$_CLICK" #FIXME? BUGBUG: "It"?
```
!ifdef MUI_WELCOMEPAGE
  ${LangFileString} MUI_TEXT_WELCOME_INFO_TITLE "សូមស្វាគមន៍ចំពោះការ​ដំឡើង​កម្មវិធី​ $(^NameDA)"
  ${LangFileString} MUI_TEXT_WELCOME_INFO_TEXT "កម្មវិធីជំនួយនេះនឹងនាំអ្នកដល់​ការ​ដំឡើង​កម្មវិធី​នៃកម្មវិធី $(^NameDA).$\r$\n$\r$\nIt ដែល​សូម​អោយ​អ្នក​បិទ​នូវ​កម្ម​វិធី​ទាំងឡាយ​ផ្សេង​ទៀត​មុន​ពេល​ចាប់ផ្តើម​ការ​ដំឡើង​។ ដើម្បី​ងាយ​ស្រួល​ក្នុង​ការ​កែប្រែ​ឯកសារ​នៃប្រព័ន្ធប្រតិបត្តការ​ដោយ​មិន​ចាំបាច់​ចាប់​ផ្តើម​ដំណើរ​ការ​ម៉ាស៊ីនរបស់​អ្នក​​សារ​ជា​ថ្មី។​ $\r$\n$\r$\n$_CLICK" #FIXME? BUGBUG: "It"?
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nsh`:27 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM "ប្រសិន​បើ​អ្នក​យល់​ព្រម​តាម​កិច្ចព្រម​ព្រាង​ សូម​ចុច​ I Agree ដើម្បី​ដំណើរការ​បន្ត។ ដើម្បីលុប​កម្មវិធី​ $(^NameDA) នេះ​អ្នក​ត្រូវ​តែ​យល់ព្រម​តាម​កិច្ចព្រមព្រាង​ $_CLICK"
```
  ${LangFileString} MUI_UNTEXT_LICENSE_TITLE "កិច្ចព្រមព្រាង​អាជ្ញា​ប័ណ្ណ"
  ${LangFileString} MUI_UNTEXT_LICENSE_SUBTITLE "សូម​អាន​អាជ្ញាប័ណ្ណ​អោយ​បាន​ត្រឹម​ត្រូវ​មុន​ពេល​លុប​កម្មវិធី​ $(^NameDA)ចេញ។​"
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM "ប្រសិន​បើ​អ្នក​យល់​ព្រម​តាម​កិច្ចព្រម​ព្រាង​ សូម​ចុច​ I Agree ដើម្បី​ដំណើរការ​បន្ត។ ដើម្បីលុប​កម្មវិធី​ $(^NameDA) នេះ​អ្នក​ត្រូវ​តែ​យល់ព្រម​តាម​កិច្ចព្រមព្រាង​ $_CLICK"
  ${LangFileString} MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX "ប្រសិន​បើ​អ្នក​យល់​ព្រម​តាម​កិច្ចព្រម​ព្រាង​សូម​ចុច​ធិច​ប្រអប់​ខាងក្រោម​។ ដើម្បីលុបកម្មវិធី​ $(^NameDA) នេះ​អ្នក​ត្រូវ​តែ​យល់ព្រម​តាម​កិច្ចព្រមព្រាង​ $_CLICK"
  ${LangFileString} MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS "ប្រសិន​បើ​អ្នក​យល់​ព្រម​តាម​កិច្ចព្រម​ព្រាង​សូមជ្រើសរើស​ជំរើស​ទីមួយ​ខាង​ក្រោម​។ ដើម្បីលុប​កម្មវិធី​ $(^NameDA) នេះ​អ្នក​ត្រូវ​តែ​យល់ព្រម​តាម​កិច្ចព្រមព្រាង $_CLICK"
```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nsh`:51 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "ដាក់កណ្តុរ​បញ្ជារបស់​អ្នក​លើ​សមាសភាគ​ណា​មួយ​ដើម្បី​មើល​ការ​ពិពណ៍នារបស់​វា​​។"
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nsh`:85 — #FIXME:MUI_TEXT_FINISH_INFO_TEXT "កម្មវិធី​ $(^NameDA) បាន​ត្រូវ​ដំឡើង​រួចរាល់​នៅក្នុង​កុំព្យូទ័រ​របស់​អ្នក​។ $\r$\n$\r$\nចុច​ Finish ដើម្បីបិទកម្មវិធីជំនួយ​នេះ​"
```
!ifdef MUI_FINISHPAGE
  ${LangFileString} MUI_TEXT_FINISH_INFO_TITLE "កម្មវិធី​ជំនួយ​ក្នុងការ​ដំឡើង​ $(^NameDA) កំពុងត្រូវ​បាន​បញ្ចប់​"
  #FIXME:MUI_TEXT_FINISH_INFO_TEXT "កម្មវិធី​ $(^NameDA) បាន​ត្រូវ​ដំឡើង​រួចរាល់​នៅក្នុង​កុំព្យូទ័រ​របស់​អ្នក​។ $\r$\n$\r$\nចុច​ Finish ដើម្បីបិទកម្មវិធីជំនួយ​នេះ​"
  ${LangFileString} MUI_TEXT_FINISH_INFO_REBOOT "កុំព្យូទ័រ​របស់​អ្នក​ត្រូវតែ​ចាប់​ដំណើរ​ការ​សារ​ជាថ្មី​ដើម្បី​បំពេញ​ការ​ដំឡើង​កម្មវិធី​ $(^NameDA)​ នេះ។ តើ​អ្នក​ចង់ចាប់ផ្តើមដំណើរការ​​កុំព្យូទ័រអ្នក​ឡើង​វិញ​ពេល​នេះ​?"
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nsh`:91 — #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT "កម្មវិធី​ $(^NameDA) បាន​ត្រូវ​លុប​រួចរាល់​ពី​ក្នុង​កុំព្យូទ័រ​របស់​អ្នក​។ $\r$\n$\r$\nចុច​ Finish ដើម្បីបិទកម្មវិធីជំនួយ​នេះ​"
```
!ifdef MUI_UNFINISHPAGE
  ${LangFileString} MUI_UNTEXT_FINISH_INFO_TITLE "កម្មវិធី​ជំនួយ​ក្នុងការ​លុប​ $(^NameDA) កំពុងត្រូវ​បាន​បញ្ចប់"
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT "កម្មវិធី​ $(^NameDA) បាន​ត្រូវ​លុប​រួចរាល់​ពី​ក្នុង​កុំព្យូទ័រ​របស់​អ្នក​។ $\r$\n$\r$\nចុច​ Finish ដើម្បីបិទកម្មវិធីជំនួយ​នេះ​"
  ${LangFileString} MUI_UNTEXT_FINISH_INFO_REBOOT "កុំព្យូទ័រ​របស់​អ្នក​ត្រូវតែ​ចាប់​ដំណើរ​ការ​សារ​ជាថ្មី​ដើម្បី​បំពេញ​ការ​ដំឡើង​កម្មវិធី​ $(^NameDA)​ នេះ។ តើ​អ្នក​ចង់ចាប់ផ្តើមដំណើរការ​​កុំព្យូទ័រអ្នក​ឡើង​វិញ​ពេល​នេះ​?"
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nsh`:98 — #FIXME:MUI_TEXT_FINISH_RUN
```
  ${LangFileString} MUI_TEXT_FINISH_REBOOTNOW "ចាប់ផ្តើមដំណើរការ​​កុំព្យូទ័រឡើង​វិញ​ពេល​នេះ​"
  ${LangFileString} MUI_TEXT_FINISH_REBOOTLATER "ចង់​ដំណើរការ​កុំព្យូទ័រ​ដោយ​ខ្លួន​ឯង​ពេល​ក្រោយ​"
  #FIXME:MUI_TEXT_FINISH_RUN
  #FIXME:MUI_TEXT_FINISH_SHOWREADME 
  #FIXME:MUI_BUTTONTEXT_FINISH 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nsh`:99 — #FIXME:MUI_TEXT_FINISH_SHOWREADME
```
  ${LangFileString} MUI_TEXT_FINISH_REBOOTLATER "ចង់​ដំណើរការ​កុំព្យូទ័រ​ដោយ​ខ្លួន​ឯង​ពេល​ក្រោយ​"
  #FIXME:MUI_TEXT_FINISH_RUN
  #FIXME:MUI_TEXT_FINISH_SHOWREADME 
  #FIXME:MUI_BUTTONTEXT_FINISH 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nsh`:100 — #FIXME:MUI_BUTTONTEXT_FINISH
```
  #FIXME:MUI_TEXT_FINISH_RUN
  #FIXME:MUI_TEXT_FINISH_SHOWREADME 
  #FIXME:MUI_BUTTONTEXT_FINISH 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Khmer.nsh`:104 — #FIXME?
```

!ifdef MUI_STARTMENUPAGE
	#FIXME?
  ${LangFileString} MUI_TEXT_STARTMENU_TITLE "ជ្រើសរើស​ថត​ម៉ឺនុយ Start "
  ${LangFileString} MUI_TEXT_STARTMENU_SUBTITLE "ជ្រើសរើស​ថត​ម៉ឺនុយ Start ដើម្បី​បង្កើត​ផ្លូវ​កាត់​ទៅ​កម្ម​វិធី​ $(^NameDA) ។"
```

- **FIXME** in `ptinstaller/Contrib/Language files/Esperanto.nsh`:51 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "Poziciu la muson sur koptistero por rigardi ties priskribon."
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Indonesian.nsh`:51 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "Tunjuk ke salah satu komponen untuk melihat deskripsi tentang komponen itu."
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Arabic.nsh`:52 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "أشر بالفأرة فوق أحد المكونات لرؤية الوصف"
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Turkish.nsh`:52 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "Bileşenlerin açıklamalarını görmek için imleci bileşen üzerine götürün."
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:7 — #FIXME:MUI_TEXT_WELCOME_INFO_TEXT
```
!ifdef MUI_WELCOMEPAGE
  ${LangFileString} MUI_TEXT_WELCOME_INFO_TITLE "Èyí ni Ètò Ìṣiṣẹ́ $(^NameDA)"
  #FIXME:MUI_TEXT_WELCOME_INFO_TEXT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:11 — #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE
```

!ifdef MUI_UNWELCOMEPAGE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE 
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT "
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:12 — #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT "
```
!ifdef MUI_UNWELCOMEPAGE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE 
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT "
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:19 — #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX
```
  ${LangFileString} MUI_TEXT_LICENSE_SUBTITLE "Jọ̀wọ́ ka àdéhùn ìfúnniláṣẹ kó o tó fi $(^NameDA) sí i."
  ${LangFileString} MUI_INNERTEXT_LICENSE_BOTTOM "tó o bá fara mọ́ ohun tá a sọ nínú àdéhùn yìí, tẹ Mo Gbà kó lè máa bá a lọ. O gbọ́dọ̀ fara mọ́ àdéhùn yìí kó o tó fi $(^NameDA) sí i."
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:20 — #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
```
  ${LangFileString} MUI_INNERTEXT_LICENSE_BOTTOM "tó o bá fara mọ́ ohun tá a sọ nínú àdéhùn yìí, tẹ Mo Gbà kó lè máa bá a lọ. O gbọ́dọ̀ fara mọ́ àdéhùn yìí kó o tó fi $(^NameDA) sí i."
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:24 — #FIXME:MUI_UNTEXT_LICENSE_TITLE
```

!ifdef MUI_UNLICENSEPAGE
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:25 — #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE
```
!ifdef MUI_UNLICENSEPAGE
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:26 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM
```
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:27 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX
```
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:28 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
```
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:36 — #FIXME:MUI_TEXT_COMPONENTS_TITLE
```

!ifdef MUI_COMPONENTSPAGE
  #FIXME:MUI_TEXT_COMPONENTS_TITLE 
  #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:37 — #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE
```
!ifdef MUI_COMPONENTSPAGE
  #FIXME:MUI_TEXT_COMPONENTS_TITLE 
  #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:41 — #FIXME:MUI_UNTEXT_COMPONENTS_TITLE
```

!ifdef MUI_UNCOMPONENTSPAGE
  #FIXME:MUI_UNTEXT_COMPONENTS_TITLE 
  #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:42 — #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE
```
!ifdef MUI_UNCOMPONENTSPAGE
  #FIXME:MUI_UNTEXT_COMPONENTS_TITLE 
  #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:48 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
  ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_TITLE "Àlàyé nípa rẹ̀"
  !ifndef PTIS_CONFIG_COMPONENTPAGE_ALTERNATIVE
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:50 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:55 — #FIXME:MUI_TEXT_DIRECTORY_TITLE
```

!ifdef MUI_DIRECTORYPAGE
  #FIXME:MUI_TEXT_DIRECTORY_TITLE 
  #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:56 — #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE
```
!ifdef MUI_DIRECTORYPAGE
  #FIXME:MUI_TEXT_DIRECTORY_TITLE 
  #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:60 — #FIXME:MUI_UNTEXT_DIRECTORY_TITLE
```

!ifdef MUI_UNDIRECTORYPAGE
  #FIXME:MUI_UNTEXT_DIRECTORY_TITLE 
  #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:61 — #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE
```
!ifdef MUI_UNDIRECTORYPAGE
  #FIXME:MUI_UNTEXT_DIRECTORY_TITLE 
  #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:89 — #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE
```

!ifdef MUI_UNFINISHPAGE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:90 — #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT
```
!ifdef MUI_UNFINISHPAGE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:91 — #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT
```
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:103 — #FIXME:MUI_TEXT_STARTMENU_TITLE
```

!ifdef MUI_STARTMENUPAGE
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:104 — #FIXME:MUI_TEXT_STARTMENU_SUBTITLE
```
!ifdef MUI_STARTMENUPAGE
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:105 — #FIXME:MUI_INNERTEXT_STARTMENU_TOP
```
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:106 — #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX
```
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:110 — #FIXME:MUI_UNTEXT_CONFIRM_TITLE
```

!ifdef MUI_UNCONFIRMPAGE
  #FIXME:MUI_UNTEXT_CONFIRM_TITLE 
  #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:111 — #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE
```
!ifdef MUI_UNCONFIRMPAGE
  #FIXME:MUI_UNTEXT_CONFIRM_TITLE 
  #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:123 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE
```

!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:124 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE
```
!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:125 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:126 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Yoruba.nsh`:127 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER
```
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Romanian.nsh`:58 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "Aşezaţi mouse-ul deasupra fiecărei componente pentru a vizualiza descrierea acesteia."
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **TODO** in `ptinstaller/Contrib/Language files/Thai.nsh`:4 — !insertmacro LANGFILE "Thai" = "ไทย" /* TODO: ISO 11940? */ "Thai"
```
;By SoKoOLz, TuW@nNu (asdfuae)

!insertmacro LANGFILE "Thai" = "ไทย" /* TODO: ISO 11940? */ "Thai"

!ifdef MUI_WELCOMEPAGE
```

- **FIXME** in `ptinstaller/Contrib/Language files/Albanian.nsh`:51 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "Për të parë përshkrimin e një përbërësi, vendosni kursorin përsipër tij."
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Hungarian.nsh`:52 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "Vigye rá az egeret az összetevőre, hogy megtekinthesse a leírását."
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:7 — #FIXME:MUI_TEXT_WELCOME_INFO_TEXT
```
!ifdef MUI_WELCOMEPAGE
  ${LangFileString} MUI_TEXT_WELCOME_INFO_TITLE "Tonga soa eto Amin'ny Fampidirana ny $(^NameDA)"
  #FIXME:MUI_TEXT_WELCOME_INFO_TEXT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:11 — #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE
```

!ifdef MUI_UNWELCOMEPAGE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE 
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:12 — #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT
```
!ifdef MUI_UNWELCOMEPAGE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE 
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:19 — #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX
```
  ${LangFileString} MUI_TEXT_LICENSE_SUBTITLE "Jereo aloha izay voalaza ao amin'ny fifanekena, alohan'ny hampidirana ny $(^NameDA)."
  ${LangFileString} MUI_INNERTEXT_LICENSE_BOTTOM "Raha ekenao izay voalaza ao amin'ny fifanekena ary te hanohy ianao, dia tsindrio ny Ekeko. Tsy maintsy manaiky an'io fifanekena io ianao vao afaka mampiditra ny $(^NameDA)."
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:20 — #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
```
  ${LangFileString} MUI_INNERTEXT_LICENSE_BOTTOM "Raha ekenao izay voalaza ao amin'ny fifanekena ary te hanohy ianao, dia tsindrio ny Ekeko. Tsy maintsy manaiky an'io fifanekena io ianao vao afaka mampiditra ny $(^NameDA)."
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:24 — #FIXME:MUI_UNTEXT_LICENSE_TITLE
```

!ifdef MUI_UNLICENSEPAGE
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:25 — #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE
```
!ifdef MUI_UNLICENSEPAGE
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:26 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM
```
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:27 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX
```
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:28 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
```
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:36 — #FIXME:MUI_TEXT_COMPONENTS_TITLE
```

!ifdef MUI_COMPONENTSPAGE
  #FIXME:MUI_TEXT_COMPONENTS_TITLE
  #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:37 — #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE
```
!ifdef MUI_COMPONENTSPAGE
  #FIXME:MUI_TEXT_COMPONENTS_TITLE
  #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:41 — #FIXME:MUI_UNTEXT_COMPONENTS_TITLE
```

!ifdef MUI_UNCOMPONENTSPAGE
  #FIXME:MUI_UNTEXT_COMPONENTS_TITLE 
  #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:42 — #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE
```
!ifdef MUI_UNCOMPONENTSPAGE
  #FIXME:MUI_UNTEXT_COMPONENTS_TITLE 
  #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:48 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
  ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_TITLE "Mombamomba Azy"
  !ifndef PTIS_CONFIG_COMPONENTPAGE_ALTERNATIVE
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:50 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:55 — #FIXME:MUI_TEXT_DIRECTORY_TITLE
```

!ifdef MUI_DIRECTORYPAGE
  #FIXME:MUI_TEXT_DIRECTORY_TITLE 
  #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:56 — #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE
```
!ifdef MUI_DIRECTORYPAGE
  #FIXME:MUI_TEXT_DIRECTORY_TITLE 
  #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:60 — #FIXME:MUI_UNTEXT_DIRECTORY_TITLE
```

!ifdef MUI_UNDIRECTORYPAGE
  #FIXME:MUI_UNTEXT_DIRECTORY_TITLE 
  #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:61 — #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE
```
!ifdef MUI_UNDIRECTORYPAGE
  #FIXME:MUI_UNTEXT_DIRECTORY_TITLE 
  #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:89 — #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE
```

!ifdef MUI_UNFINISHPAGE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:90 — #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT
```
!ifdef MUI_UNFINISHPAGE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:91 — #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT
```
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:103 — #FIXME:MUI_TEXT_STARTMENU_TITLE
```

!ifdef MUI_STARTMENUPAGE
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:104 — #FIXME:MUI_TEXT_STARTMENU_SUBTITLE
```
!ifdef MUI_STARTMENUPAGE
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME: MUI_INNERTEXT_STARTMENU_CHECKBOX 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:105 — #FIXME:MUI_INNERTEXT_STARTMENU_TOP
```
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME: MUI_INNERTEXT_STARTMENU_CHECKBOX 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:106 — #FIXME: MUI_INNERTEXT_STARTMENU_CHECKBOX
```
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME: MUI_INNERTEXT_STARTMENU_CHECKBOX 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:110 — #FIXME:MUI_UNTEXT_CONFIRM_TITLE
```

!ifdef MUI_UNCONFIRMPAGE
  #FIXME:MUI_UNTEXT_CONFIRM_TITLE 
  #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:111 — #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE
```
!ifdef MUI_UNCONFIRMPAGE
  #FIXME:MUI_UNTEXT_CONFIRM_TITLE 
  #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:123 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE
```

!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:124 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE
```
!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:125 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:126 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Malagasy.nsh`:127 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER
```
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:7 — #FIXME:MUI_TEXT_WELCOME_INFO_TEXT
```
!ifdef MUI_WELCOMEPAGE
  ${LangFileString} MUI_TEXT_WELCOME_INFO_TITLE "Apa Kutendekela $(^NameDA) Ukukopolola"
  #FIXME:MUI_TEXT_WELCOME_INFO_TEXT
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:11 — #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE
```

!ifdef MUI_UNWELCOMEPAGE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:12 — #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT
```
!ifdef MUI_UNWELCOMEPAGE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:19 — #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX
```
  ${LangFileString} MUI_TEXT_LICENSE_SUBTITLE "Mukwai belengeni ifili mu kusuminishanya ilyo mushilatendeka ukukopolola $(^NameDA)."
  ${LangFileString} MUI_INNERTEXT_LICENSE_BOTTOM "Nga mwasumina ifili mu kusuminishanya, tinikeni pali Nasumina pa kuti mutwalilile. Mufwile ukusumina ifili mu kusuminishanya pa kuti mukopolole $(^NameDA)."
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:20 — #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
```
  ${LangFileString} MUI_INNERTEXT_LICENSE_BOTTOM "Nga mwasumina ifili mu kusuminishanya, tinikeni pali Nasumina pa kuti mutwalilile. Mufwile ukusumina ifili mu kusuminishanya pa kuti mukopolole $(^NameDA)."
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:24 — #FIXME:MUI_UNTEXT_LICENSE_TITLE
```

!ifdef MUI_UNLICENSEPAGE
  #FIXME:MUI_UNTEXT_LICENSE_TITLE
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:25 — #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE
```
!ifdef MUI_UNLICENSEPAGE
  #FIXME:MUI_UNTEXT_LICENSE_TITLE
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:26 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM
```
  #FIXME:MUI_UNTEXT_LICENSE_TITLE
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:27 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX
```
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:28 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
```
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:36 — #FIXME:MUI_TEXT_COMPONENTS_TITLE
```

!ifdef MUI_COMPONENTSPAGE
  #FIXME:MUI_TEXT_COMPONENTS_TITLE 
  #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:37 — #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE
```
!ifdef MUI_COMPONENTSPAGE
  #FIXME:MUI_TEXT_COMPONENTS_TITLE 
  #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:41 — #FIXME:MUI_UNTEXT_COMPONENTS_TITLE
```

!ifdef MUI_UNCOMPONENTSPAGE
  #FIXME:MUI_UNTEXT_COMPONENTS_TITLE
  #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:42 — #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE
```
!ifdef MUI_UNCOMPONENTSPAGE
  #FIXME:MUI_UNTEXT_COMPONENTS_TITLE
  #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:48 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
  ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_TITLE "Ubulondoloshi"
  !ifndef PTIS_CONFIG_COMPONENTPAGE_ALTERNATIVE
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:50 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:55 — #FIXME:MUI_TEXT_DIRECTORY_TITLE
```

!ifdef MUI_DIRECTORYPAGE
  #FIXME:MUI_TEXT_DIRECTORY_TITLE
  #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:56 — #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE
```
!ifdef MUI_DIRECTORYPAGE
  #FIXME:MUI_TEXT_DIRECTORY_TITLE
  #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:60 — #FIXME:MUI_UNTEXT_DIRECTORY_TITLE
```

!ifdef MUI_UNDIRECTORYPAGE
  #FIXME:MUI_UNTEXT_DIRECTORY_TITLE
  #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:61 — #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE
```
!ifdef MUI_UNDIRECTORYPAGE
  #FIXME:MUI_UNTEXT_DIRECTORY_TITLE
  #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:89 — #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE
```

!ifdef MUI_UNFINISHPAGE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:90 — #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT
```
!ifdef MUI_UNFINISHPAGE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:91 — #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT
```
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:98 — #FIXME:MUI_TEXT_FINISH_SHOWREADME
```
  ${LangFileString} MUI_TEXT_FINISH_REBOOTLATER "Nalacita ifi limbi"
  ${LangFileString} MUI_TEXT_FINISH_RUN "&Tampeni $(^NameDA)"
  #FIXME:MUI_TEXT_FINISH_SHOWREADME
  ${LangFileString} MUI_BUTTONTEXT_FINISH "&Capwa"  
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:103 — #FIXME:MUI_TEXT_STARTMENU_TITLE
```

!ifdef MUI_STARTMENUPAGE
  #FIXME:MUI_TEXT_STARTMENU_TITLE
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:104 — #FIXME:MUI_TEXT_STARTMENU_SUBTITLE
```
!ifdef MUI_STARTMENUPAGE
  #FIXME:MUI_TEXT_STARTMENU_TITLE
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME: MUI_INNERTEXT_STARTMENU_CHECKBOX 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:105 — #FIXME:MUI_INNERTEXT_STARTMENU_TOP
```
  #FIXME:MUI_TEXT_STARTMENU_TITLE
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME: MUI_INNERTEXT_STARTMENU_CHECKBOX 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:106 — #FIXME: MUI_INNERTEXT_STARTMENU_CHECKBOX
```
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME: MUI_INNERTEXT_STARTMENU_CHECKBOX 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:110 — #FIXME:MUI_UNTEXT_CONFIRM_TITLE
```

!ifdef MUI_UNCONFIRMPAGE
  #FIXME:MUI_UNTEXT_CONFIRM_TITLE
  #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:111 — #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE
```
!ifdef MUI_UNCONFIRMPAGE
  #FIXME:MUI_UNTEXT_CONFIRM_TITLE
  #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:123 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE
```

!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:124 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE
```
!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:125 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE 
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:126 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Cibemba.nsh`:127 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER
```
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Georgian.nsh`:99 — ${LangFileString} MUI_TEXT_FINISH_SHOWREADME "ReadMe &ფაილის ჩვენება" #FIXME: BUGBUG "ReadMe"?
```
  ${LangFileString} MUI_TEXT_FINISH_REBOOTLATER "არა, მოგვიანებით გადავტვირთავ კომპიუტერს"
  ${LangFileString} MUI_TEXT_FINISH_RUN "$(^NameDA)–ის &ჩართვა"
  ${LangFileString} MUI_TEXT_FINISH_SHOWREADME "ReadMe &ფაილის ჩვენება" #FIXME: BUGBUG "ReadMe"?
  ${LangFileString} MUI_BUTTONTEXT_FINISH "&მზადაა"  
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Pashto.nsh`:51 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO ".د رغتوکي سپړاوي کتلو لپاره موږک د هغې دپاسه ودروﺉ"
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Luxembourgish.nsh`:51 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO "Hal den Mausfeil iwwer eng Komponent, fir d'Beschreiwung dervun ze gesinn."
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:7 — #FIXME:MUI_TEXT_WELCOME_INFO_TEXT
```
!ifdef MUI_WELCOMEPAGE
  ${LangFileString} MUI_TEXT_WELCOME_INFO_TITLE "Karibu kwenye $(^NameDA) Usanidi"
  #FIXME:MUI_TEXT_WELCOME_INFO_TEXT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:11 — #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE
```

!ifdef MUI_UNWELCOMEPAGE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE 
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT "
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:12 — #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT "
```
!ifdef MUI_UNWELCOMEPAGE
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TITLE 
  #FIXME:MUI_UNTEXT_WELCOME_INFO_TEXT "
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:19 — #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX
```
  ${LangFileString} MUI_TEXT_LICENSE_SUBTITLE "Tafadhali soma matakwa ya leseni kabla ya kusakinisha (kuweka kwenye kompyuta) $(^NameDA)."
  ${LangFileString} MUI_INNERTEXT_LICENSE_BOTTOM "Ikiwa unakubali matakwa ya mapatano, bofya Nakubali ili kuendelea. Lazima ukubali mapatano kabla ya kusakinisha (kuweka kwenye kompyuta) $(^NameDA)."
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:20 — #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
```
  ${LangFileString} MUI_INNERTEXT_LICENSE_BOTTOM "Ikiwa unakubali matakwa ya mapatano, bofya Nakubali ili kuendelea. Lazima ukubali mapatano kabla ya kusakinisha (kuweka kwenye kompyuta) $(^NameDA)."
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_INNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:24 — #FIXME:MUI_UNTEXT_LICENSE_TITLE
```

!ifdef MUI_UNLICENSEPAGE
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:25 — #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE
```
!ifdef MUI_UNLICENSEPAGE
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:26 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM
```
  #FIXME:MUI_UNTEXT_LICENSE_TITLE 
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:27 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX
```
  #FIXME:MUI_UNTEXT_LICENSE_SUBTITLE 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:28 — #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS
```
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_CHECKBOX 
  #FIXME:MUI_UNINNERTEXT_LICENSE_BOTTOM_RADIOBUTTONS 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:36 — #FIXME:MUI_TEXT_COMPONENTS_TITLE
```

!ifdef MUI_COMPONENTSPAGE
  #FIXME:MUI_TEXT_COMPONENTS_TITLE 
  #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:37 — #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE
```
!ifdef MUI_COMPONENTSPAGE
  #FIXME:MUI_TEXT_COMPONENTS_TITLE 
  #FIXME:MUI_TEXT_COMPONENTS_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:41 — #FIXME:MUI_UNTEXT_COMPONENTS_TITLE
```

!ifdef MUI_UNCOMPONENTSPAGE
  #FIXME:MUI_UNTEXT_COMPONENTS_TITLE 
  #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:42 — #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE
```
!ifdef MUI_UNCOMPONENTSPAGE
  #FIXME:MUI_UNTEXT_COMPONENTS_TITLE 
  #FIXME:MUI_UNTEXT_COMPONENTS_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:48 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
  ${LangFileString} MUI_INNERTEXT_COMPONENTS_DESCRIPTION_TITLE "Ufafanuzi"
  !ifndef PTIS_CONFIG_COMPONENTPAGE_ALTERNATIVE
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:50 — #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO
```
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !else
    #FIXME:MUI_INNERTEXT_COMPONENTS_DESCRIPTION_INFO 
  !endif
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:55 — #FIXME:MUI_TEXT_DIRECTORY_TITLE
```

!ifdef MUI_DIRECTORYPAGE
  #FIXME:MUI_TEXT_DIRECTORY_TITLE 
  #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:56 — #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE
```
!ifdef MUI_DIRECTORYPAGE
  #FIXME:MUI_TEXT_DIRECTORY_TITLE 
  #FIXME:MUI_TEXT_DIRECTORY_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:60 — #FIXME:MUI_UNTEXT_DIRECTORY_TITLE
```

!ifdef MUI_UNDIRECTORYPAGE
  #FIXME:MUI_UNTEXT_DIRECTORY_TITLE 
  #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:61 — #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE
```
!ifdef MUI_UNDIRECTORYPAGE
  #FIXME:MUI_UNTEXT_DIRECTORY_TITLE 
  #FIXME:MUI_UNTEXT_DIRECTORY_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:89 — #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE
```

!ifdef MUI_UNFINISHPAGE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:90 — #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT
```
!ifdef MUI_UNFINISHPAGE
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:91 — #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT
```
  #FIXME:MUI_UNTEXT_FINISH_INFO_TITLE 
  #FIXME:MUI_UNTEXT_FINISH_INFO_TEXT 
  #FIXME:MUI_UNTEXT_FINISH_INFO_REBOOT 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:103 — #FIXME:MUI_TEXT_STARTMENU_TITLE
```

!ifdef MUI_STARTMENUPAGE
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:104 — #FIXME:MUI_TEXT_STARTMENU_SUBTITLE
```
!ifdef MUI_STARTMENUPAGE
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:105 — #FIXME:MUI_INNERTEXT_STARTMENU_TOP
```
  #FIXME:MUI_TEXT_STARTMENU_TITLE 
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:106 — #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX
```
  #FIXME:MUI_TEXT_STARTMENU_SUBTITLE 
  #FIXME:MUI_INNERTEXT_STARTMENU_TOP 
  #FIXME:MUI_INNERTEXT_STARTMENU_CHECKBOX 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:110 — #FIXME:MUI_UNTEXT_CONFIRM_TITLE
```

!ifdef MUI_UNCONFIRMPAGE
  #FIXME:MUI_UNTEXT_CONFIRM_TITLE 
  #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:111 — #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE
```
!ifdef MUI_UNCONFIRMPAGE
  #FIXME:MUI_UNTEXT_CONFIRM_TITLE 
  #FIXME:MUI_UNTEXT_CONFIRM_SUBTITLE 
!endif

```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:123 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE
```

!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:124 — #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE
```
!ifdef MULTIUSER_INSTALLMODEPAGE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:125 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_TITLE
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:126 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS
```
  #FIXME:MULTIUSER_TEXT_INSTALLMODE_SUBTITLE 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **FIXME** in `ptinstaller/Contrib/Language files/Swahili.nsh`:127 — #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER
```
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_TOP 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_ALLUSERS 
  #FIXME:MULTIUSER_INNERTEXT_INSTALLMODE_CURRENTUSER 
!endif
```

- **note** in `ptinstaller/Contrib/PTISdl/httpget.h`:12 — **      for none). See note on proxy string below.
```
**      object to use (or NULL for none, or JNL_CONNECTION_AUTODNS for auto),
**      and the receive buffer size, and a string specifying proxy (or NULL 
**      for none). See note on proxy string below.
**   2. call addheader() to add whatever headers you want. It is recommended to
**      add at least the following two:
```

- **note** in `ptinstaller/Contrib/PTISdl/httpget.h`:18 — **   3. Call connect() with the URL you wish to GET (see URL string note below)
```
*///      addheader("Accept:*/*");
/*         ( the comment weirdness is there so I Can do the star-slash :)
**   3. Call connect() with the URL you wish to GET (see URL string note below)
**   4. Call run() once in a while, checking to see if it returns -1 
**      (if it does return -1, call geterrorstr() to see what the error is).
```

- **Note** in `ptinstaller/Contrib/PTISdl/ReadMe.txt`:7 — Note: HTTPS is not supported, only plain HTTP!
```

This plugin can be used from PTIS to download files via HTTP.
Note: HTTPS is not supported, only plain HTTP!

To connect to the internet, use the Dialer plugin.
```

- **Note** in `ptinstaller/Contrib/PTIS Menu/wx/setup.h`:295 — // Note that if the system's implementation does not support positional
```

// Support for positional parameters (e.g. %1$d, %2$s ...) in wxVsnprintf.
// Note that if the system's implementation does not support positional
// parameters, setting this to 1 forces the use of the wxWidgets implementation
// of wxVsnprintf. The standard vsnprintf() supports positional parameters on
```

- **Note** in `ptinstaller/Contrib/PTIS Menu/wx/setup.h`:366 — // Note that this class is relatively new and is still officially in alpha
```
// compatibility (and implemented in terms of wxDateTime).
//
// Note that this class is relatively new and is still officially in alpha
// stage because some features are not yet (fully) implemented. It is already
// quite useful though and should only be disabled if you are aiming at
```

- **Note** in `ptinstaller/Contrib/PTIS Menu/wx/setup.h`:407 — // Default is 1 to use native classes. Note that you may still use
```
// Unix.
//
// Default is 1 to use native classes. Note that you may still use
// wxFileConfig even if you set this to 1 - just the config object created by
// default for the applications needs will be a wxRegConfig or wxIniConfig and
```

- **Note** in `ptinstaller/Contrib/PTIS Menu/wx/setup.h`:501 — // Note: if you set this to 1, you can open ftp/http/gopher sites
```
// Define this to use native platform url and protocol support.
// Currently valid only for MS-Windows.
// Note: if you set this to 1, you can open ftp/http/gopher sites
// and obtain a valid input stream for these sites
// even when you set wxUSE_PROTOCOL_FTP/HTTP to 0.
```

- **Note** in `ptinstaller/Contrib/PTIS Menu/wx/setup.h`:1132 — // them. Set to 1 for <iostream.h>, 0 for <iostream>. Note that VC++ 7.1
```

// VC++ 4.2 and above allows <iostream> and <iostream.h> but you can't mix
// them. Set to 1 for <iostream.h>, 0 for <iostream>. Note that VC++ 7.1
// and later doesn't support wxUSE_IOSTREAMH == 1 and so <iostream> will be
// used anyhow.
```

- **Note** in `ptinstaller/Contrib/PTIS Menu/wx/setup.h`:1214 — // http://www.microsoft.com/globaldev/handson/dev/mslu_announce.mspx). Note
```
// and want to run your programs under Windows 9x and not only NT/2000/XP.
// This setting enables use of unicows.dll from MSLU (MS Layer for Unicode, see
// http://www.microsoft.com/globaldev/handson/dev/mslu_announce.mspx). Note
// that you will have to modify the makefiles to include unicows.lib import
// library as the first library (see installation instructions in install.txt
```

- **TODO** in `ptinstaller/Contrib/System/Source/stdafx.h`:17 — // TODO: reference additional headers your program requires here
```
#include <windows.h>

// TODO: reference additional headers your program requires here
//#define SYSTEM_LOG_DEBUG

```

- **TODO** in `ptinstaller/Contrib/System/Source/System.c`:297 — TODO: CallProc not implemented.
```
#ifdef SYSTEM_ARM64
/*
TODO: CallProc not implemented.
Fake the behavior of the System plugin for the LoadImage API function etc. so MUI works.
BUGBUG: MUI is leaking DeleteObject
```

- **TODO** in `ptinstaller/Contrib/System/Source/System.c`:360 — BUGBUG: TODO: CallBack support not implemented!
```
#ifdef _WIN64
/*
BUGBUG: TODO: CallBack support not implemented!
*/
SystemProc* CallBack(SystemProc *proc)
```

- **Note** in `ptinstaller/Contrib/System/Source/System.c`:1170 — if (proc->Params[i].Option > 0) // Note: We don't handle '*' pointers, "*h" and "*b" are not supported, use "*i" even on smaller types
```
        case PAT_INT:
            intval = (int)(*((INT_PTR*) place));
            if (proc->Params[i].Option > 0) // Note: We don't handle '*' pointers, "*h" and "*b" are not supported, use "*i" even on smaller types
            {
                typsiz = GetSpecialParamTypeSize(proc->Params[i]);
```

- **TODO** in `ptinstaller/Contrib/System/Source/System.c`:1461 — #error TODO
```
        retexpr[0] = BUGBUG64(0);
#else
#error TODO
#endif
    }
```

- **TODO** in `ptinstaller/Contrib/System/Source/stdafx.c`:7 — // TODO: reference any additional headers you need in STDAFX.H
```
#include "stdafx.h"

// TODO: reference any additional headers you need in STDAFX.H
// and not in this file
```

- **TODO** in `ptinstaller/Contrib/System/Source/System.h`:153 — char asm_code[BUGBUG64(1)]; // TODO: BUGBUG64
```
        char asm_code[10];
    #elif defined(SYSTEM_AMD64) || defined(SYSTEM_ARM64)
        char asm_code[BUGBUG64(1)]; // TODO: BUGBUG64
    #else
        #error "Asm thunk not implemented for this architecture!"
```

- **Note** in `ptinstaller/Contrib/Library/RegTool/RegTool.cpp`:381 — // JP: Note, if this mode[1] is used as anything but a boolean later on,
```
                continue;

              // JP: Note, if this mode[1] is used as anything but a boolean later on,
              // we'll need to consider the next line carefully.
              RegFile(mode[0], file, mode[1] == 'X');
```

- **Note** in `ptinstaller/Docs/src/defines.but`:38 — Removes an item from the global define list. Note that $\{SYMBOL\} where SYMBOL is undefined will be translated to "$\{SYMBOL\}".
```
\c [/noerrors] gflag [...]

Removes an item from the global define list. Note that $\{SYMBOL\} where SYMBOL is undefined will be translated to "$\{SYMBOL\}".

\c !define SOMETHING
```

- **Note** in `ptinstaller/Docs/src/headers.but`:1263 — \\<b\\>Note:\\</b\\>
```
\c               ;  If [File3]="" Then add [File2] to [File1]

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if input files don't exist
\\<br\\>- Error flag if output file path doesn't exist
```

- **Note** in `ptinstaller/Docs/src/build.but`:122 — This should only build makeptis and install it to the directory where a precompiled package, such as the \R{build_nightly}{nightly build} or a \W{http://sourceforge.net/project/showfiles.php?group_id=22049}{zipped release version} (ptis-x.xx.zip), is extracted. Note that the in order for this to work, the precompiled package must be compiled using the exact same sources as makeptis. In particular, \c{Source\\exehead\\config.h}, the options passed to scons and \c{Source\\exehead\\fileform.h} must be identical. Nightly builds and zipped release versions are built with the default options.
```
\c       install-compiler

This should only build makeptis and install it to the directory where a precompiled package, such as the \R{build_nightly}{nightly build} or a \W{http://sourceforge.net/project/showfiles.php?group_id=22049}{zipped release version} (ptis-x.xx.zip), is extracted. Note that the in order for this to work, the precompiled package must be compiled using the exact same sources as makeptis. In particular, \c{Source\\exehead\\config.h}, the options passed to scons and \c{Source\\exehead\\fileform.h} must be identical. Nightly builds and zipped release versions are built with the default options.

To build a native version of PTIS Menu, install \W{http://www.wxwidgets.org/}{wxWidgets 2.8} and build as usual. \c{wx-config} must be in the path.
```

- **Note** in `ptinstaller/Docs/src/attributes.but`:123 — Specifies whether or not the installer will perform a CRC on itself before allowing an install. Note that if the user uses /NCRC on the command line when executing the installer, and you didn't specify 'force', the CRC will not occur, and the user will be allowed to install a (potentially) corrupted installer.
```
\c \\<b\\>on\\</b\\>|off|force

Specifies whether or not the installer will perform a CRC on itself before allowing an install. Note that if the user uses /NCRC on the command line when executing the installer, and you didn't specify 'force', the CRC will not occur, and the user will be allowed to install a (potentially) corrupted installer.

\S2{adetailsbuttontext} DetailsButtonText
```

- **Note** in `ptinstaller/Docs/src/attributes.but`:290 — \# Note: true==System. PerMonitor is not documented because it is not fully supported yet, we need to handle WM_DPICHANGED.
```
\c \\<b\\>notset\\</b\\>|true|false

\# Note: true==System. PerMonitor is not documented because it is not fully supported yet, we need to handle WM_DPICHANGED.
\# Example: ManifestDPIAware System ; System DPI on Vista/7/8/8.1/10(<10.1607(AU))
\# Example: ManifestDPIAwareness "PerMonitorV2,System" ; PMv2 on 10.1703(CU)+, System on 10.1607(AU)
```

- **Note** in `ptinstaller/Docs/src/attributes.but`:401 — Sets whether or not the details of the install are shown. Can be 'hide' to hide the details by default, allowing the user to view them, or 'show' to show them by default, or 'nevershow', to prevent the user from ever seeing them. Note that sections can override this using \R{setdetailsview}{SetDetailsView}.
```
\c \\<b\\>hide\\</b\\>|show|nevershow

Sets whether or not the details of the install are shown. Can be 'hide' to hide the details by default, allowing the user to view them, or 'show' to show them by default, or 'nevershow', to prevent the user from ever seeing them. Note that sections can override this using \R{setdetailsview}{SetDetailsView}.

\S2{ashowuninstdetails} ShowUninstDetails
```

- **Note** in `ptinstaller/Docs/src/attributes.but`:407 — Sets whether or not the details of the uninstall are shown. Can be 'hide' to hide the details by default, allowing the user to view them, or 'show' to show them by default, or 'nevershow', to prevent the user from ever seeing them. Note that sections can override this using \R{setdetailsview}{SetDetailsView}.
```
\c \\<b\\>hide\\</b\\>|show|nevershow

Sets whether or not the details of the uninstall are shown. Can be 'hide' to hide the details by default, allowing the user to view them, or 'show' to show them by default, or 'nevershow', to prevent the user from ever seeing them. Note that sections can override this using \R{setdetailsview}{SetDetailsView}.

\S2{asilentinstall} SilentInstall
```

- **Note** in `ptinstaller/Docs/src/var.but`:36 — Note that in uninstaller code, $INSTDIR contains the directory where the uninstaller lies. It does \s{not} necessarily contain the same value it contained in the installer. For example, if you write the uninstaller to $WINDIR and the user doesn't move it, $INSTDIR will be $WINDIR in the uninstaller. If you write the uninstaller to another location, you should keep the installer's $INSTDIR in the registry or an alternative storing facility and read it in the uninstaller.
```
The installation directory ($INSTDIR is modifiable using \R{StrCpy}{StrCpy}, \R{readregstr}{ReadRegStr}, \R{readinistr}{ReadINIStr}, etc. - This could be used, for example, in the \R{oninit}{.onInit} function to do a more advanced detection of install location).

Note that in uninstaller code, $INSTDIR contains the directory where the uninstaller lies. It does \s{not} necessarily contain the same value it contained in the installer. For example, if you write the uninstaller to $WINDIR and the user doesn't move it, $INSTDIR will be $WINDIR in the uninstaller. If you write the uninstaller to another location, you should keep the installer's $INSTDIR in the registry or an alternative storing facility and read it in the uninstaller.

\e{$OUTDIR}
```

- **Note** in `ptinstaller/Docs/src/var.but`:60 — Note that some of the new constants will not work on every OS. For example, $CDBURN_AREA will only work on Windows XP and above. If it's used on Windows 98, it'll be empty. Unless mentioned otherwise, a constant should be available on every OS.
```
Constants can also be used in the \R{ainstalldir}{InstallDir} attribute.

Note that some of the new constants will not work on every OS. For example, $CDBURN_AREA will only work on Windows XP and above. If it's used on Windows 98, it'll be empty. Unless mentioned otherwise, a constant should be available on every OS.

\e{$PROGRAMFILES}, \e{$PROGRAMFILES32}, \e{$PROGRAMFILES64}
```

- **Note** in `ptinstaller/Docs/src/file.but`:183 — Continues a search began with \R{FindFirst}{FindFirst}. handle should be the handle_output_variable returned by \R{FindFirst}{FindFirst}. If the search is completed (there are no more files), filename_output is set to empty and the error flag is set. Note that the filename output is without path.
```
\c handle user_var(filename_output)

Continues a search began with \R{FindFirst}{FindFirst}. handle should be the handle_output_variable returned by \R{FindFirst}{FindFirst}. If the search is completed (there are no more files), filename_output is set to empty and the error flag is set. Note that the filename output is without path.
```

- **Note** in `ptinstaller/Docs/src/flowcontrol.but`:72 — Gets the address of the function and stores it in the output user variable. This user variable then can be passed to \R{call}{Call} or \R{goto}{Goto}. Note that if you \R{goto}{Goto} an address which is the output of \R{getfunctionaddress}{GetFunctionAddress}, your function will never be returned to (when the function you Goto'd to returns, you return instantly).
```
\c user_var(output) function_name

Gets the address of the function and stores it in the output user variable. This user variable then can be passed to \R{call}{Call} or \R{goto}{Goto}. Note that if you \R{goto}{Goto} an address which is the output of \R{getfunctionaddress}{GetFunctionAddress}, your function will never be returned to (when the function you Goto'd to returns, you return instantly).

\c Function func
```

- **Note** in `ptinstaller/Docs/src/flowcontrol.but`:87 — Gets the address of the label and stores it in the output user variable. This user variable then can be passed to \R{call}{Call} or \R{goto}{Goto}. Note that you may only call this with labels accessible from your function, but you can call it from anywhere (which is potentially dangerous). Note that if you \R{call}{Call} the output of \R{getlabeladdress}{GetLabelAddress}, code will be executed until it Return's (explicitly or implicitly at the end of a function), and then you will be returned to the statement after the \R{call}{Call}.
```
\c user_var(output) label

Gets the address of the label and stores it in the output user variable. This user variable then can be passed to \R{call}{Call} or \R{goto}{Goto}. Note that you may only call this with labels accessible from your function, but you can call it from anywhere (which is potentially dangerous). Note that if you \R{call}{Call} the output of \R{getlabeladdress}{GetLabelAddress}, code will be executed until it Return's (explicitly or implicitly at the end of a function), and then you will be returned to the statement after the \R{call}{Call}.

\c label:
```

- **Note** in `ptinstaller/Docs/src/tutorial.but`:190 — After calling the function, the variables contain the same value as before. Note the order when using multiple variables (last-in first-out):
```
For shared code, there are \R{varother}{20 registers available} (like $0 and $R0). These static variables don't have to be declared and you won't get any name conflicts. If you want to use these variables in shared code, store the original values on the stack and restore the original values afterwards.

After calling the function, the variables contain the same value as before. Note the order when using multiple variables (last-in first-out):

\c Function bla
```

- **Note** in `ptinstaller/Docs/src/jumps.but`:20 — Note that \R{insertmacro}{macro insertion} is not considered as one instruction when it comes to relative jumps. The macro is expanded before relative jumps are applied, and so relative jumps can jump into code inside an inserted macro. The following code, for example, will not skip the macro. It will show a message box.
```
\c  MessageBox MB_OK "Done"

Note that \R{insertmacro}{macro insertion} is not considered as one instruction when it comes to relative jumps. The macro is expanded before relative jumps are applied, and so relative jumps can jump into code inside an inserted macro. The following code, for example, will not skip the macro. It will show a message box.

\c !macro relative_jump_test
```

- **Note** in `ptinstaller/Docs/src/sec.but`:82 — Sets the install types the section specified by section_index defaults to the enabled state in. Note that the section index starts with zero. Every bit of inst_types is a flag that tells if the section is in that install type or not. For example, if you have 3 install types and you want the first section to be included in install types 1 and 3, then the command should look like this:
```
\c section_index inst_types

Sets the install types the section specified by section_index defaults to the enabled state in. Note that the section index starts with zero. Every bit of inst_types is a flag that tells if the section is in that install type or not. For example, if you have 3 install types and you want the first section to be included in install types 1 and 3, then the command should look like this:

\c SectionSetInstTypes 0 5
```

- **Note** in `ptinstaller/Docs/src/sec.but`:116 — Sets the size of the section specified by section_index. Note that the index starts with zero. The Value for Size must be entered in KiloByte and supports only whole numbers.
```
\c section_index new_size

Sets the size of the section specified by section_index. Note that the index starts with zero. The Value for Size must be entered in KiloByte and supports only whole numbers.

\c Section test test_section_id
```

- **Note** in `ptinstaller/Docs/src/ui.but`:95 — Sends a message to HWND. If a user variable $x is specified as the last parameter (or one before the last if you use /TIMEOUT), the return value from SendMessage will be stored in it. Note that when specifying 'msg' you must just use the integer value of the message. Include WinMessages.nsh to have all Windows messages defined in your script. If you wish to send strings use "STR:a string" as wParam or lParam where needed. Use /TIMEOUT=time_in_ms to specify the duration, in milliseconds, of the time-out period.
```
\c HWND msg wparam lparam [user_var(return value)] [/TIMEOUT=time_in_ms]

Sends a message to HWND. If a user variable $x is specified as the last parameter (or one before the last if you use /TIMEOUT), the return value from SendMessage will be stored in it. Note that when specifying 'msg' you must just use the integer value of the message. Include WinMessages.nsh to have all Windows messages defined in your script. If you wish to send strings use "STR:a string" as wParam or lParam where needed. Use /TIMEOUT=time_in_ms to specify the duration, in milliseconds, of the time-out period.

\c !include WinMessages.nsh
```

- **Note** in `ptinstaller/Docs/src/ui.but`:114 — Sets the current bitmap file displayed as the branding image. If no IMGID is specified, the first image control found will be used, or the image control created by \R{aaddbrandingimage}{AddBrandingImage}. Note that this bitmap must be present on the user's machine. Use \cw{File} first to put it there. If /RESIZETOFIT is specified the image will be automatically resized (very poorly) to the image control size. If you used \R{aaddbrandingimage}{AddBrandingImage} you can get this size by compiling your script and watching for \R{aaddbrandingimage}{AddBrandingImage} output, it will tell you the size. SetBrandingImage will not work when called from .onInit!
```
\c [/IMGID=item_id_in_dialog] [/RESIZETOFIT] path_to_bitmap_file.bmp

Sets the current bitmap file displayed as the branding image. If no IMGID is specified, the first image control found will be used, or the image control created by \R{aaddbrandingimage}{AddBrandingImage}. Note that this bitmap must be present on the user's machine. Use \cw{File} first to put it there. If /RESIZETOFIT is specified the image will be automatically resized (very poorly) to the image control size. If you used \R{aaddbrandingimage}{AddBrandingImage} you can get this size by compiling your script and watching for \R{aaddbrandingimage}{AddBrandingImage} output, it will tell you the size. SetBrandingImage will not work when called from .onInit!

\S2{setdetailsview} SetDetailsView
```

- **TODO** in `ptinstaller/Docs/src/SConscript`:1 — # TODO FIXME extra files should be targets too, so they're rebuilt in case they're gone
```
# TODO FIXME extra files should be targets too, so they're rebuilt in case they're gone

buts = Split("""
```

- **Note** in `ptinstaller/Docs/src/compilerflags.but`:42 — Note that this option has no effect when solid compression is used.
```
This command sets the compress flag which is used by the installer to determine whether or not data should be compressed. Typically the \R{asetcompress}{SetCompress} flag will affect the commands after it, and the last \R{asetcompress}{SetCompress} command in the file also determines whether or not the install info section and uninstall data of the installer is compressed. If compressflag is 'auto', then files are compressed if the compressed size is smaller than the uncompressed size. If compressflag is set to 'force', then the compressed version is always used. If compressflag is 'off' then compression is not used (which can be faster).

Note that this option has no effect when solid compression is used.

\S2{asetcompressor} SetCompressor
```

- **Note** in `ptinstaller/Docs/src/compilerflags.but`:84 — This command sets the overwrite flag which is used by the \R{file}{File} command to determine whether or not the file should overwrite any existing files that are present. If overwriteflag is 'on', files are overwritten (this is the default). If overwriteflag is 'off', files that are already present are not overwritten. If overwriteflag is 'try', files are overwritten if possible (meaning that if the file is not able to be written to, it is skipped without any user interaction). If overwriteflag is 'ifnewer', then files are only overwritten if the existing file is older than the new file. If overwriteflag is 'ifdiff', then files are only overwritten if the existing file is older or newer than the new file. Note that when in 'ifnewer' or 'ifdiff' mode, the destination file's date is set, regardless of what \R{asetdatesave}{SetDateSave} is set to.
```
\c \\<b\\>on\\</b\\>|off|try|ifnewer|ifdiff|lastused

This command sets the overwrite flag which is used by the \R{file}{File} command to determine whether or not the file should overwrite any existing files that are present. If overwriteflag is 'on', files are overwritten (this is the default). If overwriteflag is 'off', files that are already present are not overwritten. If overwriteflag is 'try', files are overwritten if possible (meaning that if the file is not able to be written to, it is skipped without any user interaction). If overwriteflag is 'ifnewer', then files are only overwritten if the existing file is older than the new file. If overwriteflag is 'ifdiff', then files are only overwritten if the existing file is older or newer than the new file. Note that when in 'ifnewer' or 'ifdiff' mode, the destination file's date is set, regardless of what \R{asetdatesave}{SetDateSave} is set to.

\c SetOverwrite off
```

- **Note** in `ptinstaller/Docs/src/misc.but`:78 — Note that, if used in installer code, this will only affect the installer, and if used in uninstaller code, this will only affect the uninstaller. To affect both, it needs to be used in both.
```
Sets the context of $SMPROGRAMS and other \R{varconstant}{shell folders}. If set to 'current' (the default), the current user's shell folders are used. If set to 'all', the 'all users' shell folder is used. The all users folder may not be supported on all OSes. If the all users folder is not found, the current user folder will be used. Please take into consideration that a "normal user" has no rights to write in the all users area. Only admins have full access rights to the all users area. You can check this by using the UserInfo plug-in. See Contrib\\UserInfo\\UserInfo.nsi for an example.

Note that, if used in installer code, this will only affect the installer, and if used in uninstaller code, this will only affect the uninstaller. To affect both, it needs to be used in both.

\c SetShellVarContext current
```

- **Note** in `ptinstaller/Docs/src/basic.but`:23 — Execute the specified program and continue immediately. Note that the file specified must exist on the target system, not the compiling system. $OUTDIR is used as the working directory. The error flag is set if the process could not be launched. Note, if the command could have spaces, you should put it in quotes to delimit it from parameters. e.g.: Exec '"$INSTDIR\\command.exe" parameters'. If you don't put it in quotes it will \e{not} work on Windows 9x with or without parameters.
```
\c command

Execute the specified program and continue immediately. Note that the file specified must exist on the target system, not the compiling system. $OUTDIR is used as the working directory. The error flag is set if the process could not be launched. Note, if the command could have spaces, you should put it in quotes to delimit it from parameters. e.g.: Exec '"$INSTDIR\\command.exe" parameters'. If you don't put it in quotes it will \e{not} work on Windows 9x with or without parameters.

\c Exec '"$INSTDIR\someprogram.exe"'
```

- **Note** in `ptinstaller/Docs/src/basic.but`:32 — Execute the specified file using ShellExecuteEx. Note that \cw{action} is usually "open", "print", etc, but can be an empty string to use the default action. \cw{Parameters} and the show type are optional. $OUTDIR is used as the working directory. The error flag is set if the file could not be launched. \cw{Flags} can be any combination of /ALLOWERRORUI, /DOENVSUBST and /INVOKEIDLIST.
```
\c [flags] action file [parameters] [SW_SHOWDEFAULT | SW_SHOWNORMAL | SW_SHOWMAXIMIZED | SW_SHOWMINIMIZED | SW_HIDE]

Execute the specified file using ShellExecuteEx. Note that \cw{action} is usually "open", "print", etc, but can be an empty string to use the default action. \cw{Parameters} and the show type are optional. $OUTDIR is used as the working directory. The error flag is set if the file could not be launched. \cw{Flags} can be any combination of /ALLOWERRORUI, /DOENVSUBST and /INVOKEIDLIST.

\c ExecShell "open" "http://ptis.sf.net/"
```

- **Note** in `ptinstaller/Docs/src/basic.but`:61 — \b Note that the output file name is $OUTDIR\\filename_portion_of_file.
```
Adds file(s) to be extracted to the current output path ($OUTDIR).

\b Note that the output file name is $OUTDIR\\filename_portion_of_file.

\b Use /oname=X switch to change the output name. X may contain variables and can be a fully qualified path or a relative path in which case it will be appended to $OUTDIR set by \R{setoutpath}{SetOutPath}. When using this switch, only one file can be specified. If the output name contains spaces, quote the entire parameter, including /oname, as shown in the examples below.
```

- **Note** in `ptinstaller/Docs/src/compiler.but`:5 — These commands are similar to the C preprocessor in terms of purpose and functionality. They allow file inclusion, conditional compilation, executable header packing and process execution during the build process. Note: None of these commands allow the use of \R{variables}{variables}.
```
\S0{compcommands} Compiler Utility Commands

These commands are similar to the C preprocessor in terms of purpose and functionality. They allow file inclusion, conditional compilation, executable header packing and process execution during the build process. Note: None of these commands allow the use of \R{variables}{variables}.

Number literals support the \c{0b}, \c{0o}, \c{0n} and \c{0x} radix prefixes (base 2, 8, 10 and 16 respectively). Note: The deprecated plain \c{0} octal prefix is also supported in some places but its usage is discouraged.
```

- **Note** in `ptinstaller/Docs/src/compiler.but`:7 — Number literals support the \c{0b}, \c{0o}, \c{0n} and \c{0x} radix prefixes (base 2, 8, 10 and 16 respectively). Note: The deprecated plain \c{0} octal prefix is also supported in some places but its usage is discouraged.
```
These commands are similar to the C preprocessor in terms of purpose and functionality. They allow file inclusion, conditional compilation, executable header packing and process execution during the build process. Note: None of these commands allow the use of \R{variables}{variables}.

Number literals support the \c{0b}, \c{0o}, \c{0n} and \c{0x} radix prefixes (base 2, 8, 10 and 16 respectively). Note: The deprecated plain \c{0} octal prefix is also supported in some places but its usage is discouraged.

\S1{include} !include
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/licence.c`:8 — "FIXME: licence text goes here",
```

static char *licencetext[] = {
  "FIXME: licence text goes here",
  NULL
};
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/keywords.c`:88 — /* FIXME: what happens to kw->text? Does it leak? */
```
          error(err_multikw, &source->fpos, &ret->para->fpos, p);
          sfree(kw);
          /* FIXME: what happens to kw->text? Does it leak? */
        }
      }
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/bk_xhtml.c`:1604 — rdaddc(&t, '[');              /* FIXME: configurability */
```
  rdstringc t = { 0, 0, NULL };

  rdaddc(&t, '[');              /* FIXME: configurability */
  xhtml_rdaddwc(&t, text, NULL);
  rdaddc(&t, ']');              /* FIXME: configurability */
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/bk_xhtml.c`:1606 — rdaddc(&t, ']');              /* FIXME: configurability */
```
  rdaddc(&t, '[');              /* FIXME: configurability */
  xhtml_rdaddwc(&t, text, NULL);
  rdaddc(&t, ']');              /* FIXME: configurability */

  if (started)
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/bk_xhtml.c`:1826 — /* FIXME: really ustricmp() and not ustrcmp()? */
```
           (itag = (indextag *) index234(idx->tags, ti)) != NULL; ti++)
      {
        /* FIXME: really ustricmp() and not ustrcmp()? */
        if (ustricmp(itag->name, text->text) == 0)
        {
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/bk_xhtml.c`:2005 — fragment = "";            /* FIXME: what else can we do? */
```
    else
    {
      fragment = "";            /* FIXME: what else can we do? */
      error(err_whatever, "Couldn't locate heading cross-reference!");
    }
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/input.c`:151 — /* FIXME: do input charmap translation. We should be returning
```
        in->pos.line++;
    }
    /* FIXME: do input charmap translation. We should be returning
     * Unicode here. */
    if (c == '\0')
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/input.c`:270 — * FIXME. The ids are explicit in here so as to allow long-name
```
{
  /*
   * FIXME. The ids are explicit in here so as to allow long-name
   * equivalents to the various very short keywords.
   *
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/input.c`:882 — /* FIXME: there will be bugs if anyone specifies an
```
          /* This is a keyword. */
          nkeys++;
          /* FIXME: there will be bugs if anyone specifies an
           * empty keyword (\foo{}), so trap this case. */
          while (dtor(t), t = get_token(in),
```

- **TODO** in `ptinstaller/Docs/src/bin/halibut/input.c`:889 — /* TODO: Merge from upstream?: (t.type == tok_cmd && t.cmd == c_u) || */
```
                 (t.type == tok_cmd && t.cmd == c__nbsp) ||
                 (t.type == tok_cmd && t.cmd == c__escaped) ||
                 /* TODO: Merge from upstream?: (t.type == tok_cmd && t.cmd == c_u) || */
                is_special_midpara_cmd(&t))
          {
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/index.c`:72 — * FIXME: want to warn on overlapping source sets.
```

  /*
   * FIXME: want to warn on overlapping source sets.
   */
  for (; *tags; tags = uadv(tags))
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/index.c`:109 — * \IM before. FIXME: we should check the text
```
        /*
         * An implicit \IM for a tag that's had an implicit
         * \IM before. FIXME: we should check the text
         * against the existing text and warn on
         * differences. And check the tag for case match
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/error.c`:154 — /* FIXME: need to get a filepos to here somehow */
```
    sprintf(error, "`\\IM' on unknown index tag `%.200s'", sp);
    flags = 0;
    /* FIXME: need to get a filepos to here somehow */
    break;
  case err_cantopenw:
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/ustring.c`:100 — /* FIXME: this doesn't even come close */
```
  if (c == L'\0')
    return c;                   /* this property needed by ustricmp */
  /* FIXME: this doesn't even come close */
  if (c >= 'A' && c <= 'Z')
    c += 'a' - 'A';
```

- **FIXME** in `ptinstaller/Docs/src/bin/halibut/misc.c`:121 — return (a->type < b->type ? -1 : +1);     /* FIXME? */
```
  {
    if (a->type != b->type)
      return (a->type < b->type ? -1 : +1);     /* FIXME? */
    t = a->type;
    if ((t != word_Normal && t != word_Code &&
```

- **Note** in `ptinstaller/Source/exehead/exec.c`:717 — INT_PTR v=GetIntPtrFromParm(0), v2=GetIntPtrFromParm(1); // Note: This needs to be INT64 if supp64 is ever set to true for 32-bit builds!
```
      {
        UINT supp64=sizeof(void*) > 4, opu=supp64 ? (BYTE) parm5 : parm5, op64=supp64 ? (SHORT) parm5 < 0 : FALSE;
        INT_PTR v=GetIntPtrFromParm(0), v2=GetIntPtrFromParm(1); // Note: This needs to be INT64 if supp64 is ever set to true for 32-bit builds!
        if (!opu) { // signed:
          if (op64) {
```

- **TODO** in `ptinstaller/Source/exehead/exec.c`:1515 — // TODO: Use SetFilePointerEx for > 4GB support on _WIN64
```
        if (*t)
        {
          // TODO: Use SetFilePointerEx for > 4GB support on _WIN64
          DWORD v=SetFilePointer((HANDLE)strtoiptr(t),GetIntFromParm(2),NULL,parm3);

```

- **note** in `ptinstaller/Source/exehead/bgbg.c`:70 — // note that we don't need to do "SelectObject(hdc, brush)"
```
          lh.lbColor = RGB(rv,gv,bv);
          brush = CreateBrushIndirect(&lh);
          // note that we don't need to do "SelectObject(hdc, brush)"
          // because FillRect lets us specify the brush as a parameter.
          r.bottom+=4;
```

- **Note** in `ptinstaller/Source/exehead/api.h`:50 — int plugin_api_version; // Plug-in ABI. See PTISPIAPIVER_CURR (Note: used to be XXX_insttype_changed)
```
  int reboot_called;      // PTIS_SUPPORT_REBOOT
  int XXX_cur_insttype;   // Deprecated
  int plugin_api_version; // Plug-in ABI. See PTISPIAPIVER_CURR (Note: used to be XXX_insttype_changed)
  int silent;             // IfSilent/SetSilent (PTIS_CONFIG_SILENT_SUPPORT)
  int instdir_error;      // GetInstDirError
```

- **TODO** in `ptinstaller/Source/exehead/Main.c`:115 — ovi.wProductType = 4; // TODO: For < NT4SP6, look it up in the registry. 4 means not W9x and not VER_NT_*
```
    if (sizeof(TCHAR) == 2 || ovi.dwPlatformId == VER_PLATFORM_WIN32_NT)
    {
      ovi.wProductType = 4; // TODO: For < NT4SP6, look it up in the registry. 4 means not W9x and not VER_NT_*
      ovi.wServicePackMajor = ovi.szCSDVersion[0] == 'S' ? ovi.szCSDVersion[13] - '0' : 0;
    }
```

- **Note** in `ptinstaller/Source/exehead/Main.c`:178 — // Note: We also import directly from KERNEL32, ADVAPI32 and SHELL32 so they
```
  // because GetSystemDirectory() can return the native system32 path we need
  // the redirection to be turned off so LoadLibrary uses the correct folder.
  // Note: We also import directly from KERNEL32, ADVAPI32 and SHELL32 so they 
  // are exempt from this requirement and SHELL32 imports from SHLWAPI on 
  // WoW64 systems and it is also on the KnownDLLs list so 
```

- **TODO** in `ptinstaller/Source/Tests/winchar.cpp`:16 — // TODO write equal() for WINWCHAR -- http://subcommanderblog.wordpress.com/2009/01/10/cppunit_assert_equal-and-custom-data-types/
```
// BUGBUG: WinWStrDupFromWC is unable to test WCToUTF16LEHlpr because it is behind #ifdef MAKEPTIS

// TODO write equal() for WINWCHAR -- http://subcommanderblog.wordpress.com/2009/01/10/cppunit_assert_equal-and-custom-data-types/

class WinCharTest : public CppUnit::TestFixture {
```

- **Note** in `ptinstaller/Include/Win/COM.nsh`:311 — ${PTISCOMIFACEDECL}IPropertyStorage Commit 9 (i)i ; Note: Some implementations might return E_NOTIMPL
```
${PTISCOMIFACEDECL}IPropertyStorage WritePropertyNames 7 (i,p,p)i
${PTISCOMIFACEDECL}IPropertyStorage DeletePropertyNames 8 (i,p)i
${PTISCOMIFACEDECL}IPropertyStorage Commit 9 (i)i ; Note: Some implementations might return E_NOTIMPL
${PTISCOMIFACEDECL}IPropertyStorage Revert 10 ()i
${PTISCOMIFACEDECL}IPropertyStorage Enum 11 (*p)i
```

- **FIXME** in `launcher/net/NetRequest.cpp`:267 — * FIXME: report Qt bug for this
```
			 * Next, make sure the URL is parsed in tolerant mode. Qt doesn't parse the location header in tolerant
			 * mode, which causes issues.
			 * FIXME: report Qt bug for this
			 */
			redirect = QUrl(redirectStr, QUrl::TolerantMode);
```

- **FIXME** in `launcher/minecraft/AssetsUtils.h`:65 — /// FIXME: this is absolutely horrendous. REDO!!!!
```
};

/// FIXME: this is absolutely horrendous. REDO!!!!
namespace AssetsUtils
{
```

- **NOTE** in `launcher/minecraft/Library.h`:179 — // NOTE: Legacy support for Core Mods display cutoff (approx 1.4.7 era). Used by jar mods only
```
	QString filename(const RuntimeContext& runtimeContext) const;

	// NOTE: Legacy support for Core Mods display cutoff (approx 1.4.7 era). Used by jar mods only
	void setDisplayName(const QString& displayName)
	{
```

- **NOTE** in `launcher/minecraft/LaunchProfile.cpp`:432 — // NOTE: order is important here, add main jar last to the lists
```
		lib->getApplicableFiles(runtimeContext, jars, nativeJars, native32, native64, overridePath);
	}
	// NOTE: order is important here, add main jar last to the lists
	if (m_mainJar)
	{
```

- **NOTE** in `launcher/minecraft/Component.cpp`:475 — // NOTE: this needs to be rewritten if the type of DependencySet changes
```
static bool deepCompare(const projt::meta::DependencySet& a, const projt::meta::DependencySet& b)
{
	// NOTE: this needs to be rewritten if the type of DependencySet changes
	if (a.size() != b.size())
	{
```

- **NOTE** in `launcher/minecraft/PackProfile.cpp`:382 — // NOTE: actually use fine-grained updates, not this...
```
	}

	// NOTE: actually use fine-grained updates, not this...
	beginResetModel();
	// disconnect all the old components
```

- **Note** in `launcher/minecraft/MinecraftInstance.cpp`:866 — // Note: Fullscreen/maximization support is limited. UI enhancements for fullscreen mode are needed.
```
		if (settings()->get("LaunchMaximized").toBool())
		{
			// Note: Fullscreen/maximization support is limited. UI enhancements for fullscreen mode are needed.
			if (!isLegacy())
			{
```

- **NOTE** in `launcher/minecraft/Component.h`:107 — // NOTE: Kept for legacy migration support.
```

	// DEPRECATED: explicit numeric order values, used for loading old non-component config.
	// NOTE: Kept for legacy migration support.
	void setOrder(int order);
	int getOrder();
```

- **NOTE** in `launcher/minecraft/Component.h`:171 — // NOTE: Kept for legacy migration support.
```

	// DEPRECATED: explicit numeric order values, used for loading old non-component config.
	// NOTE: Kept for legacy migration support.
	bool m_orderOverride = false;
	int m_order			 = 0;
```

- **Note** in `launcher/minecraft/ComponentUpdateTask.cpp`:557 — // Architecture Note: This method directly manipulates PackProfile internals.
```
}

// Architecture Note: This method directly manipulates PackProfile internals.
// Proper abstraction would require richer data types (dependency graph, version constraints),
// but current implementation is sufficient for the launcher's use case.
```

- **NOTE** in `launcher/minecraft/ComponentUpdateTask.cpp`:574 — * NOTE: this is a placeholder and should eventually be replaced with something 'serious'
```
	 *    -> requirements are injected
	 *
	 * NOTE: this is a placeholder and should eventually be replaced with something 'serious'
	 */
	auto& components	 = d->m_profile->d->components;
```

- **NOTE** in `launcher/minecraft/auth/MinecraftAccount.cpp`:189 — // NOTE: user will need to fix this themselves.
```
		case AccountTaskState::STATE_DISABLED:
		{
			// NOTE: user will need to fix this themselves.
		}
		case AccountTaskState::STATE_FAILED_SOFT:
```

- **NOTE** in `launcher/minecraft/auth/AccountList.cpp`:143 — // NOTE: Do not allow adding something that's already there. We shouldn't let it continue
```
void AccountList::addAccount(const MinecraftAccountPtr account)
{
	// NOTE: Do not allow adding something that's already there. We shouldn't let it continue
	// because of the signal / slot connections after this.
	if (m_accounts.contains(account))
```

- **FIXME** in `launcher/minecraft/launch/LauncherPartLaunch.cpp`:201 — // FIXME: make this work again
```
				return;
			}
			// FIXME: make this work again
			//  m_postlaunchprocess.processEnvironment().insert("INST_EXITCODE", QString(exitCode));
			//  run post-exit
```

- **Note** in `launcher/minecraft/launch/MinecraftTarget.cpp`:42 — // Note: This parser intentionally mirrors Minecraft's address parsing behavior exactly,
```
#include <QStringList>

// Note: This parser intentionally mirrors Minecraft's address parsing behavior exactly,
// including its tolerance for malformed input. Invalid addresses resolve to unusable
// targets, which Minecraft handles at connection time. The isValid() method can be
```

- **TODO** in `launcher/minecraft/mod/DataPack.hpp`:53 — /* TODO:
```
class Version;

/* TODO:
 *
 * Store localized descriptions
```

- **NOTE** in `launcher/minecraft/mod/ResourceFolderModel.cpp`:124 — // NOTE: fix for GH-1178: remove trailing slash to avoid issues with using the empty result of QFileInfo::fileName
```
bool ResourceFolderModel::installResource(QString original_path)
{
	// NOTE: fix for GH-1178: remove trailing slash to avoid issues with using the empty result of QFileInfo::fileName
	original_path = FS::NormalizePath(original_path);
	QFileInfo file_info(original_path);
```

- **FIXME** in `launcher/minecraft/mod/ResourceFolderModel.cpp`:553 — // Sadece kopyalama destekleniyor, taşıma (move) desteği yok. FIXME kaldırıldı.
```
				continue;
			}
			// Sadece kopyalama destekleniyor, taşıma (move) desteği yok. FIXME kaldırıldı.
			if (!installResource(url.toLocalFile()))
			{
```

- **FIXME** in `launcher/minecraft/mod/ModDetails.hpp`:80 — // FIXME: come up with a better license parsing.
```
	ModLicense(const QString license)
	{
		// FIXME: come up with a better license parsing.
		// handle SPDX identifiers? https://spdx.org/licenses/
		auto parts				 = license.split(' ');
```

- **TODO** in `launcher/minecraft/mod/ResourceFolderModel.hpp`:79 — *  TODO: Make the resources unique pointers accessible through weak pointers.
```
 *  and the resource's lifetime is contingent on the model's lifetime.
 *
 *  TODO: Make the resources unique pointers accessible through weak pointers.
 */
class ResourceFolderModel : public QAbstractListModel
```

- **TODO** in `launcher/minecraft/mod/ResourceFolderModel.hpp`:308 — *  TODO: Figure out a way to express this relationship better without templated classes (Q_OBJECT macro disallows
```
	 *  This usually calls static_cast on the specific Task type returned by createUpdateTask,
	 *  so care must be taken in such cases.
	 *  TODO: Figure out a way to express this relationship better without templated classes (Q_OBJECT macro disallows
	 * that).
	 */
```

- **TODO** in `launcher/minecraft/mod/ResourcePack.hpp`:32 — /* TODO:
```
class Version;

/* TODO:
 *
 * Store localized descriptions
```

- **TODO** in `launcher/modplatform/packwiz/Packwiz.h`:111 — * TODO: Ask the user if they want to override, and delete the old mod's files, or keep the old one.
```
		/* Updates the mod index for the provided mod.
		 * This creates a new index if one does not exist already
		 * TODO: Ask the user if they want to override, and delete the old mod's files, or keep the old one.
		 * */
		static void updateModIndex(const QDir& index_dir, Mod& mod);
```

- **todo** in `launcher/modplatform/atlauncher/ATLPackInstallTask.cpp`:1241 — // todo: use more detection
```

		// Use a component to fill in the rest of the data
		// todo: use more detection
		if (!createPackComponent(instance.instanceRoot(), components))
		{
```

- **Note** in `launcher/modplatform/flame/FlameInstanceCreationTask.cpp`:115 — // Note: Duplicate modpack detection uses managed name or instance ID lookup.
```
	auto instance_list = APPLICATION->instances();

	// Note: Duplicate modpack detection uses managed name or instance ID lookup.
	// If multiple installations exist, the first match is updated.
	InstancePtr inst;
```

- **Note** in `launcher/modplatform/flame/FlameInstanceCreationTask.cpp`:223 — // Note: Overrides intentionally replace all files on update - this matches modpack author expectations.
```

		// We will remove all the previous overrides, to prevent duplicate files!
		// Note: Overrides intentionally replace all files on update - this matches modpack author expectations.
		auto old_overrides = Override::readOverrides("overrides", old_index_folder);
		for (const auto& entry : old_overrides)
```

- **FIXME** in `launcher/modplatform/flame/FlameAPI.h`:181 — // FIXME: Client-side version filtering. This won't take into account any user-selected filtering.
```
			return arr;
		}
		// FIXME: Client-side version filtering. This won't take into account any user-selected filtering.
		auto const& mc_versions = arr.mcVersion;

```

- **Note** in `launcher/modplatform/modrinth/ModrinthInstanceCreationTask.cpp`:66 — // Note: Duplicate modpack detection uses managed name or instance ID lookup.
```
	auto instance_list = APPLICATION->instances();

	// Note: Duplicate modpack detection uses managed name or instance ID lookup.
	// If multiple installations exist, the first match is updated.
	InstancePtr inst;
```

- **Note** in `launcher/modplatform/modrinth/ModrinthInstanceCreationTask.cpp`:185 — // Note: Overrides intentionally replace all files on update - this matches modpack author expectations.
```

		// We will remove all the previous overrides, to prevent duplicate files!
		// Note: Overrides intentionally replace all files on update - this matches modpack author expectations.
		// Disabled mods (.disabled extension) are handled by the file exclusion logic above.
		auto old_overrides = Override::readOverrides("overrides", old_index_folder);
```

- **Note** in `launcher/modplatform/modrinth/ModrinthInstanceCreationTask.cpp`:337 — // Note: Currently handles mods, resource packs, and shader packs.
```
	auto root_modpack_path = FS::PathCombine(m_stagingPath, m_root_path);
	auto root_modpack_url  = QUrl::fromLocalFile(root_modpack_path);
	// Note: Currently handles mods, resource packs, and shader packs.
	// Additional resource types can be added by extending the path prefix checks below.
	QHash<QString, Resource*> resources;
```

- **NOTE** in `launcher/modplatform/modrinth/ModrinthInstanceCreationTask.cpp`:447 — // NOTE: This needs to come before the copyManagedPack call!
```
		// Only change the name if it didn't use a custom name, so that the previous custom name
		// is preserved, but if we're using the original one, we update the version string.
		// NOTE: This needs to come before the copyManagedPack call!
		if (inst->name().contains(inst->getManagedPackVersionName()) && inst->name() != instance.name())
		{
```

- **NOTE** in `launcher/modplatform/modrinth/ModrinthPackIndex.cpp`:231 — // NOTE: files.count() is 1-indexed, so we need to subtract 1 to become 0-indexed
```
	// Will default to the last one if there's no primary (though I think Modrinth requires that
	// at least one file is primary, idk)
	// NOTE: files.count() is 1-indexed, so we need to subtract 1 to become 0-indexed
	while (i < files.count() - 1)
	{
```

- **TODO** in `launcher/resources/multimc/multimc.qrc`:228 — <!-- and the same again. TODO: make a nice accounts icon -->
```
        <file>48x48/noaccount.png</file>

        <!-- and the same again. TODO: make a nice accounts icon -->
        <file alias="8x8/accounts.png">8x8/noaccount.png</file>
        <file alias="16x16/accounts.png">16x16/noaccount.png</file>
```

- **NOTE** in `launcher/ui/dialogs/NewInstanceDialog.cpp`:117 — // NOTE: m_buttons must be initialized before PageContainer, because it indirectly accesses m_buttons through
```
	ui->groupBox->lineEdit()->setPlaceholderText(tr("No group"));

	// NOTE: m_buttons must be initialized before PageContainer, because it indirectly accesses m_buttons through
	// setSuggestedPack! Do not move this below.
	m_buttons = new QDialogButtonBox(QDialogButtonBox::Help | QDialogButtonBox::Ok | QDialogButtonBox::Cancel);
```

- **NOTE** in `launcher/ui/dialogs/IconPickerDialog.cpp`:94 — // NOTE: ResetRole forces the button to be on the left, while the OK/Cancel ones are on the right. We win.
```
	contentsWidget->setModel(proxyModel);

	// NOTE: ResetRole forces the button to be on the left, while the OK/Cancel ones are on the right. We win.
	auto buttonAdd = ui->buttonBox->addButton(tr("Add Icon"), QDialogButtonBox::ResetRole);
	buttonRemove   = ui->buttonBox->addButton(tr("Remove Icon"), QDialogButtonBox::ResetRole);
```

- **FIXME** in `launcher/ui/dialogs/ResourceUpdateDialog.cpp`:314 — // FIXME: Find a more efficient way of doing this!
```
	else
	{
		// FIXME: Find a more efficient way of doing this!

		// Sort major items in alphabetical order (also sorts the children unfortunately)
```

- **NOTE** in `launcher/ui/dialogs/ResourceDownloadDialog.cpp`:143 — // NOTE: We can't have this in the ctor because PageContainer calls a virtual function, and so
```
	}

	// NOTE: We can't have this in the ctor because PageContainer calls a virtual function, and so
	// won't work with subclasses if we put it in this ctor.
	void ResourceDownloadDialog::initializeContainer()
```

- **Note** in `launcher/ui/themes/ThemeManager.cpp`:122 — // Note: Icon themes are loaded separately from instance icons.
```
void ThemeManager::initializeIcons()
{
	// Note: Icon themes are loaded separately from instance icons.
	// Instance-specific icons override theme icons where applicable.
	themeDebugLog() << "<> Initializing Icon Themes";
```

- **note** in `launcher/ui/widgets/LogView.h`:44 — // note: this supports only appending
```
  protected slots:
	void repopulate();
	// note: this supports only appending
	void rowsInserted(const QModelIndex& parent, int first, int last);
	void rowsAboutToBeInserted(const QModelIndex& parent, int first, int last);
```

- **note** in `launcher/ui/widgets/LogView.h`:47 — // note: this supports only removing from front
```
	void rowsInserted(const QModelIndex& parent, int first, int last);
	void rowsAboutToBeInserted(const QModelIndex& parent, int first, int last);
	// note: this supports only removing from front
	void rowsRemoved(const QModelIndex& parent, int first, int last);
	void modelDestroyed(QObject* model);
```

- **NOTE** in `launcher/ui/widgets/PageContainer_p.h`:86 — // NOTE: Workaround for Qt icon stretching bug on Windows.
```
				if (icon.isNull())
					icon = m_emptyIcon;
				// NOTE: Workaround for Qt icon stretching bug on Windows.
				return QIcon(icon.pixmap(QSize(48, 48)));
			}
```

- **NOTE** in `launcher/ui/widgets/WideBar.cpp`:269 — // NOTE: This is needed so that disabled actions get reflected on the button when it is made
```
						entry.bar_action->setVisible(toggled);

						// NOTE: This is needed so that disabled actions get reflected on the button when it is made
						// visible.
						static_cast<ActionButton*>(widgetForAction(entry.bar_action))->actionChanged();
```

- **NOTE** in `launcher/ui/widgets/WideBar.cpp`:327 — // NOTE: This is needed so that disabled actions get reflected on the button when it is made visible.
```
		entry.bar_action->setVisible(bits.at(i++) == '1');

		// NOTE: This is needed so that disabled actions get reflected on the button when it is made visible.
		static_cast<ActionButton*>(widgetForAction(entry.bar_action))->actionChanged();
	}
```

- **TODO** in `launcher/ui/instanceview/InstanceView.cpp`:152 — // TODO: for accessibility support, implement+register a factory, steal QAccessibleTable from Qt and return an
```
{
	QAbstractItemView::currentChanged(current, previous);
	// TODO: for accessibility support, implement+register a factory, steal QAccessibleTable from Qt and return an
	// instance of it for InstanceView.
#ifndef QT_NO_ACCESSIBILITY
```

- **FIXME** in `launcher/ui/instanceview/InstanceView.cpp`:353 — // FIXME: better!
```
		if (index != currentIndex())
		{
			// FIXME: better!
			m_currentCursorColumn = -1;
		}
```

- **FIXME** in `launcher/ui/instanceview/AccessibleInstanceView.cpp`:427 — // FIXME: if indexPosition < 0 in one coordinate, return header
```
	QPoint viewportOffset = view()->viewport()->mapTo(view(), QPoint(0, 0));
	QPoint indexPosition  = view()->mapFromGlobal(QPoint(x, y) - viewportOffset);
	// FIXME: if indexPosition < 0 in one coordinate, return header

	QModelIndex index = view()->indexAt(indexPosition);
```

- **NOTE** in `launcher/ui/instanceview/InstanceProxyModel.cpp`:110 — // NOTE: Group visual positioning is handled in InstanceView::updateGeometries() with LocaleString.
```
	else
	{
		// NOTE: Group visual positioning is handled in InstanceView::updateGeometries() with LocaleString.
		// This comparison ensures logical sorting order matches the visual layout.
		auto result = leftCategory.localeAwareCompare(rightCategory);
```

- **NOTE** in `launcher/ui/pages/instance/ManagedPackPage.cpp`:129 — // NOTE: GTK2 themes crash with the proxy style.
```
	ui->setupUi(this);

	// NOTE: GTK2 themes crash with the proxy style.
	// This seems like an upstream bug, so there's not much else that can be done.
	if (!QStyleFactory::keys().contains("gtk2"))
```

- **NOTE** in `launcher/ui/pages/global/APIPage.cpp`:102 — // NOTE: this allows http://, but we replace that with https later anyway
```
	// This function needs to be called even when the ComboBox's index is still in its default state.
	updateBaseURLPlaceholder(ui->pasteTypeComboBox->currentIndex());
	// NOTE: this allows http://, but we replace that with https later anyway
	ui->metaURL->setValidator(new QRegularExpressionValidator(s_validUrlRegExp, ui->metaURL));
	ui->resourceURL->setValidator(new QRegularExpressionValidator(s_validUrlRegExp, ui->resourceURL));
```

- **Note** in `launcher/ui/pages/global/LauncherPage.cpp`:249 — // Note: Instance migration on folder change is a complex feature requiring user confirmation,
```

	// Folders
	// Note: Instance migration on folder change is a complex feature requiring user confirmation,
	// progress tracking, and rollback capability. Deferred for dedicated implementation.
	s->set("InstanceDir", ui->instDirTextBox->text());
```

- **fixme** in `launcher/ui/pages/modplatform/atlauncher/AtlOptionalModDialog.cpp`:240 — // fixme: plumb in an error message
```
	if (response.error)
	{
		// fixme: plumb in an error message
		qWarning() << "ATLauncher API Response Error" << response.message;
		return;
```

- **FIXME** in `launcher/ui/pages/modplatform/atlauncher/AtlOptionalModDialog.cpp`:245 — // FIXME: verify pack and version, error if not matching.
```
	}

	// FIXME: verify pack and version, error if not matching.

	// Clear the current selection
```

- **fixme** in `launcher/ui/pages/modplatform/atlauncher/AtlOptionalModDialog.cpp`:267 — // fixme: plumb in an error message
```
	m_jobPtr.reset();

	// fixme: plumb in an error message
}

```

- **fixme** in `launcher/ui/pages/modplatform/atlauncher/AtlOptionalModDialog.cpp`:302 — // fixme: avoid casting here
```
						   .arg(m_version.warnings[mod.warning], tr("Are you sure that you want to enable this mod?"));

		// fixme: avoid casting here
		auto result = QMessageBox::warning(qobject_cast<QWidget*>(this->parent()),
										   tr("Warning"),
```

- **Note** in `launcher/ui/pages/modplatform/flame/FlamePage.ui`:22 — <string>Note: CurseForge allows creators to block access to third-party tools like ProjT Launcher. As such, you may need to manually download some mods to be able to install a modpack.</string>
```
     </property>
     <property name="text">
      <string>Note: CurseForge allows creators to block access to third-party tools like ProjT Launcher. As such, you may need to manually download some mods to be able to install a modpack.</string>
     </property>
     <property name="alignment">
```

- **TODO** in `launcher/ui/pages/modplatform/modrinth/ModrinthModel.cpp`:187 — } // TODO: Move to standalone API
```
				return;
			}
		} // TODO: Move to standalone API
		ResourceAPI::SortingMethod sort{};
		sort.name = m_currentSort;
```

- **FIXME** in `launcher/ui/dialogs/skins/SkinManageDialog.cpp`:222 — // FIXME: add a model for this, download/refresh the capes on demand
```
void SkinManageDialog::setupCapes()
{
	// FIXME: add a model for this, download/refresh the capes on demand
	auto& accountData = *m_acct->accountData();
	int index		  = 0;
```

## Trivial (72)

- **Note** in `CMakeLists.txt`:119 — # Note, CMake 3.25 replaces this with CMAKE_MSVC_DEBUG_INFORMATION_FORMAT
```

    # See https://github.com/ccache/ccache/issues/1040
    # Note, CMake 3.25 replaces this with CMAKE_MSVC_DEBUG_INFORMATION_FORMAT
    # See https://cmake.org/cmake/help/v3.25/variable/CMAKE_MSVC_DEBUG_INFORMATION_FORMAT.html
    foreach(config DEBUG RELWITHDEBINFO)
```

- **Note** in `README.md`:80 — > **Note**: These directories contain original upstream READMEs preserved for reference.
```
```

> **Note**: These directories contain original upstream READMEs preserved for reference.
> For Project Tick–specific documentation, see [docs/handbook/](docs/handbook/).

```

- **TODO** in `.gitignore`:16 — docs/TODO.md
```
bzip2/lib*.a
docs/TODO_FIXME_REPORT.md
docs/TODO.md
# example build directories
bzip2/build
```

- **FIXME** in `launcher/CMakeLists.txt`:1613 — # FIXME: HACK: Fix manifest issues with Ninja in release mode (and only release mode) and MSVC
```

    target_sources("${Launcher_Name}_filelink" PRIVATE filelink/filelink.exe.manifest)
    # FIXME: HACK: Fix manifest issues with Ninja in release mode (and only release mode) and MSVC
    # I have no idea why this works or why it's needed. UPDATE THIS IF YOU EDIT THE MANIFEST!!! -@YongDo-Hyun
    # Thank you 2018 CMake mailing list thread https://cmake.cmake.narkive.com/LnotZXus/conflicting-msvc-manifests
```

- **NOTE** in `launcher/InstanceImportTask.cpp`:200 — // NOTE: Prioritize modpack platforms that aren't searched for recursively.
```
	QString root;

	// NOTE: Prioritize modpack platforms that aren't searched for recursively.
	// Especially Flame has a very common filename for its manifest, which may appear inside overrides for example
	// https://docs.modrinth.com/docs/modpacks/format_definition/#storage
```

- **NOTE** in `launcher/FileSystem.cpp`:354 — // NOTE always deep copy on windows. the alternatives are too messy.
```
		m_failedPaths.clear();

// NOTE always deep copy on windows. the alternatives are too messy.
#if defined Q_OS_WIN32
		m_followSymlinks = true;
```

- **TODO** in `launcher/FileSystem.cpp`:776 — // TODO: Flatpak trash desteği eklenmeli. org.freedesktop.portal.Trash D-Bus arayüzü ile dosyalar çöp kutusuna
```
	bool trash(QString path, QString* pathInTrash)
	{
		// TODO: Flatpak trash desteği eklenmeli. org.freedesktop.portal.Trash D-Bus arayüzü ile dosyalar çöp kutusuna
		// taşınabilir. See: https://flatpak.github.io/xdg-desktop-portal/docs/doc-org.freedesktop.portal.Trash.html
		// This requires D-Bus integration with the Trash portal for proper sandboxed file deletion
```

- **todo** in `libqrencode/Doxyfile`:499 — # disable (NO) the todo list. This list is created by putting \todo
```

# The GENERATE_TODOLIST tag can be used to enable (YES) or
# disable (NO) the todo list. This list is created by putting \todo
# commands in the documentation.

```

- **Note** in `libqrencode/Doxyfile`:1124 — # documentation. Note that a value of 0 will completely suppress the enum
```
# The ENUM_VALUES_PER_LINE tag can be used to set the number of enum values
# (range [0,1..20]) that doxygen will group on one line in the generated HTML
# documentation. Note that a value of 0 will completely suppress the enum
# values from appearing in the overview section.

```

- **Note** in `libqrencode/Doxyfile`:1146 — # as images in the HTML documentation. The default is 10. Note that
```

# Use this tag to change the font size of Latex formulas included
# as images in the HTML documentation. The default is 10. Note that
# when you change the font size after a successful doxygen run you need
# to manually remove any form_*.png images from the HTML output directory
```

- **Note** in `libqrencode/Doxyfile`:1188 — # and DHTML and should work on any modern browser. Note that when using
```
# When the SEARCHENGINE tag is enabled doxygen will generate a search box
# for the HTML output. The underlying search engine uses javascript
# and DHTML and should work on any modern browser. Note that when using
# HTML help (GENERATE_HTMLHELP), Qt help (GENERATE_QHP), or docsets
# (GENERATE_DOCSET) there is already a search function so this one should
```

- **Note** in `libqrencode/Doxyfile`:1332 — # Note: wordpad (write) and others do not support links.
```
# This makes the output suitable for online browsing using WORD or other
# programs which support those fields.
# Note: wordpad (write) and others do not support links.

RTF_HYPERLINKS         = NO
```

- **Note** in `libqrencode/Doxyfile`:1405 — # and cross-referencing information) to the XML output. Note that
```
# If the XML_PROGRAMLISTING tag is set to YES Doxygen will
# dump the program listings (including syntax highlighting
# and cross-referencing information) to the XML output. Note that
# enabling this will significantly increase the size of the XML output.

```

- **Note** in `libqrencode/Doxyfile`:1417 — # documentation. Note that this feature is still experimental
```
# generate an AutoGen Definitions (see autogen.sf.net) file
# that captures the structure of the code including all
# documentation. Note that this feature is still experimental
# and incomplete at the moment.

```

- **Note** in `libqrencode/Doxyfile`:1428 — # the code including all documentation. Note that this
```
# If the GENERATE_PERLMOD tag is set to YES Doxygen will
# generate a Perl module file that captures the structure of
# the code including all documentation. Note that this
# feature is still experimental and incomplete at the
# moment.
```

- **Note** in `libqrencode/Doxyfile`:1539 — # Note that each tag file must have a unique name
```
# URLs. If a location is present for each tag, the installdox tool
# does not have to be run to correct the links.
# Note that each tag file must have a unique name
# (where the name does NOT include the path)
# If a tag file is not located in the directory in which doxygen
```

- **Note** in `libqrencode/Doxyfile`:1712 — # Note that this requires a modern browser other than Internet Explorer.
```
# If DOT_IMAGE_FORMAT is set to svg, then this option can be set to YES to
# enable generation of interactive SVG images that allow zooming and panning.
# Note that this requires a modern browser other than Internet Explorer.
# Tested and working are Firefox, Chrome, Safari, and Opera. For IE 9+ you
# need to set HTML_FILE_EXTENSION to xhtml in order to make the SVG files
```

- **Note** in `libqrencode/use/config.rpath`:266 — # Note: this linker hardcodes the directories in LIBPATH if there
```
  case "$host_os" in
    aix3*)
      # Note: this linker hardcodes the directories in LIBPATH if there
      # are no directories specified by -L.
      hardcode_minus_L=yes
```

- **NOTE** in `meta/run/update_forge.py`:169 — # NOTE: For some insane reason, the format of the versions here is special. It having a branch at the end means it
```
    new_index = DerivedForgeIndex()

    # NOTE: For some insane reason, the format of the versions here is special. It having a branch at the end means it
    #           affects that particular branch.
    #       We don't care about Forge having branches.
```

- **Note** in `extra-cmake-modules/find-modules/FindWayland.cmake`:90 — # (Note that the version from wayland-egl.pc will probably be
```
# If pkg-config didn't provide us with version information,
# try to extract it from wayland-version.h
# (Note that the version from wayland-egl.pc will probably be
# the Mesa version, rather than the Wayland version, but that
# version will be ignored as we always find wayland-client.pc
```

- **FIXME** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:791 — # FIXME: do our export macros not deal with this properly?
```
    # message "set linker flag --export-all-symbols for all targets, otherwise
    # some depending targets could not be build"
    # FIXME: do our export macros not deal with this properly?
    set (CMAKE_SHARED_LINKER_FLAGS "${CMAKE_SHARED_LINKER_FLAGS} -Wl,--export-all-symbols")
    set (CMAKE_MODULE_LINKER_FLAGS "${CMAKE_MODULE_LINKER_FLAGS} -Wl,--export-all-symbols")
```

- **note** in `extra-cmake-modules/kde-modules/KDECMakeSettings.cmake`:30 — .. note::
```
``KDE_INSTALL_LIBDIR``), and also the linker search path.

.. note::
  ``LIB_INSTALL_DIR`` or alternatively ``KDE_INSTALL_LIBDIR`` needs
  to be set before including this module.
```

- **Note** in `extra-cmake-modules/kde-modules/KDEInstallDirs5.cmake`:16 — Note that none of the variables defined by this module provide any
```
Define KDE standard installation directories for Qt5/KF5 based software.

Note that none of the variables defined by this module provide any
information about the location of already-installed KDE software.

```

- **Note** in `extra-cmake-modules/kde-modules/KDEInstallDirs6.cmake`:18 — Note that none of the variables defined by this module provide any
```
Define KDE standard installation directories for Qt6/KF6 based software.

Note that none of the variables defined by this module provide any
information about the location of already-installed KDE software.

```

- **Note** in `extra-cmake-modules/modules/ECMQtDeclareLoggingCategory.cmake`:108 — ``.renamecategories``. Note: Before 5.113, the base name should not have any
```
to lower-cased ``<exportid>.categories``. The name of the file with info about
renamed categories will use the same final base name and the suffix
``.renamecategories``. Note: Before 5.113, the base name should not have any
further ``.`` in the name, as its end would be defined by that.

```

- **Note** in `extra-cmake-modules/modules/ECMSetupVersion.cmake`:85 — ``VARIABLE_PREFIX`` defaults to the project name.  Note that ``PROJECT`` must be the
```

This will use the version information set by the ``project()`` command.
``VARIABLE_PREFIX`` defaults to the project name.  Note that ``PROJECT`` must be the
first argument.  In all other respects, it behaves like the other form of the
command.
```

- **Note** in `extra-cmake-modules/modules/ECMPackageConfigHelpers.cmake`:34 — Note that there is no argument that will disable the ``find_dependency()`` macro;
```
for more information, particularly with regard to the ``PATH_VARS`` argument.

Note that there is no argument that will disable the ``find_dependency()`` macro;
if you do not require this macro, you should use
``configure_package_config_file`` from the CMakePackageConfigHelpers module.
```

- **TODO** in `extra-cmake-modules/modules/ECMEnableSanitizers.cmake`:162 — # TODO: GCC will not link pthread library if enabled ASan
```
            # check option and enable appropriate flags
            enable_sanitizer_flags ( ${CUR_SANITIZER} )
            # TODO: GCC will not link pthread library if enabled ASan
            if(CMAKE_C_COMPILER_ID MATCHES "Clang")
              set( CMAKE_C_FLAGS "${CMAKE_C_FLAGS} ${XSAN_COMPILE_FLAGS}" )
```

- **note** in `extra-cmake-modules/modules/ECMCoverageOption.cmake`:19 — .. note::
```
links against ``libgcov``.

.. note::
  This will probably break the build if you are not using GCC.

```

- **Note** in `extra-cmake-modules/modules/ECMFindModuleHelpers.cmake`:68 — Note that if version information is found via pkg-config,
```
<name>_<component>_VERSION.

Note that if version information is found via pkg-config,
<name>_<component>_FIND_VERSION can be set to require a particular version
for each component.
```

- **NOTE** in `cmake/vcpkg-triplets/universal-osx.cmake`:2 — # NOTE: Try to keep in sync with default arm64-osx definition
```
# See https://github.com/microsoft/vcpkg/discussions/19454
# NOTE: Try to keep in sync with default arm64-osx definition
set(VCPKG_TARGET_ARCHITECTURE x64)
set(VCPKG_CRT_LINKAGE dynamic)
```

- **TODO** in `ptinstaller/Source/build.h`:72 — //DW_STALE_TEMP = 5020?, TODO: There is currently no way to disable this
```
  //DE_STUB_SECTION = 1100?,
  //DE_PP_VERBOSE_BAD_LEVEL = 4000?,
  //DW_STALE_TEMP = 5020?, TODO: There is currently no way to disable this
  DW_PACKHDR_RETNONZERO = 5021,
  DW_BADFORMAT_EXTERNAL_FILE = 5040,
```

- **TODO** in `ptinstaller/Source/lang.h`:62 — * TODO: Need better documentation here.
```
     * variables.
     * 
     * TODO: Need better documentation here.
     * @param sn [out] Set to string ID number.
     * @param index [out] Set to index value in langstring.
```

- **TODO** in `ptinstaller/Contrib/Makeptisw/makeptisw.cpp`:901 — TCHAR buf[MAX_STRING]; // BUGBUG: TODO: Too small?
```
  PROCESS_INFORMATION pi;
  if (!CreateProcess(0, g_sdata.compile_command, 0, 0, TRUE, CREATE_NEW_CONSOLE, 0, 0, &si, &pi)) {
    TCHAR buf[MAX_STRING]; // BUGBUG: TODO: Too small?
    wsprintf(buf,_T("Could not execute:\r\n %s."), g_sdata.compile_command);
    ErrorMessage(g_sdata.hwnd, buf);
```

- **Note** in `ptinstaller/Contrib/Makeptisw/lookup.cpp`:152 — return hr; // Note: Not converting E_* here, FormatMessage knows what to do.
```
    }
    if (pcbData) *pcbData = (cbOutBuf|1) * 2; // We have no real value to set, caller just has to loop until success
    return hr; // Note: Not converting E_* here, FormatMessage knows what to do.
  }
  return ERROR_NOT_SUPPORTED;
```

- **Note** in `ptinstaller/Contrib/InstallOptions/Readme.html`:366 — <strong>Note:</strong> \r\n will be converted to a newline, two back-slashes will
```
                                    a messagebox will be displayed with this text.<br />
                                    <br />
                                    <strong>Note:</strong> \r\n will be converted to a newline, two back-slashes will
                                    be converted to one - \\. Described <a href="#escaping">below</a> are PTIS functions
                                    for converting text to/from this format.</td>
```

- **Note** in `ptinstaller/Contrib/UserInfo/UserInfo.c`:106 — // the token. Note that we expect a FALSE result from GetTokenInformation
```
    {
      // We must query the size of the group information associated with
      // the token. Note that we expect a FALSE result from GetTokenInformation
      // because we've given it a NULL buffer. On exit cbTokenGroups will tell
      // the size of the group information.
```

- **NOTE** in `ptinstaller/Contrib/Modern UI/Changelog.txt`:221 — (NOTE: These macros have been removed in version 1.4)
```
* Renamed some macros
* Custom code can be used between page start/stop macros
  (NOTE: These macros have been removed in version 1.4)

1.18 - Semtember 13, 2002
```

- **note** in `ptinstaller/Contrib/PTISdl/httpget.h`:37 — **   note that user, pass, port, and /requestwhatever are all optional :)
```
** URL String:
**   should be in the format of http://user:pass@host:port/requestwhatever
**   note that user, pass, port, and /requestwhatever are all optional :)
**   note that also, http:// is really not important. if you do poo://
**   or even leave out the http:// altogether, it will still work.
```

- **note** in `ptinstaller/Contrib/PTISdl/httpget.h`:38 — **   note that also, http:// is really not important. if you do poo://
```
**   should be in the format of http://user:pass@host:port/requestwhatever
**   note that user, pass, port, and /requestwhatever are all optional :)
**   note that also, http:// is really not important. if you do poo://
**   or even leave out the http:// altogether, it will still work.
**
```

- **TODO** in `ptinstaller/Contrib/PTIS Menu/ptismenu/nslinks.cpp`:34 — // TODO: These helper function declarations should be in a header file
```
FORCE_LINK_ME(nslinks)

// TODO: These helper function declarations should be in a header file
typedef enum { SUT_UNKNOWN = 0, SUT_BIN = 0x14, SUT_DOC = 0x24, SUT_WEB = 0x34 } SPECIALURLTYPE;
SPECIALURLTYPE TransformUrl(wxString&Url);
```

- **note** in `ptinstaller/Contrib/PTIS Menu/wx/setup.h`:574 — // Please note that on Windows you will need to link with gdiplus.lib (use
```
// 2D drawing API.  (Still somewhat experimental)
//
// Please note that on Windows you will need to link with gdiplus.lib (use
// USE_GDIPLUS=1 for makefile builds) and distribute gdiplus.dll with your
// application if you want it to be runnable on pre-XP systems.
```

- **Note** in `ptinstaller/Contrib/PTIS Menu/wx/setup.h`:989 — // set to 1. Note that for some compilers (notably Microsoft Visual C++) you
```
// Setting wxUSE_GLCANVAS to 1 enables OpenGL support. You need to have OpenGL
// headers and libraries to be able to compile the library with wxUSE_GLCANVAS
// set to 1. Note that for some compilers (notably Microsoft Visual C++) you
// will need to manually add opengl32.lib and glu32.lib to the list of
// libraries linked with your program if you use OpenGL.
```

- **Note** in `ptinstaller/Contrib/Library/RegTool/RegTool.cpp`:127 — template<class T> void RenameViaWininit(const T* prevName, const T* newName) // Note: Not thread safe!
```
 * will be deleted.
 */
template<class T> void RenameViaWininit(const T* prevName, const T* newName) // Note: Not thread safe!
{
  static char szRenameLine[1024];
```

- **Note** in `ptinstaller/Docs/src/defines.but`:23 — If \e{/math} is used, the result of 'val1 OP val2', where OP may be +,-,*,/,%,<<,>>,>>>,&,|,^,~,!,&& or ||, will be used as the value of \e{gflag}. Note that val1 AND val2 MUST be integer values!
```
If \e{/intfmt} is used, \e{value} is interpreted as a integer and formatted using the same syntax as \R{intfmt}{IntFmt}.

If \e{/math} is used, the result of 'val1 OP val2', where OP may be +,-,*,/,%,<<,>>,>>>,&,|,^,~,!,&& or ||, will be used as the value of \e{gflag}. Note that val1 AND val2 MUST be integer values!

\c !define USE_SOMETHING
```

- **Note** in `ptinstaller/Docs/src/headers.but`:627 — \\<b\\>Note:\\</b\\>
```
\c $var                ; Result: Version number

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if file doesn't exist
\\<br\\>- Error flag if file doesn't contain version information
```

- **note** in `ptinstaller/Docs/src/build.but`:108 — \\<b\\>Important note for Microsoft Visual C++ 2012 users:\\</b\\> Installers generated using this release and beyond will not be able to run on Windows 9x or Windows 2000. The minimum requirement for 2012 is Windows XP.
```
\\<b\\>Important notes for Microsoft Visual C++ 6.0 users:\\</b\\> The 2003 Platform SDK must be installed before building, you can download it \W{http://groups.google.com/group/microsoft.public.platformsdk.sdk_install/msg/087b0178f5d8159e}{here} or \W{http://go.microsoft.com/fwlink/?LinkId=48615}{order it on CD}. Because of flaws in the libraries distributed with Microsoft Visual C++ 6.0, not installing the Platform SDK will result in crashes when using the \R{copyfiles}{CopyFiles} command. See \W{http://forums.winamp.com/showthread.php?s=&threadid=131964}{this forum topic} for more information. Installing the \W{http://wayback.archive.org/web/20100923113933/http://msdn.microsoft.com/en-us/vstudio/aa718349.aspx}{Processor Pack} is highly recommended to decrease the size of the installer overhead.

\\<b\\>Important note for Microsoft Visual C++ 2012 users:\\</b\\> Installers generated using this release and beyond will not be able to run on Windows 9x or Windows 2000. The minimum requirement for 2012 is Windows XP.

\H{build_posix} Building on POSIX
```

- **Note** in `ptinstaller/Docs/src/attributes.but`:251 — Specifies a text file or a RTF file to use for the license that the user can read. Omit this to not have a license displayed. Note that the file must be in DOS text format (\\r\\n). To define a multilingual license data use \R{licenselangstring}{LicenseLangString}.
```
\c licdata.(txt|rtf)

Specifies a text file or a RTF file to use for the license that the user can read. Omit this to not have a license displayed. Note that the file must be in DOS text format (\\r\\n). To define a multilingual license data use \R{licenselangstring}{LicenseLangString}.

If you are using a RTF file it is recommended that you edit it with WordPad and not MS Word. Using WordPad will result in a much smaller file.
```

- **Note** in `ptinstaller/Docs/src/attributes.but`:413 — Specifies whether or not the installer should be silent. If it is 'silent' or 'silentlog', all sections that have the SF_SELECTED flag are installed quietly (you can set this flag using \R{sectionsetflags}{SectionSetFlags}), with no screen output from the installer itself (the script can still display whatever it wants, use \R{messagebox}{MessageBox}'s /SD to specify a default for silent installers). Note that if this is set to 'normal' and the user runs the installer with /S (case sensitive) on the command line, it will behave as if \R{asilentinstall}{SilentInstall} 'silent' was used. Note: see also \R{logset}{LogSet}.
```
\c \\<b\\>normal\\</b\\>|silent|silentlog

Specifies whether or not the installer should be silent. If it is 'silent' or 'silentlog', all sections that have the SF_SELECTED flag are installed quietly (you can set this flag using \R{sectionsetflags}{SectionSetFlags}), with no screen output from the installer itself (the script can still display whatever it wants, use \R{messagebox}{MessageBox}'s /SD to specify a default for silent installers). Note that if this is set to 'normal' and the user runs the installer with /S (case sensitive) on the command line, it will behave as if \R{asilentinstall}{SilentInstall} 'silent' was used. Note: see also \R{logset}{LogSet}.

See \k{silent} for more information.
```

- **Note** in `ptinstaller/Docs/src/attributes.but`:421 — Specifies whether or not the uninstaller should be silent. If it is 'silent' the uninstall sections will run quietly, with no screen output from the uninstaller itself (the script can still display whatever it wants, use \R{messagebox}{MessageBox}'s /SD to specify a default for silent uninstallers). Note that if this is set to 'normal' and the user runs the uninstaller with /S on the command line, it will behave as if \R{asilentuninstall}{SilentUnInstall} 'silent' was used.
```
\c \\<b\\>normal\\</b\\>|silent

Specifies whether or not the uninstaller should be silent. If it is 'silent' the uninstall sections will run quietly, with no screen output from the uninstaller itself (the script can still display whatever it wants, use \R{messagebox}{MessageBox}'s /SD to specify a default for silent uninstallers). Note that if this is set to 'normal' and the user runs the uninstaller with /S on the command line, it will behave as if \R{asilentuninstall}{SilentUnInstall} 'silent' was used.

See \k{silent} for more information.
```

- **Note** in `ptinstaller/Docs/src/library.but`:27 — Note that the library macros are limited on non-Windows platforms. DLL version information is required when compiling on non-Windows platforms.
```
\c !include Library.nsh

Note that the library macros are limited on non-Windows platforms. DLL version information is required when compiling on non-Windows platforms.


```

- **Note** in `ptinstaller/Docs/src/generalpurpose.but`:44 — keyboard_shortcut should be in the form of 'flag|c' where flag can be a combination (using |) of: \e{ALT}, \e{CONTROL}, \e{EXT}, or \e{SHIFT}. c is the character to use (a-z, A-Z, 0-9, F1-F24, etc). Note that no spaces are allowed in this string. A good example is "ALT|CONTROL|F8". $OUTDIR is stored as the shortcut's working directory property. You can change it by using \R{setoutpath}{SetOutPath} before creating the shortcut or use /NoWorkingDir if you don't need to set the working directory property.
```
The icon used for the shortcut is 'icon.file,icon_index_number'; for default icon settings use empty strings for both icon.file and icon_index_number.
start_options should be one of: \e{SW_SHOWNORMAL}, \e{SW_SHOWMAXIMIZED}, \e{SW_SHOWMINIMIZED}, or an empty string.
keyboard_shortcut should be in the form of 'flag|c' where flag can be a combination (using |) of: \e{ALT}, \e{CONTROL}, \e{EXT}, or \e{SHIFT}. c is the character to use (a-z, A-Z, 0-9, F1-F24, etc). Note that no spaces are allowed in this string. A good example is "ALT|CONTROL|F8". $OUTDIR is stored as the shortcut's working directory property. You can change it by using \R{setoutpath}{SetOutPath} before creating the shortcut or use /NoWorkingDir if you don't need to set the working directory property.
description should be the description of the shortcut, or comment as it is called under XP.
The error flag is set if the shortcut cannot be created (i.e. either of the paths (link or target) does not exist, or some other error).
```

- **Note** in `ptinstaller/Docs/src/log.but`:7 — Sets whether install logging to $INSTDIR\\install.log will happen. $INSTDIR must have a value before you call this function or it will not work. Note that the \e{PTIS_CONFIG_LOG} build setting must be set (\c{scons PTIS_CONFIG_LOG=yes}) when building (it is not set by default) to support this. See \R{build}{Building PTIS} for more information about recompiling PTIS.
```
\c on|\\<b\\>off\\</b\\>

Sets whether install logging to $INSTDIR\\install.log will happen. $INSTDIR must have a value before you call this function or it will not work. Note that the \e{PTIS_CONFIG_LOG} build setting must be set (\c{scons PTIS_CONFIG_LOG=yes}) when building (it is not set by default) to support this. See \R{build}{Building PTIS} for more information about recompiling PTIS.

\S2{logtext} LogText
```

- **Note** in `ptinstaller/Docs/src/basic.but`:49 — Execute the specified program and wait for the executed process to quit. See \R{exec}{Exec} for more information. If no output variable is specified \R{execwait}{ExecWait} sets the error flag if the program executed returns a nonzero error code, or if there is an error. If an output variable is specified, \R{execwait}{ExecWait} sets the variable with the exit code (and only sets the error flag if an error occurs; if an error occurs the contents of the user variable are undefined). Note, if the command could have spaces, you should put it in quotes to delimit it from parameters. e.g.: ExecWait '"$INSTDIR\\command.exe" parameters'. If you don't put it in quotes it will \e{not} work on Windows 9x with or without parameters.
```
\c command [user_var(exit code)]

Execute the specified program and wait for the executed process to quit. See \R{exec}{Exec} for more information. If no output variable is specified \R{execwait}{ExecWait} sets the error flag if the program executed returns a nonzero error code, or if there is an error. If an output variable is specified, \R{execwait}{ExecWait} sets the variable with the exit code (and only sets the error flag if an error occurs; if an error occurs the contents of the user variable are undefined). Note, if the command could have spaces, you should put it in quotes to delimit it from parameters. e.g.: ExecWait '"$INSTDIR\\command.exe" parameters'. If you don't put it in quotes it will \e{not} work on Windows 9x with or without parameters.

\c ExecWait '"$INSTDIR\someprogram.exe"'
```

- **Note** in `ptinstaller/Docs/src/compiler.but`:15 — This command will include 'file' as if it was part of the original script. Note that if a file is included in another directory, the current directory is still where the script was compiled from (not where the included file resides). If the compiler can't find the file it will look for it in every include directory. See \R{addincludedir}{!addincludedir} for more information. If the /nonfatal switch is used and no files are found, a warning will be issued instead of an error. /charset can be used to specify a codepage for plain text files without a BOM.
```
\c [/NONFATAL] [/CHARSET=ACP|OEM|CP#|UTF8|UTF16LE|UTF16BE] file

This command will include 'file' as if it was part of the original script. Note that if a file is included in another directory, the current directory is still where the script was compiled from (not where the included file resides). If the compiler can't find the file it will look for it in every include directory. See \R{addincludedir}{!addincludedir} for more information. If the /nonfatal switch is used and no files are found, a warning will be issued instead of an error. /charset can be used to specify a codepage for plain text files without a BOM.

\c !include WinMessages.nsh
```

- **TODO** in `ptinstaller/Source/exehead/util.h`:183 — MGA_SHGetFolderPath, // TODO: This can probably call something else directly on x64
```
#endif
  MGA_SHAutoComplete, // x64 can link to shlwapi directly but as long as MGA_SHGetFolderPath is used we can stick with myGetProcAddress
  MGA_SHGetFolderPath, // TODO: This can probably call something else directly on x64
#ifdef PTIS_SUPPORT_GETDLLVERSION
  MGA_GetFileVersionInfoSize, // Version.dll exists in all Windows versions, it is delay loaded to avoid DLL hijacking [bug #1125]
```

- **FIXME** in `ptinstaller/SCons/Config/ms`:52 — #	env.Append(CCFLAGS = ['/GL']) # FIXME: Creates problems with memset in VS v8.0 (CL v14.00)
```
		env.Append(LINKFLAGS = ['/OPT:ICF,9'])
		#if msvs_version >= 7.0:
		#	env.Append(CCFLAGS = ['/GL']) # FIXME: Creates problems with memset in VS v8.0 (CL v14.00)
		#	env.Append(LINKFLAGS = ['/LTCG'])
		return
```

- **Note** in `launcher/news/NewsEntry.h`:56 — * Note that content may contain HTML.
```
	/*!
	 * Constructs a new news entry.
	 * Note that content may contain HTML.
	 */
	NewsEntry(const QString& title, const QString& content, const QString& link, QObject* parent = 0);
```

- **NOTE** in `launcher/minecraft/VersionFile.h`:131 — * NOTE: this is shared with the meta format!!!
```
	/**
	 * ProjT Launcher: set of packages this depends on
	 * NOTE: this is shared with the meta format!!!
	 */
	projt::meta::DependencySet m_requires;
```

- **NOTE** in `launcher/minecraft/VersionFile.h`:137 — * NOTE: this is shared with the meta format!!!
```
	/**
	 * ProjT Launcher: set of packages this conflicts with
	 * NOTE: this is shared with the meta format!!!
	 */
	projt::meta::DependencySet conflicts;
```

- **Note** in `launcher/minecraft/MinecraftInstance.cpp`:370 — // Note: This check may not work correctly for non-release versions due to version string formatting. Demo support
```
	Version instance_ver{ getPackProfile()->getComponentVersion("net.minecraft") };
	// Demo mode was introduced in 1.3.1: https://minecraft.wiki/w/Demo_mode#History
	// Note: This check may not work correctly for non-release versions due to version string formatting. Demo support
	// is based on release versions.
	return instance_ver >= Version("1.3.1");
```

- **NOTE** in `launcher/minecraft/Rule.h`:87 — // NOTE: unsupported, but retained to avoid information loss
```
	{
		QString name;
		// NOTE: unsupported, but retained to avoid information loss
		QString version;
	};
```

- **NOTE** in `launcher/minecraft/mod/tasks/LocalModParseTask.cpp`:80 — // NOTE: 'authors' is used as a fallback or alternative key for 'authorList' in some metadata formats.
```
			if (authors.size() == 0)
			{
				// NOTE: 'authors' is used as a fallback or alternative key for 'authorList' in some metadata formats.
				authors = firstObj.value("authors").toArray();
			}
```

- **NOTE** in `launcher/ui/dialogs/ImportResourceDialog.cpp`:53 — // NOTE: We can't have uniform sizes because the text may wrap if it's too long. If we set this, it will cut off the
```
	contentsWidget->setWordWrap(true);
	contentsWidget->setWrapping(true);
	// NOTE: We can't have uniform sizes because the text may wrap if it's too long. If we set this, it will cut off the
	// wrapped text.
	contentsWidget->setUniformItemSizes(false);
```

- **NOTE** in `launcher/ui/pages/instance/ManagedPackPage.cpp`:334 — // NOTE: the id from version isn't the same id in the modpack format spec...
```
			QString name = version.getVersionDisplayString();

			// NOTE: the id from version isn't the same id in the modpack format spec...
			// e.g. HexMC's 4.4.0 has versionId 4.0.0 in the modpack index..............
			if (version.version == m_inst->getManagedPackVersionName())
```

- **FIXME** in `launcher/ui/pages/instance/ScreenshotsPage.cpp`:220 — // FIXME: this is a workaround for a bug in QFileSystemModel, where it doesn't
```
		if (role != Qt::EditRole)
			return false;
		// FIXME: this is a workaround for a bug in QFileSystemModel, where it doesn't
		// sort after renames
		{
```

- **Note** in `launcher/ui/pages/global/ProxyPage.ui`:190 — <string>Note: Proxy username and password are stored in plain text inside the launcher's configuration file!</string>
```
       <widget class="QLabel" name="proxyPlainTextWarningLabel">
        <property name="text">
         <string>Note: Proxy username and password are stored in plain text inside the launcher's configuration file!</string>
        </property>
        <property name="wordWrap">
```

- **Note** in `launcher/ui/pages/global/APIPage.ui`:90 — <string>Note: you probably want to change or clear the Base URL after changing the paste service type.</string>
```
           <widget class="QLabel" name="baseURLNote">
            <property name="text">
             <string>Note: you probably want to change or clear the Base URL after changing the paste service type.</string>
            </property>
            <property name="wordWrap">
```

- **Note** in `launcher/ui/pages/global/APIPage.ui`:231 — <string>Note: you probably don't need to set this if logging in via Microsoft Authentication already works.</string>
```
           <widget class="QLabel" name="label_3">
            <property name="text">
             <string>Note: you probably don't need to set this if logging in via Microsoft Authentication already works.</string>
            </property>
            <property name="textFormat">
```

- **Note** in `launcher/ui/pages/global/APIPage.ui`:289 — <string>&lt;html&gt;&lt;head/&gt;&lt;body&gt;&lt;p&gt;Note: you only need to set this to access private data. Read the &lt;a href=&quot;https://docs.modrinth.com/api/#authentication&quot;&gt;documentation&lt;/a&gt; for more information.&lt;/p&gt;&lt;/body&gt;&lt;/html&gt;</string>
```
           <widget class="QLabel" name="label_8">
            <property name="text">
             <string>&lt;html&gt;&lt;head/&gt;&lt;body&gt;&lt;p&gt;Note: you only need to set this to access private data. Read the &lt;a href=&quot;https://docs.modrinth.com/api/#authentication&quot;&gt;documentation&lt;/a&gt; for more information.&lt;/p&gt;&lt;/body&gt;&lt;/html&gt;</string>
            </property>
            <property name="wordWrap">
```

- **Note** in `launcher/ui/pages/global/APIPage.ui`:347 — <string>Note: you probably don't need to set this if CurseForge already works.</string>
```
           <widget class="QLabel" name="label_10">
            <property name="text">
             <string>Note: you probably don't need to set this if CurseForge already works.</string>
            </property>
            <property name="wordWrap">
```

- **Note** in `launcher/ui/pages/global/APIPage.ui`:390 — <string>&lt;html&gt;&lt;head/&gt;&lt;body&gt;&lt;p&gt;Note: you only need to set this to access private data.&lt;/p&gt;&lt;/body&gt;&lt;/html&gt;</string>
```
           <widget class="QLabel" name="label_11">
            <property name="text">
             <string>&lt;html&gt;&lt;head/&gt;&lt;body&gt;&lt;p&gt;Note: you only need to set this to access private data.&lt;/p&gt;&lt;/body&gt;&lt;/html&gt;</string>
            </property>
            <property name="wordWrap">
```

## Unknown (201)

- **NOTE** in `CMakeLists.txt`:387 — # NOTE: These API keys are here for convenience. If you rebrand this software or intend to break the terms of service
```

# API Keys
# NOTE: These API keys are here for convenience. If you rebrand this software or intend to break the terms of service
# of these platforms, please change these API keys beforehand.
# Be aware that if you were to use these API keys for malicious purposes they might get revoked, which might cause
```

- **Note** in `CMakeLists.txt`:433 — # Note: ZLIB is handled via bundled version only, no NuGet fallback
```
    endif()

    # Note: ZLIB is handled via bundled version only, no NuGet fallback

    # Find OpenSSL from NuGet
```

- **NOTE** in `CMakeLists.txt`:900 — # NOTE: this must always be last to appease the CMake deity of quirky install command evaluation order.
```
    projt_pop_output_dirs()
endif()
# NOTE: this must always be last to appease the CMake deity of quirky install command evaluation order.
if(NOT LAUNCHER_FUZZ_ONLY)
    projt_push_output_dirs("launcher")
```

- **Note** in `launcher/BaseInstance.h`:235 — * Note that this method is not const.
```
	 * This settings object stores instance-specific settings.
	 *
	 * Note that this method is not const.
	 * It may call loadSpecificSettings() to ensure those are loaded.
	 *
```

- **NOTE** in `launcher/MTPixmapCache.h`:100 — // NOTE: Every function returns something non-void to simplify the macros.
```
	DEFINE_FUNC_ONE_PARAM(setFastEvictionThreshold, bool, int)

	// NOTE: Every function returns something non-void to simplify the macros.
  private slots:
	int _cacheLimit()
```

- **NOTE** in `launcher/InstanceList.h`:266 — // NOTE: Optimized using QHash for cached lookups.
```
	QString m_instDir;
	QFileSystemWatcher* m_watcher;
	// NOTE: Optimized using QHash for cached lookups.
	QSet<QString> m_collapsedGroups;
	QHash<InstanceId, GroupId> m_instanceGroupIndex;
```

- **NOTE** in `launcher/FileSystem.cpp`:1275 — // NOTE: std::copy may not overwrite existing files on GNU libstdc++ on Windows
```
		fs::copy_options opt = copy_opts::recursive | copy_opts::overwrite_existing;

		// NOTE: std::copy may not overwrite existing files on GNU libstdc++ on Windows
		fs::copy(StringUtils::toStdString(override_path), StringUtils::toStdString(overwritten_path), opt, err);

```

- **NOTE** in `launcher/FileSystem.cpp`:1869 — // NOTE: this resizing might seem weird...
```
		if (length == 0)
			return {};
		// NOTE: this resizing might seem weird...
		// when GetShortPathNameW fails, it returns length including null character
		// when it succeeds, it returns length excluding null character
```

- **NOTE** in `launcher/InstanceList.cpp`:814 — // NOTE: Some launcher versions didn't save the InstanceType properly. We will just bank on the probability that
```
	QString inst_type = instanceSettings->get("InstanceType").toString();

	// NOTE: Some launcher versions didn't save the InstanceType properly. We will just bank on the probability that
	// this is probably a OneSix instance
	if (inst_type == "OneSix" || inst_type.isEmpty())
```

- **Note** in `launcher/PSaveFile.h`:66 — * Note: Please do not use the `setFileName` function directly, as it
```
 * files during critical operations.
 *
 * Note: Please do not use the `setFileName` function directly, as it
 * is not virtual and cannot be overridden.
 */
```

- **NOTE** in `launcher/BaseInstance.cpp`:119 — // NOTE: Sometimees InstanceType is already registered, as it was used to identify the type of
```
	m_settings->registerOverride(globalSettings->getSetting("RecordGameTime"), gameTimeOverride);

	// NOTE: Sometimees InstanceType is already registered, as it was used to identify the type of
	// a locally stored instance
	if (!m_settings->getSetting("InstanceType"))
```

- **NOTE** in `launcher/InstanceCopyTask.cpp`:54 — // NOTE: Regex for filtering files during copy.
```
	{
		// Set regex filter:
		// NOTE: Regex for filtering files during copy.
		QRegularExpression regexp(filters, QRegularExpression::CaseInsensitiveOption);
		m_matcher = Filters::regexp(regexp);
```

- **TODO** in `ptinstaller/TODO.txt`:1 — TODO
```
TODO
----

```

- **TODO** in `cmake/GetGitRevisionDescription.cmake`:193 — # TODO sanitize
```
    endif()

    # TODO sanitize
    #if((${ARGN}" MATCHES "&&") OR
    #	(ARGN MATCHES "||") OR
```

- **Note** in `tests/Index_test.cpp`:33 — // Note: MetaIndex now requires different initialization
```
	{
		projt::meta::MetaIndex windex;
		// Note: MetaIndex now requires different initialization
		// Basic test for component existence
		QVERIFY(!windex.hasComponent("nonexistent"));
```

- **Note** in `libqrencode/acinclude.m4`:312 — # contains only /bin. Note that ksh looks also at the FPATH variable,
```
if test "${PATH_SEPARATOR+set}" != set; then
  # Determine PATH_SEPARATOR by trying to find /bin/sh in a PATH which
  # contains only /bin. Note that ksh looks also at the FPATH variable,
  # so we have to set that as well for the test.
  PATH_SEPARATOR=:
```

- **Note** in `libqrencode/acinclude.m4`:1039 — dnl Note: acl_hardcode_libdir_flag_spec uses $libdir and $wl.
```
        alldirs="${alldirs}${alldirs:+$acl_hardcode_libdir_separator}$found_dir"
      done
      dnl Note: acl_hardcode_libdir_flag_spec uses $libdir and $wl.
      acl_save_libdir="$libdir"
      libdir="$alldirs"
```

- **NOTE** in `libqrencode/qrinput.c`:267 — * @param parity parity among input data. (NOTE: each symbol of a set of structured symbols has the same parity data)
```
 * @param size number of structured symbols.
 * @param number index number of the symbol. (1 <= number <= size)
 * @param parity parity among input data. (NOTE: each symbol of a set of structured symbols has the same parity data)
 * @retval 0 success.
 * @retval -1 error occurred and errno is set to indeicate the error. See Execptions for the details.
```

- **note** in `libqrencode/qrencode.h`:42 — * Please note that a C string can not contain NUL characters. If your data
```
 * QRcode_encodeString8bit() instead.
 *
 * Please note that a C string can not contain NUL characters. If your data
 * contains NUL, you must use QRcode_encodeData().
 *
```

- **note** in `libqrencode/qrencode.h`:59 — * Please note that the version of the result may be larger than specified.
```
 * See ::QRcode for the details. You can free the object by QRcode_free().
 *
 * Please note that the version of the result may be larger than specified.
 * In such cases, the input data would be too large to be encoded in a
 * symbol of the specified version.
```

- **Note** in `libqrencode/Doxyfile`:88 — # Note: if both HIDE_UNDOC_MEMBERS and BRIEF_MEMBER_DESC are set to NO, the
```
# If the REPEAT_BRIEF tag is set to YES (the default) Doxygen will prepend
# the brief description of a member or function before the detailed description.
# Note: if both HIDE_UNDOC_MEMBERS and BRIEF_MEMBER_DESC are set to NO, the
# brief descriptions will be completely suppressed.

```

- **Note** in `libqrencode/Doxyfile`:483 — # Note: This option is not very useful if HIDE_SCOPE_NAMES is set to YES.
```
# NO (the default), the class list will be sorted only by class name,
# not including the namespace part.
# Note: This option is not very useful if HIDE_SCOPE_NAMES is set to YES.
# Note: This option applies only to the class list, not to the
# alphabetical list.
```

- **Note** in `libqrencode/Doxyfile`:484 — # Note: This option applies only to the class list, not to the
```
# not including the namespace part.
# Note: This option is not very useful if HIDE_SCOPE_NAMES is set to YES.
# Note: This option applies only to the class list, not to the
# alphabetical list.

```

- **Note** in `libqrencode/Doxyfile`:680 — # Note that relative paths are relative to the directory from which doxygen is
```
# excluded from the INPUT source files. This way you can easily exclude a
# subdirectory from a directory tree whose root is specified with the INPUT tag.
# Note that relative paths are relative to the directory from which doxygen is
# run.

```

- **Note** in `libqrencode/Doxyfile`:693 — # certain files from those directories. Note that the wildcards are matched
```
# If the value of the INPUT tag contains directories, you can use the
# EXCLUDE_PATTERNS tag to specify one or more wildcard patterns to exclude
# certain files from those directories. Note that the wildcards are matched
# against the file with absolute path, so to exclude all test directories
# for example use the pattern */test/*
```

- **Note** in `libqrencode/Doxyfile`:775 — # Note: To get rid of all source code in the generated output, make sure also
```
# If the SOURCE_BROWSER tag is set to YES then a list of source files will
# be generated. Documented entities will be cross-referenced with these sources.
# Note: To get rid of all source code in the generated output, make sure also
# VERBATIM_HEADERS is set to NO.

```

- **Note** in `libqrencode/Doxyfile`:876 — # that header. Note that the header is subject to change so you typically
```
# It is advised to generate a default header using "doxygen -w html
# header.html footer.html stylesheet.css YourConfigFile" and then modify
# that header. Note that the header is subject to change so you typically
# have to redo this when upgrading to a newer version of doxygen or when
# changing the value of configuration settings such as GENERATE_TREEVIEW!
```

- **Note** in `libqrencode/Doxyfile`:891 — # will generate a default style sheet. Note that doxygen will try to copy
```
# style sheet that is used by each HTML page. It can be used to
# fine-tune the look of the HTML output. If the tag is left blank doxygen
# will generate a default style sheet. Note that doxygen will try to copy
# the style sheet file to the HTML output directory, so don't put your own
# style sheet in the HTML output directory as well, or it will be erased!
```

- **note** in `libqrencode/Doxyfile`:901 — # files. In the HTML_STYLESHEET file, use the file name only. Also note that
```
# that these files will be copied to the base HTML output directory. Use the
# $relpath$ marker in the HTML_HEADER and/or HTML_FOOTER files to load these
# files. In the HTML_STYLESHEET file, use the file name only. Also note that
# the files will be copied as-is; there are no commands or markers available.

```

- **Note** in `libqrencode/Doxyfile`:1223 — # Note that when enabling USE_PDFLATEX this option is only used for
```
# The LATEX_CMD_NAME tag can be used to specify the LaTeX command name to be
# invoked. If left blank `latex' will be used as the default command name.
# Note that when enabling USE_PDFLATEX this option is only used for
# generating bitmaps for formulas in the HTML output, but not in the
# Makefile that is written to the output directory.
```

- **Note** in `libqrencode/Doxyfile`:1294 — # Note that which sources are shown also depends on other settings
```
# If LATEX_SOURCE_CODE is set to YES then doxygen will include
# source code with syntax highlighting in the LaTeX output.
# Note that which sources are shown also depends on other settings
# such as SOURCE_BROWSER.

```

- **Note** in `libqrencode/Doxyfile`:1574 — # or super classes. Setting the tag to NO turns the diagrams off. Note that
```
# If the CLASS_DIAGRAMS tag is set to YES (the default) Doxygen will
# generate a inheritance diagram (in HTML, RTF and LaTeX) for classes with base
# or super classes. Setting the tag to NO turns the diagrams off. Note that
# this option also works with HAVE_DOT disabled, but it is recommended to
# install and use dot, since it yields more powerful graphs.
```

- **Note** in `libqrencode/Doxyfile`:1676 — # or class method. Note that enabling this option will significantly increase
```
# If the CALL_GRAPH and HAVE_DOT options are set to YES then
# doxygen will generate a call dependency graph for every global function
# or class method. Note that enabling this option will significantly increase
# the time of a run. So in most cases it will be better to enable call graphs
# for selected functions only using the \callgraph command.
```

- **Note** in `libqrencode/Doxyfile`:1684 — # or class method. Note that enabling this option will significantly increase
```
# If the CALLER_GRAPH and HAVE_DOT tags are set to YES then
# doxygen will generate a caller dependency graph for every global function
# or class method. Note that enabling this option will significantly increase
# the time of a run. So in most cases it will be better to enable caller
# graphs for selected functions only using the \callergraph command.
```

- **Note** in `libqrencode/Doxyfile`:1739 — # visualized by representing a node as a red box. Note that doxygen if the
```
# nodes that will be shown in the graph. If the number of nodes in a graph
# becomes larger than this value, doxygen will truncate the graph, which is
# visualized by representing a node as a red box. Note that doxygen if the
# number of direct children of the root node in a graph is already larger than
# DOT_GRAPH_MAX_NODES then the graph will not be shown at all. Also note
```

- **note** in `libqrencode/Doxyfile`:1741 — # DOT_GRAPH_MAX_NODES then the graph will not be shown at all. Also note
```
# visualized by representing a node as a red box. Note that doxygen if the
# number of direct children of the root node in a graph is already larger than
# DOT_GRAPH_MAX_NODES then the graph will not be shown at all. Also note
# that the size of a graph can be further restricted by MAX_DOT_GRAPH_DEPTH.

```

- **NOTE** in `.github/actions/setup-dependencies/windows/action.yml`:20 — # NOTE: Installed on MinGW as well for SignTool
```

  steps:
    # NOTE: Installed on MinGW as well for SignTool
    - name: Enter VS Developer shell
      if: ${{ runner.os == 'Windows' }}
```

- **TODO** in `meta/model/mojang.py`:289 — logging: Optional[Dict[str, MojangLogging]]  # TODO improve this?
```
    type: Optional[str]
    inherits_from: Optional[str] = Field("inheritsFrom")
    logging: Optional[Dict[str, MojangLogging]]  # TODO improve this?
    compliance_level: Optional[int] = Field(alias="complianceLevel")
    javaVersion: Optional[JavaVersion]
```

- **NOTE** in `meta/run/update_quilt.py`:67 — # NOTE: Quilt Meta does not make any guarantees about Last-Modified.
```

def compute_jar_file(path, url):
    # NOTE: Quilt Meta does not make any guarantees about Last-Modified.
    # Always download the JAR file instead
    jar_path = path + ".jar"
```

- **note** in `extra-cmake-modules/find-modules/FindWayland.cmake`:29 — A list of all targets imported by this module (note that there may be more
```
    Found Wayland version
``Wayland_TARGETS``
    A list of all targets imported by this module (note that there may be more
    than the components that were requested)
``Wayland_LIBRARIES``
```

- **NOTE** in `extra-cmake-modules/find-modules/FindFFmpeg.cmake`:69 — The components version (NOTE: always empty on Windows)
```

``<component>_VERSION``
    The components version (NOTE: always empty on Windows)

As the versions of the various FFmpeg components differ for a given release,
```

- **NOTE** in `extra-cmake-modules/find-modules/FindFFmpeg.cmake`:81 — NOTE: version checking is skipped on Windows
```
  find_package(FFmpeg 55.27 COMPONENTS AVUTIL)

NOTE: version checking is skipped on Windows

Since 6.20.0.
```

- **note** in `extra-cmake-modules/find-modules/FindXCB.cmake`:38 — A list of all targets imported by this module (note that there may be more
```
    Found xcb version
``XCB_TARGETS``
    A list of all targets imported by this module (note that there may be more
    than the components that were requested)
``XCB_LIBRARIES``
```

- **note** in `extra-cmake-modules/find-modules/Find7Zip.cmake`:29 — .. note::
```
    Path to 7-Zip executable

.. note::
    It will see to only find the original 7-Zip, not one of the  p7zip forks.

```

- **note** in `extra-cmake-modules/find-modules/FindPoppler.cmake`:28 — A list of all targets imported by this module (note that there may be more
```
    Found Poppler version
``Poppler_TARGETS``
    A list of all targets imported by this module (note that there may be more
    than the components that were requested)
``Poppler_LIBRARIES``
```

- **note** in `extra-cmake-modules/find-modules/Find7z.cmake`:29 — .. note::
```
    Path to 7z executable

.. note::
    Only works on Windows.

```

- **Note** in `extra-cmake-modules/find-modules/FindSharedMimeInfo.cmake`:79 — # Note that targets and most variables are not available to install code
```
    endif()

    # Note that targets and most variables are not available to install code
    install(CODE "
set(DESTDIR_VALUE \"\$ENV{DESTDIR}\")
```

- **NOTE** in `extra-cmake-modules/find-modules/FindSasl2.cmake`:39 — # NOTE: libsasl2.pc doesn't export the include dir.
```
cmake_policy(VERSION 3.16)

# NOTE: libsasl2.pc doesn't export the include dir.
find_package(PkgConfig QUIET)
pkg_check_modules(PC_Sasl2 libsasl2)
```

- **note** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:22 — .. note::
```
specific target.

.. note::
  It is recommended to include this module with the ``NO_POLICY_SCOPE``
  flag, otherwise you may get spurious warnings with some versions of CMake.
```

- **note** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:333 — # using libc calls (note that this issue does not occur when using QFile).
```
    # By default (in glibc, at least), on 32bit platforms off_t is 32 bits,
    # which causes a SIGXFSZ when trying to manipulate files larger than 2Gb
    # using libc calls (note that this issue does not occur when using QFile).
    check_cxx_source_compiles("
#include <sys/types.h>
```

- **Note** in `extra-cmake-modules/kde-modules/KDECompilerSettings.cmake`:516 — # Note that NOTFOUND evaluates to False, so we do enter the if.
```
            # NOTFOUND, which breaks build if we concatenate anything to
            # the "NOTFOUND" string.
            # Note that NOTFOUND evaluates to False, so we do enter the if.
            set(flags "")
        endif()
```

- **note** in `extra-cmake-modules/kde-modules/KDEFrameworkCompilerSettings.cmake`:26 — .. note::
```
Qt-defined keywords like signals and slots will not be defined.

.. note::
  It is recommended to include this module with the NO_POLICY_SCOPE
  flag, otherwise you may get spurious warnings with some versions of CMake.
```

- **TODO** in `extra-cmake-modules/kde-modules/KDECMakeSettings.cmake`:233 — # TODO KF6: discuss enabling AUTOUIC and note porting requirements. autouic
```
   # Enable autorcc and in cmake so qrc files get generated.
   # Since CMake 3.0
   # TODO KF6: discuss enabling AUTOUIC and note porting requirements. autouic
   #   acts on all #include "ui_*.h" assuming *.ui exists
   set(CMAKE_AUTORCC ON)
```

- **Note** in `extra-cmake-modules/kde-modules/KDEInstallDirsCommon.cmake`:104 — # Note that we cannot use arg vars directly in if() because macro args are
```

    # Surprisingly complex logic to deal with joining paths.
    # Note that we cannot use arg vars directly in if() because macro args are
    # not proper variables.
    set(_parent "${parent}")
```

- **Note** in `extra-cmake-modules/kde-modules/KDEInstallDirsCommon.cmake`:166 — # Note: If KDE_INSTALL_DIRS_NO_CMAKE_VARIABLES is true, _cmakename will be empty due to
```
        else()
            # CMake name is one of those defined by GNUInstallDirs.
            # Note: If KDE_INSTALL_DIRS_NO_CMAKE_VARIABLES is true, _cmakename will be empty due to
            # the logic on top of this macro, hence you will never end up here in this case

```

- **TODO** in `extra-cmake-modules/kde-modules/KDEInstallDirs6.cmake`:277 — # TODO MOVE TO KXMLGUI
```
_define_relative(KNOTIFYRCDIR DATAROOTDIR "knotifications6"
    "knotify description files")
# TODO MOVE TO KXMLGUI
_define_relative(KXMLGUIDIR DATAROOTDIR "kxmlgui5"
    "kxmlgui .rc files")
```

- **Note** in `extra-cmake-modules/modules/ECMGenerateExportHeader.cmake`:108 — final inclusion guard closing. Note that before 5.98 this was broken and
```
``CUSTOM_CONTENT_FROM_VARIABLE`` specifies the name of a variable whose
content will be appended at the end of the generated file, before any
final inclusion guard closing. Note that before 5.98 this was broken and
would only append the string passed as argument value.

```

- **note** in `extra-cmake-modules/modules/ECMQtDeclareLoggingCategory.cmake`:191 — # note data in global properties
```
    endif()

    # note data in global properties
    set(_propertyprefix "ECM_QT_LOGGING_CATEGORY_${ARG_EXPORT}")
    set_property(GLOBAL APPEND PROPERTY "${_propertyprefix}_CATEGORIES" ${ARG_CATEGORY_NAME})
```

- **note** in `extra-cmake-modules/modules/ECMQtDeclareLoggingCategory.cmake`:288 — # note data in global properties
```
    endif()

    # note data in global properties
    if (ARG_EXPORT)
        set(_default_severity)
```

- **Note** in `extra-cmake-modules/modules/ECMSetupVersion.cmake`:64 — next 8 bits and ``<major>`` in the remaining bits.  Note that ``<patch>`` and
```

``<prefix>_VERSION`` has ``<patch>`` in the bottom 8 bits, ``<minor>`` in the
next 8 bits and ``<major>`` in the remaining bits.  Note that ``<patch>`` and
``<minor>`` must be less than 256.

```

- **TODO** in `extra-cmake-modules/modules/ECMAddAndroidApk.cmake`:17 — # TODO extra args?
```
      [ANDROID_DIR <dir>]
      [PACKAGE_NAME <name>]
      # TODO extra args?
  )

```

- **TODO** in `extra-cmake-modules/modules/check-outbound-license.py`:118 — # TODO check if required arguments are present and give meaningful feedback
```
    args = parser.parse_args()

    # TODO check if required arguments are present and give meaningful feedback

    # collect name and licenses from SPDX blocks
```

- **Note** in `extra-cmake-modules/modules/ECMQmlModule.cmake`:204 — KDE_INSTALL_QMLDIR will be used. Note that if ``NO_PLUGIN`` is set, a C++
```
  module. The built static library will be installed in a subdirectory of
  ``DESTINATION`` based on the QML module's URI. If this value is not set,
  KDE_INSTALL_QMLDIR will be used. Note that if ``NO_PLUGIN`` is set, a C++
  QML plugin will be generated to include the QRC files.
- If ``BUILD_SHARED_LIBS`` is on, all generated files, QML sources and the C++
```

- **note** in `extra-cmake-modules/modules/ECMFindModuleHelpers.cmake`:92 — has this variable set (note that components are searched for in the order
```
``<name>_VERSION``
  the value of ``<name>_<component>_VERSION`` for the first component that
  has this variable set (note that components are searched for in the order
  they are passed to the macro), although if it is already set, it will not
  be altered
```

- **note** in `extra-cmake-modules/modules/ECMFindModuleHelpers.cmake`:96 — .. note::
```
  be altered

.. note::
  These variables are never cleared, so if
  ``ecm_find_package_handle_library_components()`` is called multiple times with
```

- **note** in `extra-cmake-modules/modules/ECMGeneratePkgConfigFile.cmake`:70 — .. note::
```
cache variable is set to something different.

.. note::
  The first call to ``ecm_generate_pkgconfig_file()`` with the ``INSTALL``
  argument will cause ``ECM_PKGCONFIG_INSTALL_DIR`` to be set to the cache,
```

- **Note** in `extra-cmake-modules/modules/ECMAddQtDesignerPlugin.cmake`:135 — arguments with the default of ``CREATE_WIDGET_CODE_FROM_VARIABLE``. Note
```

``CONSTRUCTOR_ARGS_CODE`` specifies the C++ code to use for the constructor
arguments with the default of ``CREATE_WIDGET_CODE_FROM_VARIABLE``. Note
that the parentheses are required. The default is "(parent)".

```

- **note** in `extra-cmake-modules/toolchain/Android.cmake`:24 — .. note::
```
https://cmake.org/cmake/help/v3.7/manual/cmake-toolchains.7.html#cross-compiling-for-android

.. note::

  This module requires CMake 3.18.
```

- **NOTE** in `ptinstaller/Include/UpgradeDLL.nsh`:3 — NOTE:
```
/*

NOTE:
-----
This macro is provided for backwards compatibility with PTIS 2.0 scripts.
```

- **NOTE** in `ptinstaller/Include/Util.nsh`:113 — StrCmp $_LOGICLIB_TEMP 0 ret ; NOTE: Must use StrCmp, IntCmp fails on "0x8000000000000001 Z> 1"
```
System::Int64Op $1 ^ $2 ; Using xor so $_LOGICLIB_TEMP ends up as 0 when they are equal
Pop $_LOGICLIB_TEMP
StrCmp $_LOGICLIB_TEMP 0 ret ; NOTE: Must use StrCmp, IntCmp fails on "0x8000000000000001 Z> 1"
System::Int64Op $1 < 0
Pop $_LOGICLIB_TEMP
```

- **Note** in `ptinstaller/Source/util.cpp`:343 — // Note: Behavior depends on the LC_CTYPE category of the current locale.
```
int _wcsnicmp(const wchar_t *a, const wchar_t *b, size_t n)
{
  // Note: Behavior depends on the LC_CTYPE category of the current locale.
#if _XOPEN_SOURCE >= 700 || _POSIX_C_SOURCE >= 200809L
  return wcsncasecmp(a, b, n);
```

- **NOTE** in `ptinstaller/Source/util.cpp`:439 — // NOTE: Only the first drive in the path will be converted to posix style (c:\foo d:\bar > /c/foo d:/bar)
```
{
  if (!wcmd) return system(NULL);
  // NOTE: Only the first drive in the path will be converted to posix style (c:\foo d:\bar > /c/foo d:/bar)
  const char *cmd = PTISRT_wctombpath(wcmd);
  if (!cmd) return -1;
```

- **Note** in `ptinstaller/Source/util.cpp`:915 — // Note: Not using _tgetenv(_T("_")) here because it is not always set correctly. !makeptis instruction in SVN r7372 will execute "/usr/bin/scons"!
```
basic:
      free(path);
      // Note: Not using _tgetenv(_T("_")) here because it is not always set correctly. !makeptis instruction in SVN r7372 will execute "/usr/bin/scons"!
      return get_full_path(argv0);
    }
```

- **Note** in `ptinstaller/Source/util.cpp`:959 — // Note: Unlike PathGetDriveNumber(), we require a path separator after the colon.
```
static int PathGetDosDriveNumber(const TCHAR *p)
{
  // Note: Unlike PathGetDriveNumber(), we require a path separator after the colon.
  if (p[0] && _T(':') == p[1] && IsAgnosticPathSeparator(p[2]))
  {
```

- **Note** in `ptinstaller/Source/script.cpp`:74 — static const struct { const TCHAR*Name; UINT Id; } map[] = { // Note: This list is incomplete.
```
static bool LookupWinSysColorId(const TCHAR*Str, UINT&Clr)
{
  static const struct { const TCHAR*Name; UINT Id; } map[] = { // Note: This list is incomplete.
    { _T("WINDOW"), 5 }, { _T("WINDOWTEXT"), 8 },
    { _T("3DFACE"), 15 }, { _T("BTNTEXT"), 18 }, // "Three-dimensional display elements and dialog box"
```

- **note** in `ptinstaller/Source/script.cpp`:482 — // note that if VAR does not exist, ${VAR} or $%VAR% will go through unmodified
```
  // convert $\r, $\n to their literals
  // preprocessor replace ${VAR} and $%VAR% with whatever value
  // note that if VAR does not exist, ${VAR} or $%VAR% will go through unmodified
  const TCHAR *in=str;
  while (*in)
```

- **Note** in `ptinstaller/Source/script.cpp`:1585 — build_header.license_bg=-COLOR_BTNFACE; /* Note: This might not actually be gray */
```
        else if (!_tcsicmp(p,paramname = _T("/grey")) || !_tcsicmp(p,_T("/gray")))
        {
          build_header.license_bg=-COLOR_BTNFACE; /* Note: This might not actually be gray */
          SCRIPT_MSG(_T("%") NPRIs _T(": %") NPRIs _T("\n"),cmdname,paramname);
        }
```

- **NOTE** in `ptinstaller/Source/winchar.cpp`:111 — // NOTE: Anything outside the ASCII range will not convert correctly!
```
  return (WINWCHAR*) cnv.Detach();
#else
  // NOTE: Anything outside the ASCII range will not convert correctly!
  size_t cch = wcslen(s);
  WINWCHAR* p = (WINWCHAR*) malloc(++cch * 2);
```

- **NOTE** in `ptinstaller/Source/mmap.cpp`:320 — // NOTE: mmap() requires the file descriptor to stay open for get(),
```
  {
    if (!setfile(hFile, size)) size = 0;
    // NOTE: mmap() requires the file descriptor to stay open for get(), 
    // otherwise get() will fail with errno code EBADFD (bad file descriptor).
    m_hFile = hFile, m_bTempHandle = TRUE;
```

- **NOTE** in `ptinstaller/Source/makenssi.cpp`:369 — // NOTE: Unlike the fork, we print \r\n and not just \n.
```
    {
      // Emulate the scratchpaper.com fork and its /RAW switch.
      // NOTE: Unlike the fork, we print \r\n and not just \n.
      outputbom=0;
      outputenc.SetCodepage(NStreamEncoding::UTF16LE);
```

- **TODO** in `ptinstaller/Source/util.h`:190 — #define _vsntprintf Error: TODO
```
// We don't hook fflush since the native handle is only used with WriteConsoleW
#undef _vsntprintf
#define _vsntprintf Error: TODO
#undef _tprintf
#define _tprintf WinStdIO_wprintf
```

- **note** in `ptinstaller/Source/Plugins.cpp`:106 — files_itr++) // note: files are listed alphabetically, so plugin.dll will be listed before pluginW.dll
```
  for (dir_reader::iterator files_itr = dr->files().begin();
       files_itr != dr->files().end();
       files_itr++) // note: files are listed alphabetically, so plugin.dll will be listed before pluginW.dll
  {
    if (!dir_reader::matches(*files_itr, _T("*.dll")))
```

- **Note** in `ptinstaller/Source/strlist.h`:168 — * Jim Park: Note that SortedStringList actually <b>owns</b> T.name.
```
  public:
    /**
     * Jim Park: Note that SortedStringList actually <b>owns</b> T.name.
     * Yes, this violates all kinds of encapsulation ideas.
     */
```

- **Note** in `ptinstaller/Source/lang.cpp`:340 — // Note: The functions below refer to the methods related to Languages.
```
//                           class CEXEBuild
//
// Note: The functions below refer to the methods related to Languages.
//////////////////////////////////////////////////////////////////////////////

```

- **Note** in `ptinstaller/Source/build.cpp`:553 — if (NS_IS_CODE(i)) // Note: this includes '\0'
```
    {
        _TUCHAR i = (_TUCHAR)*p++;
        if (NS_IS_CODE(i)) // Note: this includes '\0'
        {
            // convert all character up to, and including this code
```

- **Note** in `ptinstaller/Source/build.cpp`:1502 — ERROR_MSG(_T("Note: uninstall functions must begin with \"un.\", and install functions must not\n"));
```
  }
  ERROR_MSG(_T("Error: resolving %") NPRIs _T(" function \"%") NPRIs _T("\" in %") NPRIs _T("\n"),str,(TCHAR*)ns_func.get()+fptr,fn);
  ERROR_MSG(_T("Note: uninstall functions must begin with \"un.\", and install functions must not\n"));
  return 1;
}
```

- **NOTE** in `ptinstaller/Source/tokens.cpp`:390 — // NOTE: This assumes that all TOK_P_* in tokens.h are grouped together.
```
bool CEXEBuild::is_pp_token(int tkid)
{
  // NOTE: This assumes that all TOK_P_* in tokens.h are grouped together.
  return (tkid >= TOK_P_IF && tkid <= TOK_P_SEARCHREPLACESTRING);
}
```

- **Note** in `ptinstaller/Examples/AppGen.nsi`:2 — Note: This is not a installer example script, it's a script for a simple application used by some of the other examples.
```
/*
Note: This is not a installer example script, it's a script for a simple application used by some of the other examples.
*/

```

- **TODO** in `ptinstaller/Scripts/release.py`:55 — TODO
```
=========================

TODO
~~~~

```

- **ToDo** in `ptinstaller/Scripts/release.py`:468 — def ToDo():
```
	os.system('start ' + PURGE_URL % 'Download')

def ToDo():
	print('automatic phase done\n')
	print("""
```

- **Note** in `ptinstaller/Contrib/nsExec/nsexec.c`:443 — bufCh[0] = ((CHAR*)pSrc)[0], cchDstChar = 1; // Note: OEM codepage will be converted by LogMessage
```
          }
          else { // DBCS --> DBCS:
            bufCh[0] = ((CHAR*)pSrc)[0], cchDstChar = 1; // Note: OEM codepage will be converted by LogMessage
          }
        }
```

- **Note** in `ptinstaller/Contrib/Splash/splash.txt`:29 — Note that the first parameter to splash.exe is the length to show the
```
FunctionEnd

Note that the first parameter to splash.exe is the length to show the
screen for (in milliseconds), and the second is the splash bitmap filename (without
the .bmp). The BMP file used will be this parameter.bmp, and the wave file used
```

- **Note** in `ptinstaller/Contrib/Splash/splash.txt`:36 — Note: the return value of splash is 1 if the user closed the splash
```
(If you already have an .onInit function, put that in it)

Note: the return value of splash is 1 if the user closed the splash 
screen early (pop it from the stack)

```

- **Note** in `ptinstaller/Contrib/Makeptisw/utils.h`:116 — template<class T> static T* FindLastComponent(T*p) // Note: Returns "" for "dir\"
```
struct FSPath {
  template<class T> static inline bool IsAgnosticSeparator(const T c) { return '\\' == c || '/' == c; }
  template<class T> static T* FindLastComponent(T*p) // Note: Returns "" for "dir\"
  {
    for (T *sep = 0, *start = p;; ++p)
```

- **Note** in `ptinstaller/Contrib/Makeptisw/utils.cpp`:58 — wsprintf(buf + cch, _T("\\%hs.dll"), Mod); // Note: We always append ".dll"
```
  UINT dirmax = MAX_PATH, cch;
  if ((cch = GetSystemDirectory(buf, dirmax)) >= dirmax) cch = 0;
  wsprintf(buf + cch, _T("\\%hs.dll"), Mod); // Note: We always append ".dll"
  path = buf + !cch; // Full path or just the filename
  return LoadLibrary(path);
```

- **Note** in `ptinstaller/Contrib/Makeptisw/update.cpp`:72 — // Note: InternetOpenUrlW does not handle the http headers in the same way, be careful if you stop forcing the A version
```
    InetWorkOnline(hInet);

    // Note: InternetOpenUrlW does not handle the http headers in the same way, be careful if you stop forcing the A version
    static const CHAR httpheaders[] = "Accept:*/*\r\n";
    DWORD connflags = INTERNET_FLAG_NO_UI|INTERNET_FLAG_IGNORE_REDIRECT_TO_HTTPS;
```

- **Note** in `ptinstaller/Contrib/Modern UI 2/Pages.nsh`:235 — System::Call 'USER32::MapWindowPoints(p0,p$hwndparent,pr1,i2)' ; Note: Assuming control is not in inner dialog
```
  System::Store "S"
  System::Call 'USER32::GetWindowRect(psr0,@r1)'
  System::Call 'USER32::MapWindowPoints(p0,p$hwndparent,pr1,i2)' ; Note: Assuming control is not in inner dialog
  System::Call '*$1(i.r5,i.r6,i.r7,i.r8)'
  IntOp $7 $7 - $5
```

- **Note** in `ptinstaller/Contrib/Modern UI 2/Pages.nsh`:248 — System::Call 'GDI32::DeleteObject(pr1)' ; Note: Assuming the previous image (if any) was a bitmap
```
  SendMessage $0 ${STM_SETIMAGE} ${IMAGE_BITMAP} $2 $1
  Push $2 ; Return value
  System::Call 'GDI32::DeleteObject(pr1)' ; Note: Assuming the previous image (if any) was a bitmap
  System::Call 'USER32::GetClientRect(pr0,@r1)'
  System::Call '*$1(i,i,i.r1,i.r2)'
```

- **Note** in `ptinstaller/Contrib/Modern UI 2/Pages.nsh`:269 — System::Call 'USER32::MapWindowPoints(p0,p$hwndparent,pr1,i2)' ; Note: Assuming control is not in inner dialog
```
  System::Store "S"
  System::Call 'USER32::GetWindowRect(psr0,@r1)'
  System::Call 'USER32::MapWindowPoints(p0,p$hwndparent,pr1,i2)' ; Note: Assuming control is not in inner dialog
  System::Call '*$1(i.r5,i.r6,i.r7,i.r8)'
  IntOp $7 $7 - $5
```

- **Note** in `ptinstaller/Contrib/Modern UI 2/Pages.nsh`:282 — System::Call 'GDI32::DeleteObject(pr1)' ; Note: Assuming the previous image (if any) was a bitmap
```
  SendMessage $0 ${STM_SETIMAGE} ${IMAGE_BITMAP} $2 $1
  Push $2 ; Return value
  System::Call 'GDI32::DeleteObject(pr1)' ; Note: Assuming the previous image (if any) was a bitmap
  System::Call 'USER32::GetClientRect(pr0,@r1)'
  System::Call '*$1(i,i,i.r1,i.r2)'
```

- **Note** in `ptinstaller/Contrib/AdvSplash/advsplash.txt`:46 — Note 2: the return value of splash is 1 if the user closed the splash
```
for images with transparent color no fading effect will occur.

Note 2: the return value of splash is 1 if the user closed the splash 
screen early (pop it from the stack)

```

- **Note** in `ptinstaller/Contrib/InstallOptions/Readme.html`:296 — <strong>Note:</strong> For labels, \r\n will be converted to a newline. To use a
```
                                    this specifies the path to the image.<br />
                                    <br />
                                    <strong>Note:</strong> For labels, \r\n will be converted to a newline. To use a
                                    back-slash in your text you have to escape it using another back-slash - \\. Described
                                    <a href="#escaping">below</a> are PTIS functions for converting text to/from this
```

- **Note** in `ptinstaller/Contrib/InstallOptions/Readme.html`:314 — <strong>Note:</strong> For Text fields with the MULTILINE flag, \r\n will be converted
```
                                    can specify something to be executed or opened (using ShellExecute).<br />
                                    <br />
                                    <strong>Note:</strong> For Text fields with the MULTILINE flag, \r\n will be converted
                                    to a newline. To use a back-slash in your text you have to escape it using another
                                    back-slash - \\. Described <a href="#escaping">below</a> are PTIS functions for
```

- **Note** in `ptinstaller/Contrib/InstallOptions/Readme.html`:383 — <strong>Note:</strong> You can specify negative coordinates to specify the distance
```
                                    using a resource editor and copy the dimensions to the INI file.<br />
                                    <br />
                                    <strong>Note:</strong> You can specify negative coordinates to specify the distance
                                    from the right or bottom edge.<br />
                                    <br />
```

- **Note** in `ptinstaller/Contrib/InstallOptions/Readme.html`:386 — <strong>Note (2):</strong> For combobox or droplist, the &quot;<em>bottom</em>&quot;
```
                                    from the right or bottom edge.<br />
                                    <br />
                                    <strong>Note (2):</strong> For combobox or droplist, the &quot;<em>bottom</em>&quot;
                                    value is not used in the same way.<br />
                                    In this case, the bottom value is the maximum size of the window when the pop-up
```

- **Note** in `ptinstaller/Contrib/InstallOptions/Readme.html`:394 — <strong>Note (3):</strong> FileRequest and DirRequest controls will allocate 15
```
                                    height required is the number of items in the list multiplied by 8, plus 20.<br />
                                    <br />
                                    <strong>Note (3):</strong> FileRequest and DirRequest controls will allocate 15
                                    dialog units to the browse button. Make this control wide enough the contents of
                                    the textbox can be seen.</td>
```

- **Note** in `ptinstaller/Contrib/UserInfo/UserInfo.nsi`:25 — StrCmp $1 "Admin" 0 +3 ; Note: Win9x always returns "Admin"
```
	UserInfo::GetOriginalAccountType
	Pop $2
	StrCmp $1 "Admin" 0 +3 ; Note: Win9x always returns "Admin"
		MessageBox MB_OK 'User "$0" is in the Administrators group${REALMSG}'
		Goto done
```

- **NOTE** in `ptinstaller/Contrib/Modern UI/Changelog.txt`:235 — (NOTE: A new macro for the language selection dialog has been introduced in version 1.63)
```
1.15 - Semtember 4, 2002
* Multilanguage example: changed LangDialog to LangDLL::LangDialog (using the DLL name is now required)
  (NOTE: A new macro for the language selection dialog has been introduced in version 1.63)

1.14 - Semtember 3, 2002
```

- **NOTE** in `ptinstaller/Contrib/Modern UI/Changelog.txt`:252 — (NOTE: All settings mentioned here do not exist anymore)
```

1.1 - August 29, 2002
  (NOTE: All settings mentioned here do not exist anymore)

* Header file with macros, it's now very easy to use the UI in your scripts
```

- **Note** in `ptinstaller/Contrib/Language files/Slovenian.nsh`:4 — # We use "slovenski", the other alternative is "slovenščina" (Note: "slovenčina" is the native name of Slovak)
```
;By Janez Dolinar, edited by Martin Srebotnjak - Lugos.si

# We use "slovenski", the other alternative is "slovenščina" (Note: "slovenčina" is the native name of Slovak)
!insertmacro LANGFILE "Slovenian" = "Slovenski" =

```

- **Note** in `ptinstaller/Contrib/PTISdl/httpget.cpp`:39 — // Note: The authority section is not skipped here because it is assumed to not exist without a scheme!
```
  if (len) return GLT_ABS; // "xyz:..."
  s+=len;
  // Note: The authority section is not skipped here because it is assumed to not exist without a scheme!
  if (s[0] != '/') return GLT_PATHREL; // "xyz..." or "./xyz..." (or "" but don't rely on this)
  if (s[1] == '/') return GLT_NETREL; // "//..." (relative "network-path reference")
```

- **Note** in `ptinstaller/Contrib/PTIS Menu/wx/setup.h`:555 — // XML parsing classes. Note that their API will change in the future, so
```
#define wxUSE_XRC       0

// XML parsing classes. Note that their API will change in the future, so
// using wxXmlDocument and wxXmlNode in your app is not recommended.
//
```

- **NOTE** in `ptinstaller/Contrib/System/Source/Plugin.h`:42 — extern TCHAR *system_getuservariable(int varnum); // NOTE: This dupes with GlobalAlloc!
```
extern TCHAR *AllocString();
#define system_getuservariableptr(varnum) ( (g_variables)+(varnum)*(g_stringsize) )
extern TCHAR *system_getuservariable(int varnum); // NOTE: This dupes with GlobalAlloc!
extern TCHAR *system_setuservariable(int varnum, TCHAR *var);
extern TCHAR* system_popstring();  // NULL - stack empty
```

- **Note** in `ptinstaller/Contrib/System/Source/System.c`:866 — proc->Params[ParamIndex].Output = (int) temp4; // Note: As long as we never assign a pointer to temp4 when parsing a destination the cast to int is OK.
```
                }
                if (temp3 == 1)
                    proc->Params[ParamIndex].Output = (int) temp4; // Note: As long as we never assign a pointer to temp4 when parsing a destination the cast to int is OK.
                // Next parameter is output or something else
                temp3++;
```

- **note** in `ptinstaller/Contrib/System/Source/System.c`:1188 — lstrcpyn(realbuf,*((TCHAR**) place), g_stringsize); // note: lstrcpyn always includes a null terminator (unlike strncpy)
```
            realbuf[g_stringsize-1] = _T('\0'); // make sure we have a null terminator
#else
            lstrcpyn(realbuf,*((TCHAR**) place), g_stringsize); // note: lstrcpyn always includes a null terminator (unlike strncpy)
#endif
            break;
```

- **note** in `ptinstaller/Contrib/System/Source/System.c`:1205 — lstrcpyn(realbuf, wstr, g_stringsize); // note: lstrcpyn always include a null terminator (unlike strncpy)
```
            wstr = *((LPWSTR*)place);
#ifdef _UNICODE
            lstrcpyn(realbuf, wstr, g_stringsize); // note: lstrcpyn always include a null terminator (unlike strncpy)
#else
            WideCharToMultiByte(CP_ACP, 0, wstr, g_stringsize, realbuf, g_stringsize-1, NULL, NULL);
```

- **Note** in `ptinstaller/Docs/src/headers.but`:88 — \\<b\\>Note:\\</b\\>
```
\c FunctionEnd

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if disk or directory isn't exist
\\<br\\>- Error flag if syntax error
```

- **Note** in `ptinstaller/Docs/src/headers.but`:333 — \\<b\\>Note:\\</b\\>
```
\c $var3         ; Result3: Sum of directories

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if disk or directory isn't exist
\\<br\\>- Error flag if syntax error
```

- **Note** in `ptinstaller/Docs/src/headers.but`:396 — \\<b\\>Note:\\</b\\>
```
\c $var          ; Result: Size

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if disk isn't exist or not ready
\\<br\\>- Error flag if syntax error
```

- **Note** in `ptinstaller/Docs/src/headers.but`:511 — \\<b\\>Note:\\</b\\>
```
\c $var7           ; Result7: seconds

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if file isn't exist
\\<br\\>- Error flag if syntax error
```

- **Note** in `ptinstaller/Docs/src/headers.but`:599 — \\<b\\>Note:\\</b\\>
```
\c                   ;    $var=0   file has no specified attributes

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag is set if file doesn't exist

```

- **Note** in `ptinstaller/Docs/src/headers.but`:686 — \\<b\\>Note:\\</b\\>
```
\c $var               ; Result: option string

\\<b\\>Note:\\</b\\>
\\<br\\>- The error flag is set if the option is not found
\\<br\\>- The first character in the option string is treated as a parameter delimiter
```

- **Note** in `ptinstaller/Docs/src/headers.but`:979 — \\<b\\>Note:\\</b\\>
```
\c FunctionEnd

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if input file doesn't exist
\\<br\\>- Error flag if output file path doesn't exist
```

- **Note** in `ptinstaller/Docs/src/headers.but`:1152 — \\<b\\>Note:\\</b\\>
```
\c $var             ; Result: Line

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if input file doesn't exist
\\<br\\>- Error flag if line number not found
```

- **Note** in `ptinstaller/Docs/src/headers.but`:1185 — \\<b\\>Note:\\</b\\>
```
\c FunctionEnd

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if input file doesn't exist

```

- **Note** in `ptinstaller/Docs/src/headers.but`:1240 — \\<b\\>Note:\\</b\\>
```
\c $var          ; Result: Sum of lines

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if input file doesn't exist

```

- **Note** in `ptinstaller/Docs/src/headers.but`:1313 — \\<b\\>Note:\\</b\\>
```
\c FunctionEnd

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if File1 or File2 doesn't exist
\\<br\\>- Error flag if syntax error
```

- **Note** in `ptinstaller/Docs/src/headers.but`:1473 — \\<b\\>Note:\\</b\\>
```
\c $var          ; Result:  Value

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if entry not found
\\<br\\>- Error flag if file doesn't exist
```

- **Note** in `ptinstaller/Docs/src/headers.but`:1516 — \\<b\\>Note:\\</b\\>
```
\c               ;    $var=SAME     Entry and Value already exist

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if file doesn't exist
\\<br\\>- Error flag if file can't be opened
```

- **Note** in `ptinstaller/Docs/src/headers.but`:1558 — \\<b\\>Note:\\</b\\>
```
\c                 ; CharToOem   -from Windows to DOS

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if file doesn't exist
\\<br\\>- Error flag if syntax error
```

- **Note** in `ptinstaller/Docs/src/headers.but`:1669 — \\<b\\>Note:\\</b\\>
```
\c $var               ;output (result)

\\<b\\>Note:\\</b\\>
\\<br\\>- Accepted numbers 1,01,001,...

```

- **Note** in `ptinstaller/Docs/src/headers.but`:2350 — \\<b\\>Note:\\</b\\>
```
\c $var             ;output (result)

\\<b\\>Note:\\</b\\>
\\<br\\>- Error flag if syntax error
\\<br\\>- Same symbol to include & to exclude = to exclude
```

- **Note** in `ptinstaller/Docs/src/headers.but`:2502 — \\<b\\>Note:\\</b\\>
```
\c $var                ; Result: converted version

\\<b\\>Note:\\</b\\>
\\<br\\>- Converted letters are separated with dot
\\<br\\>- If character is non-digit and not in list then it will be converted to dot
```

- **Note** in `ptinstaller/Docs/src/usefulinfos.but`:15 — Note that uninstallers copy themselves to the temporary directory and execute from there so the original uninstaller can be deleted. This means the error level the uninstaller sets is not available to the executing process, unless it simulates this copy process and executes the copied uninstaller. To simulate this process, use:
```
You can set the error level to other values using \R{seterrorlevel}{SetErrorLevel}.

Note that uninstallers copy themselves to the temporary directory and execute from there so the original uninstaller can be deleted. This means the error level the uninstaller sets is not available to the executing process, unless it simulates this copy process and executes the copied uninstaller. To simulate this process, use:

\c InitPluginsDir
```

- **Note** in `ptinstaller/Docs/src/attributes.but`:5 — The commands below all adjust attributes of the installer. These attributes control how the installer looks and functions, including which pages are present in the installer, which text is displayed in each part of each page, the name of the installer, the icon the installer uses, the default installation directory and more. Note that these attributes can be set anywhere in the file except in a \R{ssection}{Section} or \R{ffunction}{Function}.
```
\S1{attribgen} General Attributes

The commands below all adjust attributes of the installer. These attributes control how the installer looks and functions, including which pages are present in the installer, which text is displayed in each part of each page, the name of the installer, the icon the installer uses, the default installation directory and more. Note that these attributes can be set anywhere in the file except in a \R{ssection}{Section} or \R{ffunction}{Function}.

\\<b\\>\\<u\\>Defaults are bold and underlined\\</u\\>\\</b\\>
```

- **Note** in `ptinstaller/Docs/src/attributes.but`:206 — Sets the colors to use for the install info screen (the default is 00FF00 000000. Use the form RRGGBB (in hexadecimal, as in HTML, only minus the leading '#', since # can be used for comments). Note that if "/windows" is specified as the only parameter, the default windows colors will be used.
```
\c /windows | (foreground_color background_color)

Sets the colors to use for the install info screen (the default is 00FF00 000000. Use the form RRGGBB (in hexadecimal, as in HTML, only minus the leading '#', since # can be used for comments). Note that if "/windows" is specified as the only parameter, the default windows colors will be used.

\S2{ainstalldir} InstallDir
```

- **Note** in `ptinstaller/Docs/src/attributes.but`:212 — Sets the default installation directory. See the \R{variables}{variables section} for variables that can be used to make this string (especially $PROGRAMFILES). Note that the part of this string following the last \\ will be used if the user selects 'browse', and may be appended back on to the string at install time (to disable this, end the directory with a \\ (which will require the entire parameter to be enclosed with quotes). If this doesn't make any sense, play around with the browse button a bit.
```
\c definstdir

Sets the default installation directory. See the \R{variables}{variables section} for variables that can be used to make this string (especially $PROGRAMFILES). Note that the part of this string following the last \\ will be used if the user selects 'browse', and may be appended back on to the string at install time (to disable this, end the directory with a \\ (which will require the entire parameter to be enclosed with quotes). If this doesn't make any sense, play around with the browse button a bit.

\S2{ainstalldirregkey} InstallDirRegKey
```

- **Note** in `ptinstaller/Docs/src/attributes.but`:229 — Valid values for flag are "smooth" (smooth the progress bar) or "colored" (color the progress bar with the colors set by \R{ainstallcolors}{InstallColors}. Examples: "InstProgressFlags" (default old-school windows look), "InstProgressFlags smooth" (new smooth look), "InstProgressFlags smooth colored" (colored smooth look whee). Note: neither "smooth" or "colored" work with \R{axpstyle}{XPStyle} on when the installer runs on Windows XP with a modern theme.
```
\c [flag [...]]

Valid values for flag are "smooth" (smooth the progress bar) or "colored" (color the progress bar with the colors set by \R{ainstallcolors}{InstallColors}. Examples: "InstProgressFlags" (default old-school windows look), "InstProgressFlags smooth" (new smooth look), "InstProgressFlags smooth colored" (colored smooth look whee). Note: neither "smooth" or "colored" work with \R{axpstyle}{XPStyle} on when the installer runs on Windows XP with a modern theme.

\S2{ainsttype} InstType
```

- **Note** in `ptinstaller/Docs/src/var.but`:3 — All variables are global and can be used in Sections or Functions. Note that by default, variables are limited to 1024 characters. To extend this limit, \R{build}{build Project Tick Install System} with a bigger value of the PTIS_MAX_STRLEN build setting or use the \W{https://ptis.sourceforge.io/download/specialbuilds/}{special build}.
```
\H{variables} Variables

All variables are global and can be used in Sections or Functions. Note that by default, variables are limited to 1024 characters. To extend this limit, \R{build}{build Project Tick Install System} with a bigger value of the PTIS_MAX_STRLEN build setting or use the \W{https://ptis.sourceforge.io/download/specialbuilds/}{special build}.

\S1{varuser} User Variables
```

- **Note** in `ptinstaller/Docs/src/usefulfunc.but`:21 — \c     StrCpy $R0 $R0 1 ; Note: This truncates 5.50 to 5 etc.
```
\c   ReadRegStr $R0 HKLM "Software\Microsoft\Internet Explorer" "Version" ; IE v4..9
\c   IfErrors lbl_123
\c     StrCpy $R0 $R0 1 ; Note: This truncates 5.50 to 5 etc.
\c     Goto lbl_done
\c   lbl_123:
```

- **NOTE** in `ptinstaller/Docs/src/usefulfunc.but`:43 — \c ; NOTE: This is only able to detect .NET v1.x and v2.x!
```
\c ; IsDotNETInstalled
\c ;
\c ; NOTE: This is only able to detect .NET v1.x and v2.x!
\c ;
\c ; Based on GetDotNETVersion
```

- **Note** in `ptinstaller/Docs/src/file.but`:143 — Note that only the low byte of the integer is used, i.e. writing 256 is the same as writing 0, etc.
```
\c FileWriteByte file_handle "10"

Note that only the low byte of the integer is used, i.e. writing 256 is the same as writing 0, etc.

\S2{FileWriteWord} FileWriteWord
```

- **Note** in `ptinstaller/Docs/src/file.but`:156 — Note that only the low WORD of the integer is used, i.e. writing 65536 is the same as writing 0, etc.
```
\c FileWriteWord file_handle "10"

Note that only the low WORD of the integer is used, i.e. writing 65536 is the same as writing 0, etc.

\S2{FindClose} FindClose
```

- **Note** in `ptinstaller/Docs/src/file.but`:168 — Performs a search for 'filespec', placing the first file found in filename_output (a user variable). It also puts the handle of the search into handle_output (also a user variable). If no files are found, both outputs are set to empty and the error flag is set. \R{FindClose}{FindClose} must be used to close the handle. Note that the filename output is without path.
```
\c user_var(handle output) user_var(filename output) filespec

Performs a search for 'filespec', placing the first file found in filename_output (a user variable). It also puts the handle of the search into handle_output (also a user variable). If no files are found, both outputs are set to empty and the error flag is set. \R{FindClose}{FindClose} must be used to close the handle. Note that the filename output is without path.

\c FindFirst $0 $1 $INSTDIR\*.txt
```

- **Note** in `ptinstaller/Docs/src/config.but`:35 — \define{PtisBlockHeaderNote} \s{Note:}
```
\define{PtisBlockHeaderWarning} \s{Warning:}

\define{PtisBlockHeaderNote} \s{Note:}

\define{PtisBlockHeaderExeheadU} \s{Unicode:}
```

- **Note** in `ptinstaller/Docs/src/usection.but`:14 — Note that in uninstaller code, $INSTDIR contains the directory where the uninstaller lies. It does \s{not} necessarily contain the same value it contained in the installer.
```
The first \R{delete}{Delete} instruction works (deleting the uninstaller), because the uninstaller is transparently copied to the system temporary directory for the uninstall.

Note that in uninstaller code, $INSTDIR contains the directory where the uninstaller lies. It does \s{not} necessarily contain the same value it contained in the installer.
```

- **Note** in `ptinstaller/Docs/src/sec.but`:130 — Gets the size of the section specified by section_index and stores the value in the given user variable. Note that the index starts with zero. The error flag will be set if the section index is out of range.
```
\c section_index user_var

Gets the size of the section specified by section_index and stores the value in the given user variable. Note that the index starts with zero. The error flag will be set if the section index is out of range.

\c Section test test_section_id
```

- **Note** in `ptinstaller/Docs/src/generalpurpose.but`:7 — Calls a function named \e{function_name} inside a PTIS extension DLL, a plug-in. See the \L{../Examples/Plugin/}{example plugin} for how to make one. Extension DLLs can access the stack and variables. Note: To automatically extract and call plug-in DLLs, use a plug-in command instead of \R{callinstdll}{CallInstDLL}.
```
\c dllfile function_name

Calls a function named \e{function_name} inside a PTIS extension DLL, a plug-in. See the \L{../Examples/Plugin/}{example plugin} for how to make one. Extension DLLs can access the stack and variables. Note: To automatically extract and call plug-in DLLs, use a plug-in command instead of \R{callinstdll}{CallInstDLL}.

\c Push "a parameter"
```

- **Note** in `ptinstaller/Docs/src/generalpurpose.but`:115 — Assign the full path of the file specified to user variable $x. If the path portion of the parameter is not found, the error flag will be set and $x will be empty. If /SHORT is specified, the path is converted to the short filename form. However, if /SHORT is not specified, the path isn't converted to its long filename form. To get the long filename, call GetLongPathName using the System plug-in. Note that GetLongPathName is only available on Windows 98, Windows 2000 and above.
```
\c [/SHORT] user_var(output) path_or_file

Assign the full path of the file specified to user variable $x. If the path portion of the parameter is not found, the error flag will be set and $x will be empty. If /SHORT is specified, the path is converted to the short filename form. However, if /SHORT is not specified, the path isn't converted to its long filename form. To get the long filename, call GetLongPathName using the System plug-in. Note that GetLongPathName is only available on Windows 98, Windows 2000 and above.

\c StrCpy $INSTDIR $PROGRAMFILES\PTIS
```

- **Note** in `ptinstaller/Docs/src/basic.but`:89 — \s{Note:} When using the \e{/r} switch, both matching directories and files will be searched. This is always done with or without the use of wildcards, even if the given path perfectly matches one directory. That means, the following directory structure:
```
\c File /r /x *.res /x *.obj /x *.pch source\*.*

\s{Note:} When using the \e{/r} switch, both matching directories and files will be searched. This is always done with or without the use of wildcards, even if the given path perfectly matches one directory. That means, the following directory structure:

\c <DIR> something
```

- **Note** in `ptinstaller/Docs/src/basic.but`:151 — Note that the current working directory can not be deleted. The current working directory is set by \R{setoutpath}{SetOutPath}. For example, the following example will not delete the directory.
```
\c RMDir /REBOOTOK $INSTDIR\DLLs

Note that the current working directory can not be deleted. The current working directory is set by \R{setoutpath}{SetOutPath}. For example, the following example will not delete the directory.

\c SetOutPath $TEMP\dir
```

- **NOTE** in `ptinstaller/Docs/src/compiler.but`:11 — \# NOTE: \PtisInputCharset define cannot be used in a \c block
```
\S1{include} !include

\# NOTE: \PtisInputCharset define cannot be used in a \c block 

\c [/NONFATAL] [/CHARSET=ACP|OEM|CP#|UTF8|UTF16LE|UTF16BE] file
```

- **NOTE** in `ptinstaller/Docs/src/compiler.but`:43 — \# NOTE: \PtisOutputCharset define cannot be used in a \c block
```
\S1{appendfile} !appendfile

\# NOTE: \PtisOutputCharset define cannot be used in a \c block 

\c [/CHARSET=ACP|OEM|CP#|UTF8[SIG]|UTF16<LE|BE>[BOM]] [/RawNL] file text
```

- **Note** in `ptinstaller/Docs/src/bin/halibut/LICENSE`:3 — Note: This version is modified by Robert Rainwater and Amir Szekely
```
Halibut is copyright (c) 1999-2001 Simon Tatham and James Aylett.

Note: This version is modified by Robert Rainwater and Amir Szekely

Permission is hereby granted, free of charge, to any person
```

- **Note** in `ptinstaller/Docs/src/bin/halibut/bk_xhtml.c`:209 — if (!ustricmp(v, L"html4")) g_outputtype = OT_HTML4; // Note: Upstream treats this as "W3C HTML 4.01 Strict"
```
      {
        const wchar_t* v = uadv(source->keyword);
        if (!ustricmp(v, L"html4")) g_outputtype = OT_HTML4; // Note: Upstream treats this as "W3C HTML 4.01 Strict"
        else if (!ustricmp(v, L"html5")) g_outputtype = OT_HTML5;
        else if (!ustricmp(v, L"xhtml1.0transitional")) g_outputtype = OT_XHTML;
```

- **Note** in `ptinstaller/Docs/src/bin/halibut/bk_xhtml.c`:387 — /* Note that this can happen, if you have a cross-reference to before the first chapter starts.
```
    if (p3 == NULL)
    {                           /* for some reason, we couldn't find a section before this paragraph ... ? */
      /* Note that this can happen, if you have a cross-reference to before the first chapter starts.
       * So don't do that, then.
       */
```

- **note** in `ptinstaller/Docs/src/bin/halibut/input.c`:747 — * (note that \# is special in a way, and \nocite takes no
```
     * special commands that define a paragraph type.
     *
     * (note that \# is special in a way, and \nocite takes no
     * text)
     */
```

- **Note** in `ptinstaller/Docs/src/bin/halibut/ustring.c`:206 — /* Note: +1 for the terminating 0, -1 for the initial space in fmt */
```
  }

  /* Note: +1 for the terminating 0, -1 for the initial space in fmt */
  wblk = resize((wchar_t *) blk, len);
  text = mknewa(char, len);
```

- **Note** in `ptinstaller/Source/exehead/exec.c`:1415 — // Note: In Unicode version, we put a WORD in buf1[0] and will write 1 or 2 bytes, depending on FileWriteByte/Word.
```
        if (writeCodPt) // FileWriteByte or FileWriteWord
        {
          // Note: In Unicode version, we put a WORD in buf1[0] and will write 1 or 2 bytes, depending on FileWriteByte/Word.
          ((_TUCHAR *)buf1)[0]=(_TUCHAR) GetIntFromParm(1);
          l=(ansi)?1:sizeof(TCHAR);
```

- **Note** in `ptinstaller/Source/exehead/Ui.c`:1095 — *        Note that trimslashtoend() will nullify "C:".
```
     *   1. GetDiskFreeSpaceEx is always called at least once for large HD.
     *        Even if skip_root() returned NULL (e.g. "C:").
     *        Note that trimslashtoend() will nullify "C:".
     *   2. GetDiskFreeSpaceEx is called with the deepest valid directory.
     *        e.g. C:\drive when the user types C:\drive\folder1\folder2.
```

- **NOTE** in `ptinstaller/Source/exehead/fileform.h`:511 — HBRUSH bkb; // NOTE: Placed above lbStyle for better alignment
```
  COLORREF bkc;
#ifndef MAKEPTIS
  HBRUSH bkb; // NOTE: Placed above lbStyle for better alignment
#else
  INT64 bkb;
```

- **Note** in `ptinstaller/Source/exehead/fileform.h`:553 — #define DELREGKEY_ONLYIFNOSUBKEYS 0x01 // Note: Shifted (stored as 2 in the binary) for compatibility with <= v3.1
```
#define DELREG_VALUE 0 // TOK_DELETEREGVALUE
#define DELREG_KEY 1 // TOK_DELETEREGKEY
#define DELREGKEY_ONLYIFNOSUBKEYS 0x01 // Note: Shifted (stored as 2 in the binary) for compatibility with <= v3.1
#define DELREGKEY_ONLYIFNOVALUES  0x02
//      DELREGKEY_SAMVIEWMASK     REGROOTVIEWTOSAMVIEW(REGROOTVIEW32|REGROOTVIEW64) // Reserved for KEY_WOW64_xxKEY, cannot be used as flags!
```

- **NOTE** in `ptinstaller/Source/Tests/winver.nsi`:459 — DetailPrint "NOTE: NT4 SP1 client/server test is inaccurate"
```

	!insertmacro TestWinVer "Windows NT4 SP1" NT4 1 client nt ; Server can only be detected by reading the registry, cannot be performed in test environment.
	DetailPrint "NOTE: NT4 SP1 client/server test is inaccurate"

	#### WINDOWS NT4 SP6 Server
```

- **NOTE** in `ptinstaller/Include/Win/Propkey.nsh`:10 — ;NOTE: This list is incomplete
```
WTypes.h
**************************************************/
;NOTE: This list is incomplete
!define VT_EMPTY     0
!define VT_NULL      1
```

- **Note** in `launcher/ui/LaunchMenu.cpp`:49 — // Note: We use APPLICATION global as logic handler
```

		// Connect actions
		// Note: We use APPLICATION global as logic handler
		QObject::connect(normalLaunch, &QAction::triggered, [instance] { APPLICATION->launch(instance); });
		QObject::connect(normalLaunchOffline,
```

- **NOTE** in `launcher/ui/MainWindow.cpp`:722 — // NOTE: this is done so the accounts button text is not set to the accounts menu title
```
	ui->accountsMenu->clear();

	// NOTE: this is done so the accounts button text is not set to the accounts menu title
	QMenu* accountsButtonMenu = ui->actionAccountsButton->menu();
	if (accountsButtonMenu)
```

- **NOTE** in `launcher/ui/MainWindow.cpp`:1048 — // NOTE: This loop only processes one dropped file!
```
void MainWindow::processURLs(QList<QUrl> urls)
{
	// NOTE: This loop only processes one dropped file!
	for (auto& url : urls)
	{
```

- **NOTE** in `launcher/tasks/Task.cpp`:131 — // NOTE: only fall through to here in end states
```
		}
	}
	// NOTE: only fall through to here in end states
	m_state = State::Running;
	emit started();
```

- **NOTE** in `launcher/minecraft/PackProfile.h`:168 — // NOTE: used ONLY by MinecraftInstance to provide legacy version mappings from instance config
```
	std::shared_ptr<LaunchProfile> getProfile() const;

	// NOTE: used ONLY by MinecraftInstance to provide legacy version mappings from instance config
	void setOldConfigVersion(const QString& uid, const QString& version);

```

- **Note** in `launcher/minecraft/BackupManager.cpp`:38 — // Note: This is a rough estimate for UI display. Actual size may vary.
```
{
	// Rough estimation based on typical Minecraft instance sizes
	// Note: This is a rough estimate for UI display. Actual size may vary.
	qint64 size = 0;
	if (includeSaves)
```

- **NOTE** in `launcher/minecraft/LaunchProfile.cpp`:435 — // NOTE: If we have jar mods, we use the modified jar allocated by ModMinecraftJar step
```
	if (m_mainJar)
	{
		// NOTE: If we have jar mods, we use the modified jar allocated by ModMinecraftJar step
		//       The modified jar is expected to be named "minecraft.jar" in the search path.
		if (m_jarMods.size() && !moddedJarSearchResultPath.isEmpty())
```

- **TODO** in `launcher/minecraft/PackProfile.cpp`:166 — // TODO @RESILIENCE: ignore invalid values/structure here?
```

	// cached
	// TODO @RESILIENCE: ignore invalid values/structure here?
	component->m_cachedVersion	 = Json::ensureString(obj.value("cachedVersion"));
	component->m_cachedName		 = Json::ensureString(obj.value("cachedName"));
```

- **Note** in `launcher/minecraft/PackProfile.cpp`:717 — // Note: This method intentionally uses no precision for row indices -
```
}

// Note: This method intentionally uses no precision for row indices -
// items are indexed by position, not by any floating-point value
Qt::ItemFlags PackProfile::flags(const QModelIndex& index) const
```

- **NOTE** in `launcher/translations/POTranslator.cpp`:186 — // NOTE: PO header has empty id. We skip it.
```
	{
		auto strStr = QString::fromUtf8(str);
		// NOTE: PO header has empty id. We skip it.
		if (!id.isEmpty())
		{
```

- **Note** in `launcher/translations/TranslationsModel.cpp`:611 — // Note: Qt translations may not be present in all installations (e.g., custom builds).
```
	// otherwise install new translations
	bool successful = false;
	// Note: Qt translations may not be present in all installations (e.g., custom builds).
	// This is a best-effort attempt; the app continues working without Qt translations.
	d->m_qt_translator.reset(new QTranslator());
```

- **NOTE** in `launcher/minecraft/auth/MinecraftAccount.cpp`:193 — // NOTE: this doesn't do much. There was an error of some sort.
```
		case AccountTaskState::STATE_FAILED_SOFT:
		{
			// NOTE: this doesn't do much. There was an error of some sort.
		}
		break;
```

- **Note** in `launcher/minecraft/auth/AuthFlow.cpp`:193 — // Note: StepResult::Succeeded means "step succeeded, continue flow"
```
{
	// Map step result to flow state
	// Note: StepResult::Succeeded means "step succeeded, continue flow"
	// The flow itself only succeeds when all steps complete (pipeline empty)
	const auto flowState = stepResultToFlowState(result);
```

- **Note** in `launcher/minecraft/skins/SkinUpload.h`:51 — // Note this class takes ownership of the file.
```
	using Ptr = shared_qobject_ptr<SkinUpload>;

	// Note this class takes ownership of the file.
	SkinUpload(QString path, QString variant);
	virtual ~SkinUpload() = default;
```

- **NOTE** in `launcher/minecraft/mod/tasks/LocalModParseTask.cpp`:60 — // NOTE: ignore stupid example mods copies where the author didn't even bother to change the name
```
			details.mod_id = firstObj.value("modid").toString();
			auto name	   = firstObj.value("name").toString();
			// NOTE: ignore stupid example mods copies where the author didn't even bother to change the name
			if (name != "Example Mod")
			{
```

- **NOTE** in `launcher/modplatform/flame/PackManifest.h`:74 — // NOTE: the opposite to 'optional'
```
		int projectId = 0;
		int fileId	  = 0;
		// NOTE: the opposite to 'optional'
		bool required = true;

```

- **Note** in `launcher/ui/dialogs/CreateShortcutDialog.ui`:200 — <string>Note: If a shortcut is moved after creation, it won't be deleted when deleting the instance.</string>
```
    <widget class="QLabel" name="noteLabel">
     <property name="text">
      <string>Note: If a shortcut is moved after creation, it won't be deleted when deleting the instance.</string>
     </property>
    </widget>
```

- **Note** in `launcher/ui/themes/ThemeManager.cpp`:181 — // Note: Themes with same name in different directories are distinguished by load order.
```
	}

	// Note: Themes with same name in different directories are distinguished by load order.
	// The first loaded theme takes precedence.

```

- **NOTE** in `launcher/ui/themes/SystemTheme.cpp`:73 — // NOTE: SystemTheme is reconstructed on page refresh. We can't accurately determine the system palette here
```
	m_themeName	  = isDefaultTheme ? "system" : styleName;
	m_widgetTheme = styleName;
	// NOTE: SystemTheme is reconstructed on page refresh. We can't accurately determine the system palette here
	// See also S_NATIVE_STYLES comment
	if (S_NATIVE_STYLES.contains(m_themeName))
```

- **NOTE** in `launcher/ui/widgets/FileIgnoreProxy.h`:68 — // NOTE: Sadly, we have to do sorting ourselves.
```
  public:
	FileIgnoreProxy(QString root, QObject* parent);
	// NOTE: Sadly, we have to do sorting ourselves.
	bool lessThan(const QModelIndex& left, const QModelIndex& right) const;

```

- **NOTE** in `launcher/ui/widgets/FileIgnoreProxy.cpp`:68 — // NOTE: Sadly, we have to do sorting ourselves.
```
FileIgnoreProxy::FileIgnoreProxy(QString root, QObject* parent) : QSortFilterProxyModel(parent), m_root(root)
{}
// NOTE: Sadly, we have to do sorting ourselves.
bool FileIgnoreProxy::lessThan(const QModelIndex& left, const QModelIndex& right) const
{
```

- **todo** in `launcher/ui/widgets/FileIgnoreProxy.cpp`:205 — QStack<QModelIndex> todo;
```
			QModelIndex doing	  = rootIndex;
			int row				  = 0;
			QStack<QModelIndex> todo;
			while (1)
			{
```

- **todo** in `launcher/ui/widgets/FileIgnoreProxy.cpp`:211 — if (!todo.size())
```
				if (!node.isValid())
				{
					if (!todo.size())
					{
						break;
```

- **todo** in `launcher/ui/widgets/FileIgnoreProxy.cpp`:217 — doing = todo.pop();
```
					else
					{
						doing = todo.pop();
						row	  = 0;
						continue;
```

- **todo** in `launcher/ui/widgets/FileIgnoreProxy.cpp`:226 — todo.push(node);
```
				{
					// continue processing cover later
					todo.push(node);
				}
				else
```

- **todo** in `launcher/ui/widgets/FileIgnoreProxy.cpp`:254 — QStack<QModelIndex> todo;
```
		QModelIndex doing = index;
		int row			  = 0;
		QStack<QModelIndex> todo;
		while (1)
		{
```

- **todo** in `launcher/ui/widgets/FileIgnoreProxy.cpp`:260 — if (!todo.size())
```
			if (!node.isValid())
			{
				if (!todo.size())
				{
					break;
```

- **todo** in `launcher/ui/widgets/FileIgnoreProxy.cpp`:266 — doing = todo.pop();
```
				else
				{
					doing = todo.pop();
					row	  = 0;
					continue;
```

- **todo** in `launcher/ui/widgets/FileIgnoreProxy.cpp`:272 — todo.push(node);
```
			}
			emit dataChanged(node, node, { Qt::CheckStateRole });
			todo.push(node);
			row++;
		}
```

- **NOTE** in `launcher/ui/pages/modplatform/TexturePackModel.cpp`:91 — // NOTE: Logic duplicated from meta 'texturepacks' trait due to lack of index file access.
```
			for (auto&& version : m_version_list->allVersions())
			{
				// NOTE: Logic duplicated from meta 'texturepacks' trait due to lack of index file access.
				//        Avoiding download of every version file for check.
				if (auto ver = version->asComparableVersion(); ver <= maximumTexturePackVersion())
```

- **NOTE** in `launcher/ui/pages/instance/ManagedPackPage.cpp`:418 — // NOTE: Don't use 'm_pack.id' here, since we didn't completely parse all the metadata for the pack, including this
```

	QMap<QString, QString> extra_info;
	// NOTE: Don't use 'm_pack.id' here, since we didn't completely parse all the metadata for the pack, including this
	// field.
	extra_info.insert("pack_id", m_inst->getManagedPackID());
```

- **Note** in `launcher/ui/pages/instance/McClient.cpp`:132 — // Note: >>> means that the sign bit is shifted with the rest of the number rather than being left alone
```

		// Erase theses 7 bits from the value to write
		// Note: >>> means that the sign bit is shifted with the rest of the number rather than being left alone
		value >>= 7;
	}
```

- **NOTE** in `launcher/ui/pages/instance/WorldListPage.cpp`:105 — // NOTE: Minecraft uses the same placeholder for servers AND worlds
```
			if (iconFile.isNull())
			{
				// NOTE: Minecraft uses the same placeholder for servers AND worlds
				return QIcon::fromTheme("unknown_server");
			}
```

- **Note** in `launcher/ui/pages/global/LauncherPage.cpp`:82 — // Note: InstSortMode is defined here as it's only used by LauncherPage sorting preferences.
```
#include <QProcess>

// Note: InstSortMode is defined here as it's only used by LauncherPage sorting preferences.
enum InstSortMode
{
```

- **Note** in `launcher/ui/pages/modplatform/flame/FlamePage.cpp`:238 — // Note: Empty versions could be due to 3rd-party distribution being disabled or network issues
```
				qWarning() << "Failed to cache versions for the current pack!";

			// Note: Empty versions could be due to 3rd-party distribution being disabled or network issues
			if (m_current->versionsLoaded && m_ui->versionSelectionBox->count() < 1)
			{
```

- **Note** in `launcher/ui/pages/modplatform/flame/FlamePage.cpp`:264 — // Note: Empty versions could be due to 3rd-party distribution being disabled or network issues
```
	}

	// Note: Empty versions could be due to 3rd-party distribution being disabled or network issues
	if (m_current->versionsLoaded && m_ui->versionSelectionBox->count() < 1)
	{
```

- **Note** in `launcher/ui/pages/modplatform/import_ftb/ImportFTBPage.ui`:22 — <string>Note: If your FTB instances are not in the default location, select it using the button next to search.</string>
```
     </property>
     <property name="text">
      <string>Note: If your FTB instances are not in the default location, select it using the button next to search.</string>
     </property>
     <property name="alignment">
```


---

Notes:
- `trivial`: likely simple fixes (typos, docs).
- `implementable`: small code changes may resolve.
- `complex`: needs design review or larger refactor.
- `unknown`: manual review recommended.
